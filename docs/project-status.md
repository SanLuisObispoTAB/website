# SLOTAB Website — Project Status & Backlog

A living document the board updates between sessions to keep
decisions, pending work, and external inputs in one place.

> **Last updated:** 2026-08-24 *(🎉 **It works — a real donation notification reached the Membership VP, six minutes after the fix deployed.** Production log: last `404` at **00:09:54**, then **00:16:05** `POST /api/webhook 200` with `[square-webhook] donation email … : **sent**`. One line, four answers: the alias is serving, the **signature verified** (so the multi-URL list did its job and no env var had to change), the event was correctly classified a *donation* so the **#187 to-do checklist** is what went out, and — open since #177 — **Resend has now delivered**. The mailer was never broken; nothing had ever been able to reach it. That was going to wait on Trina's Monday report to answer; a real donor's gift answered it first, and that gift is now in Dannene's inbox with its designation, split and checklist attached — because the fix went into the code instead of into a queue behind a volunteer — #192. Earlier: ✅ **Confirmed and fixed: the Square webhook had been 404-ing since the day it was built.** Erik read the subscription — `wbhk_f7a4…`, Enabled, `payment.updated` — and its URL is **`https://slotab.org/api/webhook`**, one segment short of the route. The delivery log is wall-to-wall `404` with retries queued, which means **the #177 sponsorship email has never fired once**, with all four env vars correctly set the whole time. Fixed **in code, not just the dashboard**, because Square had pending retries and a deploy beats waiting on a volunteer: `/api/webhook` is now a working **alias** for the same handler, and `SQUARE_WEBHOOK_URL` accepts a **list**, with the canonical/alias sibling derived automatically — so **no env change is needed**, whatever it currently holds, and correcting the dashboard later is safe rather than breaking. Verified seven ways with the real production strings, including that a **forged signature still 401s** and a signature for an **unconfigured URL still 401s**. `/board` now lists the accepted URLs so the next mismatch is visible rather than silent — #191. Earlier: 🔴 **Reading the logs instead of the doc immediately found a live fault: something POSTs to `https://slotab.org/api/webhook` every few minutes and gets a 404 — that path does not exist here.** Ours is `/api/square/webhook`. The pattern is a **retrying sender**, not a scanner: triplets ~2s apart, repeating all hour, 23 POSTs in the retained window. **If that is Square, every event it has ever sent has 404'd** and the #177 sponsorship email has never fired — despite all four env vars being correctly set, which is exactly why it looked fine from Vercel. Unproven: the logs carry no user-agent, so the sender is not identified. **Check: Square Dashboard → Developers → Webhooks → read the notification URL**, and note the fix is *two* changes — the dashboard URL and `SQUARE_WEBHOOK_URL`, identical strings, because Square signs `notificationUrl + body`. The bad-signature log now prints both URLs so a mismatch is visible rather than guessed — #189. Earlier: 🏛️ **The AD approved the Hall of Fame page and widened it: a STANDING fund, not a Class of 2026 drive.** *"I do believe the goal is to fund the Hall of Fame in general and not just this class. The alumni are an integral part of the Tiger Community."* Every single-class string is out of the band (the thermometer sentence included — it now reads off a `goalLabel` field), **alumni get their own block** between the ladder and the buttons, and the donate button label is data so its next rename is a CMS edit. An Alumni Membership tier was deliberately **not** invented — there is no Alumni level on the 2026-27 sheet (E8). **The goal figure is coming from the AD**; the thermometer render path was proven with temporary numbers so it lights up the moment that lands — #186. Earlier: 👀 **The AD’s review copy is live at an unlisted URL on production** — `https://slotab.ravens-peak-consulting.com/preview/hof-fund`. A Vercel preview link could not be used: SLOHS district wifi **blocks `vercel.app`** (the reason the alias exists) and preview deployments sit behind **Vercel SSO**. `main` therefore carries all of #184 **except** the wiring into `/hall-of-fame` and the home page, which stay in PR #6 until the AD signs off — so the public site is unchanged bar the Hall of Fame option in the donate dropdown. Unlisted, not secret: no links in, out of the sitemap, `noindex` on itself, and a loud draft banner saying the figures are unconfirmed — #185. Earlier: 🏆 **The Hall of Fame can now be donated to.** `hall-of-fame` is a real designation — a **third** shape of gift alongside teams and the general fund, taking **100%** with no 75/25 split — and `/hall-of-fame` has a designed dark fundraising band whose ladder is written as **objects** (nameplate · medallion · award · one inductee fully honored), each card deep-linking its amount. Carries **Trina’s QuickBooks class `Donation: Hall of Fame`** verbatim into the Square note, the board report and a new CSV column, plus an **"in honor of"** tribute field. **The thermometer is built but hidden — it needs a real goal from the AD** (`goalDollars` in `hof.json`), and the dollar-to-item mapping needs an engraver quote before it is repeated in print — #184. Earlier: 🛍️ **Merch store link recovered** — `slotab-apparel.square.site/slotab-merch`, a *third* Square storefront in a *second* account. The shop button is back. **Per-item links are not possible**: the store was checked item by item and carries **none** of the nine sport-specific designs in the gallery — those go through coaches and ParentSquare — #183. Earlier: 👕 **`/merch` rebuilt so each design links to its own product page** — structure is in, but the **store URL is genuinely lost**: `slotab-3.square.site` carries 37 products and *zero* apparel, so merch lives on a different Square Online site in the club's other account. Blocked on that link — #182. Earlier: 🚀 **The Square checkout is LIVE** — a real general-membership donation went through. `/membership` cut from **eight** routes to the donate page down to **two**, one per tier group, which also retires a sponsorship checkout that never collected the business's name — #181. Earlier: 📬 **Weekly report to Trina every Monday**, trailing 7 days, via Vercel Cron rather than a GitHub Action — deliberately, to avoid giving the unrestricted Square token a second home. `/membership` now links into the matching half of the donate form via **`?tab=`**, and that same parameter gives the flyer a **QR code straight to general membership** — decoded back to confirm it scans — #180. Earlier: ☎️ **Trina call outcomes.** Donate box now **defaults to the Sponsorship tab**. Her reported "sponsorship checkout fails" is **not a sponsorship bug** — general membership is equally not going through Square, it just hides it by falling back to the old storefront, which sponsorship deliberately has no fallback to. **One flag fixes both.** A **weekly emailed report** is requested and needs two decisions before building — #179. Earlier: 🏷️ **Sponsor wall corrected to 9 — Dannene's email was right on both discrepancies**: Crocker is Silver, and SLO Safe Ride is a Gold sponsor the newer list had dropped entirely. Newer did not mean more reliable — #178. Earlier: 📧 **Square now emails the Membership VP the moment a sponsorship is paid**, with the perk checklist rendered from the board's own sheet and an explicit "logo/website ON FILE or NEEDED" line. Triggered by a **signed webhook**, not the thank-you page, because that page only renders if the buyer comes back. Signature verification tested four ways including a tampered body. **Inert until four env vars are set** — see the README. Also: 🏷️ **the sponsor wall is rebuilt for 2026-27**, 54 sponsors down to 8, which required making `logo` optional (half of them have no artwork yet, and they render as wordmarks) and exposed two layout bugs a full wall had been hiding — #176, #177. Earlier: 🧭 **The Teams menu is back to current-season-only — #174 reverted at Erik's call (#175)**; out-of-season sports are reached via **All Teams**, which lists all 22 team pages by season. The two nav files are byte-identical to their pre-#174 state, and the revert was scoped by hand so the rosters in the same commit were untouched. Also worth remembering: 🚨 **a push to `main` can silently fail to deploy** — #173 reached GitHub but Vercel never queued a build for it, no failure recorded, and the rosters sat invisible until an empty commit re-fired the webhook. Earlier: 📋 **Rosters are live for Cheer (48) and Flag Football (39)**, now that the club is cleared to list first name, last name and grade. The roster type had **never been used by any team** and broke on first contact with real data — `number` and `position` are now optional (cheer issues no jersey numbers; JV positions aren't settled) and a new `squad` field groups Varsity/JV. The flag football roster **settled the one-team-or-two question**: two squads, so the page drops "Varsity" from its title. Also: 🧭 **the Teams menu now lists every team with a page**, grouped by season in a 3-column panel — it previously showed only in-season teams, which made Baseball unreachable from the nav in September — #173, #174. Earlier: 📸 **The Dance and XC photo drops are in — 5 of 9 files used, and the #111 re-export trap fired for real.** `SLOHigh_DanceTeam_…jpg` turned out to be the *same image* as `gdance-team-2025.jpg` (RMSE 0.0017) and was correctly not re-imported; `photo-intake`'s byte check would have let it through. Dance now leads with a **real pom-routine hero**, so the posed exception from #171 lasted about three hours. Three files rejected on inspection, not on filename. XC gained its **first current-season photos**. Showcase EXIF also proved that event is **Winter, not Spring** — #172. Earlier: 💃 **Dance finally has its own page at `/teams/dance`**, built from Coach Mandi Mettler's own 2026-08-18 mail — closing a question open since #98. The work was **deleting a `permanent: true` redirect** in `next.config.ts`, not adding a file: #132's `/teams/dance → /teams/cheer` rule silently served Cheer from the new route until it came out, and a 308 that shipped stays cached in browsers that saw it. First real use of `seasons: ["Fall","Winter"]`, so Dance lists under both season blocks. **Spring and gender are deliberately not claimed**, and the action slots are **empty on purpose** per #108 — the seven photos Mandi attached can't be pulled through the Gmail connector and need a manual `photo-inbox/` drop — #171. Earlier: 📄 **Tiers rebuilt from the board's FINAL 2026-27 sheet**, which reverses several instructions from earlier the same day: Gold banners go 1→**3** locations, **Silver's banner returns** at 2, sports designation flattens to **3/3/3/0/0** (Tiger Pride and Varsity credit none), **HUDL video** added to Champion and Gold, and **all monthly prices are gone**. The black *For Businesses* band is removed from `/membership` — #170. Earlier: 🧾 **The Treasurer's donation report is now a page in the Board Hub**, not a CLI script — Trina picks a date range and gets the per-sport 75/25 table plus CSV, no terminal. The CLI version was **deleted** rather than kept alongside it. Its CSV route sits under `/api`, which the proxy does **not** gate, so it checks the session itself — #169. Earlier: 🚩 **Banners are now Champion and Gold only, both one year** — Silver's banner withdrawn, the two-year terms cut to one. **The Square storefront still describes the old arrangement** and needs the same edit, or the two surfaces drift apart again a day after they were aligned — #168. Earlier: 💛 **Each sponsorship tier now shows what it buys** — a gold box under the tier selector, listing the selected package and updating as you click, with the ad-perks badge and the sports-credit line generated from the same field the picker enforces — #167. Earlier: 🏢 **The donate box now has two doors** — *General membership* (the default) and *Business sponsorship*, the latter with tier selection, the sport picker sized to the tier, and **two ways to pay: card or "request an invoice"**, because a sponsor who needs a PO is not a lost sponsor — #166. Earlier: 🏈 **Business sponsorships can now name the sports they are credited to** — up to 5/4/3/2/1 by tier, matching what the storefront has always offered. The limit is enforced **server-side** from the same field the card renders, so a disabled checkbox is a courtesy rather than the control — #165. Earlier: 🟢 **#162 CLEARED — the integration now points at the account that actually takes the club's donations** (`6YKJFPPASVNW3`, location *SLOTAB General Memberships*, ACTIVE with card processing). Launch is a one-value change again. Also: `/membership` finally states **how many sports each sponsorship credits** — 5/4/3/2/1 — a perk missing from the site since #88 — #164. Earlier: ✅ **The storefront and the website finally agree on every tier** — Erik recovered the second account's credentials and re-tiered it: all seven levels now match `sponsor-tiers.ts` to the dollar, pass counts corrected, headings on `2026-27`. **This closes #143**, live since the re-tier. Surfaced a perk the website has never mentioned: sponsors can credit **N sports**, and nothing on our side says so — #163. Earlier: 🚨 **THERE ARE TWO SQUARE ACCOUNTS, and the production token is from the wrong one.** The storefront taking every donation today belongs to merchant **`6YKJFPPASVNW3`**; the token in Vercel is from merchant **`1RZ6X80KDPPNR`** (SLOTAB + SLOTAB Merch). Flipping `SQUARE_LIVE_DONATE=true` would **redirect all donation income to a different Square account** — different deposits, and probably invisible to Trina's QuickBooks connector. **Launch blocked until resolved** — #162. Earlier: 🧮 **Trina reconciles from a QuickBooks connector — which means the sport designation never reaches her ledger at all.** New **`npm run square-report`** produces the per-sport 75/25 allocation the connector cannot, keyed on metadata rather than item names, excluding abandoned checkouts and board test gifts. Testing caught that a paid payment link is **`OPEN`, not `COMPLETED`** — the first cut would have reported **zero** for every real gift — #161. Earlier: 🧾 **The Square checkout now names the level too** — *"Volleyball (Girls) — SLOTAB donation (Gold Sponsor)"*. It had to go in the **line item name**: `variation_name` and the line `note` are accepted by the API and invisible to the buyer, and a second $0 line replaces the heading with a generic "Checkout". **🟡 Trina should know Square's item report now keys on sport *and* level** — #160. Earlier: 🪜 **One ladder now spans both halves of the sheet** — businesses currently enrol through the same donate form as families, so a gift is placed against the sponsorship tiers as well as the general memberships: $500 → Varsity, $5,000 → Gold Sponsor, $10,000+ → Champion Sponsor. The five levels above $125 were previously unreachable from the page where the money is given — #159. Earlier: 🏷️ **The donate page was naming membership levels that do not exist** — a private ladder from a May research draft, sharing the word *Champion* with `/membership` while disagreeing about it **by a factor of six**. Now derived from the same array `/membership` renders. **Open question for the board: every gift from $125 to $5,000 now reads "Family"** — #158. Earlier: ⏱️ **The handoff panel no longer flashes past** — it was being destroyed by its own redirect, taking with it the one thing on it a donor may need to act on. Now a 5-second countdown with a Continue button, and **no countdown at all** for a donor who asked to stay anonymous — #157. Earlier: 📝 **Name and phone now carry across to Square, not just email** — and the reason they didn't is that Square **rejects the phone format a parent actually types**. `805-555-1212` fails; it needs a country code. Normalised to E.164 server-side — #156. Earlier: 🧾 **Retail furniture removed from the Square checkout** — the *Add coupon* box Erik spotted is gone, along with the REWARDS loyalty opt-in, and a real SLOTAB support address now appears on the one screen in the flow that isn't ours — #155. Earlier: 🧹 **Monthly giving pulled from the donate UI on both surfaces.** Now that the button really takes money, a "Monthly recurring" tab on a checkout that can only charge once would take a monthly pledge and **silently bill it a single time**. Gated behind one flag with every branch intact, so restoring it when the Subscriptions API lands is a one-line change — #154. Earlier: 🏷️ **Board test gifts are now marked as tests.** Review runs on **production** credentials into the **live Merch location** while Trina is asked why `SLOTAB` is deactivated — so a reviewer's $25 is a real charge that would otherwise be indistinguishable from a parent's donation. Marked in the payment note and metadata, deliberately **not** in the line item, so the checkout still looks exactly like production — #153. Earlier: 🎯 **Root cause found: the `SLOTAB` Square location is `INACTIVE`.** It keeps its card-processing capability while deactivated, so my own check showed a **green box for a blocked configuration** — corrected to require `ACTIVE` *and* the capability. **The fix is in Square, not the code:** reactivate `SLOTAB`, or point at `SLOTAB Merch` and accept donations commingling with merch — #152. Earlier: 🔴 **Production checkout blocked by Square, not by us:** *"This business is currently not accepting payments."* The token and location are **correct** — the link is created fine and Square refuses at the checkout page, which means the location lacks `CREDIT_CARD_PROCESSING`. The config panel now reports that per location, so the fix is visible rather than guessed — #151. Earlier: 🔎 **The preview page now self-checks the Square configuration** — swapping in a production token is a *three*-variable change, and the public endpoint answers a flat `503` whether the credentials are perfect or quietly broken, so there was no way to tell those apart. Green/red read-out, names the wrong variable, lists the locations the token can actually see — #150. Earlier: 👀 **Board review unblocked: a production token can now go in without launching to donors.** New `SQUARE_LIVE_DONATE` flag separates *"Square works"* from *"donors use it"*, so the board can review the **real production checkout** through the preview route while `/donate` stays on the storefront. Square's sandbox structurally cannot show a payable checkout, so the preview page now warns about the testing panel instead of pretending — #149. Earlier: ✍️ **Correction to #147: the sandbox checkout DID go live, and Erik caught it.** The domain sitting on an older build was not an accident or a misconfiguration — it was a deliberate **Instant Rollback** after he saw the sandbox checkout serving on the real `/donate`. The guard in #146 was therefore fixing a **real production incident**, not a hypothetical — #148. Earlier: 🚀 **Phase one is LIVE and verified in production — and the guard is doing its job**: `slotab.org` refuses to mint checkouts, so donors fall back to the storefront exactly as intended. Discovered while debugging a push error that **nothing pushed since 18 Aug 17:00 had actually been serving** — the domain was pinned to a manual redeploy, which by luck is why the unguarded build never reached a donor — #147. Earlier: 🛡️ **Shipping phase one early is now safe** — the live site **refuses to run on sandbox credentials**, because nothing would have errored: a real parent would have been handed a sandbox checkout, believed they had donated, and moved no money. `/donate` therefore keeps using the old storefront until a production token lands, while a **secret preview page** exercises the new flow on the live site. Also: the sponsorship button **silently did nothing** for Erik — a bare `mailto:` on a $10,000 enrolment path — now a panel that always shows the address and the message — #146. Earlier: ⚡ **Phase one of the real Square integration is BUILT and verified against sandbox** — donations and sponsorships now go through payment links minted server-side, so the amount and the designation are set for the donor and the mispriced storefront items are out of the path entirely. A posted price cannot override a sponsorship tier (tested). **Live on sandbox credentials only; needs a production token to go live** — #145. Earlier: 🔴 **The Square storefront is still selling the pre-#88 sponsorship ladder** — its "Gold" is **$2,500 with four passes and a one-year banner** where this site's Gold promises **$5,000, eight passes, two years**, and its "Platinum" sells the **$10,000 Champion perk list for $5,000**. Businesses are being sent to the website by the Sponsorship lead, so `/membership` now opens with a business enrolment path — routed through the board, deliberately **not** deep-linked to those mispriced items — #143. Also: **the donation designation can no longer be skipped**, boxed in gold and starting unchosen (#142), and **Square cannot be made to pre-fill an amount from a URL** — tested and ruled out (#143). Earlier: **The 404s and the "I’m still seeing the old site" reports were one problem, not two** — the old host set a **31-day** `Cache-Control` on its HTML, so pre-cutover visitors are served the old site from their own disk without ever reaching us, and **five of the eight links on that cached page 404’d here**, trapping them. All 25 legacy URLs now redirect (rebuilt from the Internet Archive, not guessed), the WordPress-era ones carrying `Clear-Site-Data` to evict the stale copy, and the stock Next.js 404 is replaced with a branded one. 🔴 **The old WordPress site is still live and still billing** — #141. Earlier: 🔴→✅ **The live Donate button was a demo stub** — it popped an alert instead of taking money, on a site two days past launch. Now handed off to the existing Square storefront, deep-linked per sport so the designation rides in the transaction; intent Square can't carry is captured by email. **Wants a real-browser spot check** — #140. Earlier: **JV Black / JV Gold split explained on the page** — it's new for 2026-27, which is why last season's shoot has one combined JV photo; new `note` field on squad portraits carries what the picture can't — #139. Earlier: **Video stills ruled out for TCC** — sampled 14 frames across all seven stunt clips and every one is sideline cheer on the football-field track, not competition on a mat; also only 768px against a 1200px standard. No change made, recorded so it is not re-proposed — #138. Earlier: **Coach McDonald's cheer photos landed** — 12 self-labelled shots closed four open items in one go: the Frosh squad is finally represented, all three cheer pages have action heroes, and my guessed winter portrait was replaced by the real one. Seven stunt **videos** came too and were deliberately not used — no video support on team pages — #137. Earlier: **Fall and Winter cheer split into separate pages** — #132 had both seasons pointing at the same Fall page, so a parent clicking Cheer in Winter landed on the wrong programme; three distinct cheer pages now, and the Fall page regained a verified team portrait — #136. Earlier: **Triaged the inherited backlog against the actual repo** — five rows closed as already done, four re-measured with real coverage numbers, four turned into questions (including a thermometer goal for a thermometer that does not exist) — #135. Earlier: **Competitive Cheer given real photos** — its `/teams` card was falling through to the generic student-section fallback; now uses the two comp-cheer shots already sitting unreferenced in the library, one per surface — #134. Earlier: **Weekly schedule feed diagnosed — the scraper works, the department's sheet is empty**; correcting my own earlier "scraper is broken" call, and hardened so a real break now fails loudly instead of looking like a quiet week — #133. Earlier: **Tiger Cheer restructured to Coach McDonald's brief** — moved to its own `/teams/cheer` URL with a redirect, now runs **Fall *and* Winter** via new multi-season support (it would otherwise have vanished from the nav in December), and **Traditional Competitive Cheer** has its own Winter page — #132. An inbox audit also found **five fall coaches who never replied**. Earlier: **SLOHS announcement incorporated** — the **Fall Sports Parent Meeting is TOMORROW, Mon 17 Aug, 6-8 PM**, now on the calendar; `/volunteer` gained the two contact emails it was missing and the new 25% Tiger Bites team revenue share, and a stale "grab a shift" for an event three days past is gone. 🔴 **The President's announcement says the September board meeting is the 16th; the site says the 14th** — one is wrong, needs Andria — #131. Earlier: **Girls Flag Football intake applied** — coach bio, five assistants, wishlist; costs now accept ranges rather than flattening a coach's "$3,000-4,000" to one invented figure. Flagged: the page says **Varsity** but three of five staff are **JV** coaches — #130. Earlier: **New Tiger Pride tier on the sponsor wall + four sponsors placed** — and #128's SLO Credit Union logo was **404ing on the live homepage** because I added the gold copy but not its `/sponsors/alpha/` twin; fixed, plus two pre-existing breakages of the same kind found by a sweep — #129. Earlier: **SLO Credit Union live on the Gold wall** — $5,000 brought in by Cheer, the first sponsorship the new site can be credited with; logo trimmed and linked — #128. Earlier: **17 XC state-meet photos live** — classified by eye off a contact sheet (9 boys / 7 girls / 1 coaching staff), and the job surfaced a real `photo-intake` bug: EXIF orientation was being stripped without `-auto-orient`, so rotated photos shipped sideways — fixed, and older photos want a spot-check — #127. Earlier: **Cross country updates from Coach Steinmaus** — two new assistants, team leaders as captains, Athletic.net link; roster held, and note its spreadsheet carries **Student IDs** that must never reach the site — #126. 17 state-meet photos staged for curation. Earlier: **Tiger Cheer intake applied** — coach bio, seven assistants, wishlist, links and the team's own tagline; **the doc's embargo on five unannounced captains was caught and respected**, and the 48-name roster is held pending a board call on publishing athlete names — #125. 🔴 The new **$5,000 SLO Credit Union** sponsorship is blocked: no logo in the file, the name is a credit union not a bank, and $5k is Gold not Champion. Earlier: **Tiger Cheer's banner + team portrait pulled — both were the dance team**; the page runs photoless above the fold until the coach supplies real cheer shots, and the `/teams` card is unaffected — #123. Same sitting: **Cheer is now "Tiger Cheer" with no gender designation** — new liaisons Rollins + Ross, and `gender` made optional end to end so a team can carry no label at all; photos deliberately untouched pending the team's cull list — #122. ✅ **Vercel Web Analytics is confirmed collecting** — 10 visitors / 52 page views, curve starting at the toggle date; the earlier API `404` was this session's connector lacking read scope, not the site. Still open: whether the four **custom section events** record on the Hobby tier — that panel wasn't visible — #124. Earlier: 🔴 **Vercel Web Analytics was never switched on** — the client fires cleanly but the beacons are being discarded, so every event since 2026-08-11 is gone and the board still has no readable numbers. One dashboard toggle, Erik-side, nothing retroactive — #119. Also fixed the `first`-visit date being stamped in UTC, which showed evening visitors tomorrow's date on `/privacy` — #120; and Erik confirmed the sponsorship scroll event works in a real browser, closing the last open item from #101 — #121. Earlier: shipped `/board/cms-access` — the last stranded work, re-derived against current `main` rather than replayed; fixed `decap-setup.md`, which had been pointing the board at the wrong repo entirely — #118. Same sitting: post-launch repo hygiene: dropped a mislabelled 765 MB photo commit that could never have been pushed, gitignored `Team Photos/` + Word lock files, and tagged the baselines `launch-2026-08-15` / `baseline-2026-08-16` — #117. Also shipped the dead pre-Tiger `Header`/`Footer` deletion that had been stranded behind it — #116. Earlier: 🚀 **slotab.org is LIVE and serving from the apex** — cutover complete, canonicals consistent, the ravens-peak alias still `noindex` — #115. Remaining: **confirm domain auto-renew before 2026-09-19**, Search Console + sitemap, the paired OAuth/`base_url` swap, delete three stale Cloudflare ACME records, and cancel GoDaddy hosting 48h after go-live.)*
>
> *Previously: 2026-08-11 — Step-by-step cutover runbook at `docs/dns-cutover-runbook.md`, written against the verified Vercel account state — #102. Earlier: cookie consent + analytics (#101); cutover code side, host-aware indexing (#100) — **DNS itself still pending, Erik-side**; #94–#99.*
>
> Update this doc after each board meeting or working session.

---

## How to use this doc

- **Decisions Log** — append new rows; never edit prior entries.
- **Current Build State** — flip the status box when work lands.
- **Active Backlog** — work in priority order. Move done items to "Built" below.
- **Open Questions** — anything we're explicitly waiting to decide.
- **External Inputs Pending** — assets/credentials we're waiting on.

Status legend: ✅ done · 🟡 in progress · 🔴 blocked · ⏳ deferred · ❌ removed

---

## Decisions Log

| Date | # | Decision | Owner |
|---|---|---|---|
| 2026-08-24 | 192 | **It works, end to end, and the proof arrived six minutes after the deploy.** #191 shipped the `/api/webhook` alias at 23:26 UTC on the reasoning that Square had **queued retries** and a deploy beats waiting on a dashboard edit. The production runtime log settles it: **last 404 at 00:09:54** on the old deployment, then at **00:16:05** on `dpl_DmNScxbWkLLJT4WNrJBb7H5M8buD` — `POST /api/webhook 200`, and beneath it **`[square-webhook] donation email for HVLTzFNhAoluSFeVGCo985Od28EZY: sent`**. That single line closes **four** separate questions at once. **(1)** The alias is serving — Square's event reached the handler instead of a 404. **(2)** The signature **verified**, so the multi-URL list did exactly the job it was built for and no environment variable had to change. **(3)** The order-metadata fetch worked and the event was correctly classified a *donation*, so what went out was the **#187 to-do checklist**, not a bare receipt. **(4)** — and this is the one that had been open since #177 — **`sent`, not `skipped`. Resend has now delivered.** The mailer was never broken; nothing had ever been able to reach it. That question was going to wait for Trina's Monday report at 07:00 PT to answer, and instead a real donation answered it first. **A real donor's gift, which had been silently reaching nobody, is now sitting in the Membership VP's inbox with its designation, split, level and to-do list attached** — and it got there because the fix went into the code rather than into a queue behind a volunteer. Worth keeping as the argument for that call the next time it comes up. | Erik + Claude ✅ |
| 2026-08-23 | 191 | **Confirmed: the Square webhook has been 404-ing since the day it was built, and the fix went in the code rather than only the dashboard.** #190's hypothesis was right. Erik opened the subscription — **`wbhk_f7a4328dae9f45fe862c689a09679839`**, production, Enabled, `payment.updated`, API version `2026-08-19` — and its URL is **`https://slotab.org/api/webhook`**, one path segment short of the route that implements it. The delivery log is wall-to-wall `404 payment.updated` with retries queued. **The #177 sponsorship handoff has therefore never fired once**, while all four environment variables sat correctly set, which is exactly why every surface reported health. **Two changes, and the choice between them matters.** The dashboard could simply be corrected — but Square had **pending retries at that moment**, a deploy lands in a minute and a dashboard edit waits on a volunteer, so the queued events would likely have expired first. So `/api/webhook` is now a **working alias** for the same handler: one implementation, two URLs, the same signature check, nothing to drift. **And the deeper fix: `SQUARE_WEBHOOK_URL` now accepts a comma-separated list, with the canonical/alias sibling derived automatically.** One env var that has to match a setting in somebody else's dashboard is a single point of *silent* failure — that is the whole story of this bug — and a small accepted set is not. **It does not weaken the check:** every candidate is still compared against an HMAC computed with the secret key, so an attacker gains nothing from there being more than one accepted URL; they still have to forge a signature. Verified seven ways, using the real production strings: Square posting to the alias signed with the dashboard URL ✅; the same when the env var names only the *canonical* URL (sibling rescue) ✅; a comma list of both ✅; the two post-correction shapes, once Erik moves the dashboard to the canonical path ✅✅; and — the ones that matter — **a forged signature 401s on the alias**, and **a signature computed for an unconfigured URL 401s** too. **Net effect: no environment change is needed, whatever `SQUARE_WEBHOOK_URL` currently holds, and correcting the dashboard later is safe rather than breaking.** `/board` now lists the accepted URLs so the next mismatch is visible instead of silent, reading them from a shared `lib/square-webhook.ts` so the panel and the verifier cannot disagree — a green tick beside a broken integration is the failure this whole thread has been about. The alias is **safe to delete** once the dashboard points at the canonical path, but check the delivery log for 404s first. | Erik + Claude ✅ |
| 2026-08-23 | 190 | **Applying #189's own rule immediately found a live fault: something has been POST-ing to `/api/webhook` every few minutes and getting a 404.** #189 said *check, don't cite* — so instead of restating that the notification pipeline was fine, I read the production runtime logs. `/api/square/webhook` had **9 hits, all GET/HEAD → 405** in one eleven-second burst (crawlers; the route is POST-only). But the same table showed **`/api/webhook` with 23 POSTs, every one a 404** — and **that path does not exist in this repo**; `grep` finds no reference to it anywhere. The timing is the tell: **triplets roughly two seconds apart, repeating every few minutes across the whole retained hour** — 22:06:36/38/43, 22:07:38/40/43, 22:09:40/42/45, 22:13:42/44/47, 22:21:44/46/48, 22:37:47/48/50. A scanner sweeps many paths once; **a webhook sender retries one path forever**, which is what this looks like — and the gaps between clusters **widen** (1min → 2 → 3 → 2 → 4 → 2 → **16**), which is exponential backoff. Scanners do not back off. **Hypothesis, stated as one:** the Square subscription's notification URL is missing the `/square` segment, so every event Square has sent has 404'd and the #177 sponsorship handoff has **never fired** — which would explain Dannene receiving nothing but Square's own thin notifications while the configuration looked complete. **What cannot be proven from here:** Vercel's log lines carry no user-agent or body, so the sender is unidentified, and `/api/webhook` is exactly the URL a person types from memory when wiring one up by hand. Deliberately **not** "fixed" by adding a second route at that path — inventing an entry point to match a config nobody has read is how you end up with two webhooks and no idea which is live. **The check is one page of the Square dashboard.** One code change did land: the bad-signature warning now prints **both** the `SQUARE_WEBHOOK_URL` it verified against **and** the URL the request actually arrived at, because Square signs `notificationUrl + body` and a URL mismatch fails *every* signature while the message "bad signature" sends you looking at the key, which is almost never it. Neither value is secret; the signature key is still never logged. **Note the fix is two changes, not one** — correct the URL in Square *and* set `SQUARE_WEBHOOK_URL` to the identical string. | Erik + Claude 🔴 |
| 2026-08-23 | 189 | **Three wrong statements to the board in one sitting, from one bad habit: reading an External Inputs row as a fact instead of as a dated claim.** Erik, twice: *"Those env variables were previously set when we created the Monday report for Trina"*, then *"SQUARE_WEBHOOK_SIGNATURE_KEY and SQUARE_WEBHOOK_URL also already exist."* Both correct. **All four variables were in Vercel the whole time**, so the sponsorship handoff has been live since #177 shipped and the donation checklist goes live the moment #187 merges — I told the board twice it was blocked, and #188 "corrected" it to a two-variable blocker that was also wrong. **The root cause is structural, and it is in this doc.** *Decisions Log* and *External Inputs Pending* look alike and behave nothing alike: a decisions row is **history** — "on 21 Aug we chose X" is true forever, and append-only is right for it — while an External Inputs row is a **claim about the current state of something outside this repo**, true on the day it was written and possibly false an hour later, with nothing in the repo to notice. I applied Decisions-Log trust to an External Inputs row. Worse, the row was **refutable from inside this repo**: `src/app/api/cron/weekly-report/route.ts` had been calling the same `sendEmail` for two days, and a grep would have caught it. **New convention, now in `CLAUDE.md` under its own heading:** never cite an External Inputs row as present-tense fact — re-verify, or say "as of `<date>`, unverified"; every row carries an **as-of date** and a **"how to check now"** (command, URL or page); check the repo before believing the doc; **if the check can be automated, build it and point the row at it**; and suspect any row older than the work around it. **The #188 panel turns out to be the right answer to the wrong question** — it was built to show a blocker and instead shows there was never one, which is exactly what a live readout is for and exactly what a hand-written row cannot do. The resolved row now carries both checks: `/board` for the live state, and `curl -X POST https://slotab.org/api/square/webhook -d '{}'` for anyone outside the board password — **401 means configured** (it rejected an unsigned caller), **503 means not**. Neither could be run from the Claude session: egress to `slotab.org` is refused by the sandbox network policy and the Vercel MCP does not expose environment variables, so **for this class of fact the answer has to live where a human can look** — which is the whole argument for the panel. 🟡 **One thing still genuinely unverified, and stated as such:** whether Resend has ever *delivered*. An unverified `EMAIL_FROM` domain fails **422**; the first scheduled proof is **Trina's Monday report, 2026-08-24 07:00 PT**. | Erik + Claude ✅ |
| 2026-08-23 | 188 | **Two of the four "blocking" env vars had been set for two days, and nothing in the repo could tell anyone.** Erik, reading the #187 write-up: *"Those env variables were previously set when we created the Monday report for Trina."* He is right, and the error is mine twice over — the PR, the status doc and the `Last updated` line all told the board the notification emails were blocked on **four** variables when **`RESEND_API_KEY` and `EMAIL_FROM` were set on 2026-08-21**, the same afternoon, because **#180's weekly report to Trina runs on the same `sendEmail`**. The External Inputs row I was reading was written with **#177**, hours before #180 made half of it untrue, and I repeated it twice without checking it against a route sitting in the same repo. **What is actually outstanding is two variables, both off one page of the Square dashboard** — `SQUARE_WEBHOOK_SIGNATURE_KEY` and `SQUARE_WEBHOOK_URL`, from Developers → Webhooks after subscribing to `payment.updated`. No signup, no provider decision, nothing to buy. **The fix is not just correcting the row, because the row will go stale again.** Both emails fail *safe* — they log loudly and send nothing — so the failure mode is **silence**, which is indistinguishable from a quiet fundraising week; and the configuration lives in Vercel where no document can see it. So `/board` now carries a **three-state readout** built from the variables themselves: 🔴 off / 🟡 half on (*"Trina's Monday report will send; the per-payment emails will not"*) / ✅ on, naming the missing variables and the exact Square dashboard path — **names, never values**, and behind the board password. Same reasoning as the Square config panel in #150: a 503 meaning *"not configured"* and a 503 meaning *"misconfigured"* are the same 503, and guessing between them costs a working day. `isEmailConfigured()` had existed unused since #177 and is now what the panel reads. The page needed **`export const dynamic = "force-dynamic"`** — without it Next prerenders `/board` and bakes the variable state in at *build* time, so the readout would have kept saying "off" after someone set them, which is the same failure wearing a green tick. Logic moved to `lib/notification-config.ts` rather than living in the page, so it could be exercised: five states verified, including **webhook set but mailer missing** (correctly "off", naming only what is absent) and **half a pair — `EMAIL_FROM` absent while `RESEND_API_KEY` is present**. 🟡 **One thing the panel cannot tell anyone:** the mailer is configured but has **never actually sent** — the cron is `0 14 * * 1` and the first Monday run is **2026-08-24**. A Resend `EMAIL_FROM` on an unverified domain fails **422**, and that will show up as Trina's report not arriving. | Erik + Claude ✅ |
| 2026-08-23 | 187 | **Square's donation notifications lost their detail because *we* changed the rail, not because Square changed anything — so the detail comes back from us.** Dannene, 2026-08-23, with before/after screenshots: *"I am not getting enough information now when someone donates."* She is right, and the cause is #181. Until then a donation was a **Square Online storefront sale**, which raises an **order** notification — item, quantity, buyer name, buyer email. Since the cutover the site mints a **payment link**, and a payment link raises a **payment** notification, which says an amount and a card. **Both options in Erik's question were considered and only one is real:** there is nothing to report to Square and no setting to change — the contents of those two notification types are fixed by Square, and asking them to put the storefront's fields on a payment notification is asking for a product change, not a fix. So `/api/square/webhook` now handles `kind: "donation"` alongside the sponsorship branch it was built for in #177 — the extension its own comment invited — and emails the Membership VP the donor, the designation, **the split in dollars** (`$112.50 to the team · $37.50 to the general fund`, not a percentage she has to apply), the level, any *in honor of*, the QuickBooks class for a named fund, and the Square payment ID. **The split is not recomputed here.** `allocateCents` was extracted from `square-report.ts` and is imported by both, so this email and the Treasurer's report cannot disagree about how much reached Water Polo — and a named fund keeps 100% for free rather than by remembering to. **Three fields had to be put into the transaction before they could come out of it**, because Square's payment object does not carry them: the donor's **name as they typed it** (Square's is the *cardholder*, and the email now shows both when they differ), their **phone** (which is why the sponsorship email had said *"Phone: not provided"* on every sponsorship since #177 — one line, fixed in passing), and the **donor-wall checkbox**. That last one was reaching Dannene *only* if the donor clicked the `mailto:` on the handoff panel — a preference whose default is *publish my name* and whose wrong answer is the one thing here that is awkward to undo. It now rides in order metadata, which also means the anonymous donor is **no longer held on the handoff panel** (the #157 hold has nothing left to protect) and no longer told their choice "can't travel through that checkout", which since this change is false on the minted path. **The metadata cap is now the real constraint and is documented as such:** Square allows **ten** order-metadata pairs and a Hall of Fame gift with a level, a tribute, the test marker and the donor block sits on **exactly ten** — verified, 10/10, nothing dropped. The donor block is written **last** so an overflow costs a phone number in an email rather than `designation` or `test`, the two keys the Treasurer's report is keyed on, and `sanitizeMetadata` now **logs loudly** instead of dropping in silence. `wall` is stored only on opt-out for the same reason — spending a capped slot to say "behave normally" is the wrong trade. **The screenshots arrived after the build and confirm the diagnosis exactly.** Before (17 Aug, 4:08 PM): a **SLOTAB-branded** *"You received an order!"* naming **Gold Business Sponsorship**, the scoreboard-advertising perk text, **the two sports credited — Dance and Cross Country** — and the buyer with **name, phone AND email**. After (22 Aug, 6:33 PM): a **Square-branded** *"Payment Link"*, an amount, and a customer **name and email — no phone, and nothing about what the money was for**. Two things that were not visible from the code: the customer's **phone** is the field that disappeared (the new notification does still carry name and email, so the loss is narrower than "fewer details" suggested — but it is the designation, which is the whole point), and the notification renders the literal words **"Payment Link"** where the item name should be, which means **our carefully-built line item name is not what that email reads** — the #160 assumption that the name is the durable visible record holds for the *checkout page*, not for this email. One long shot added on the back of that: the payment link's own `description` field is now set to the same string, since it is the next most likely candidate for what that email renders. **Unverified — proving it needs a real charge in the club's account** — and it is a bonus rather than the fix; it definitely does name the link in the dashboard's payment-link list, where every entry currently reads the same. **Then Erik redirected it, and the redirect was the making of it: "email membership a detailed list of items to do."** The first cut was a *receipt* — it told her a gift arrived and left her to work out what it obliged. The obligations are knowable, so the mail is now a **numbered checklist**: the thank-you (escalating to *"written tax acknowledgement — REQUIRED"* at the **$250** IRS threshold, with the club's EIN in the line), the donor wall in whichever of its three states applies, **whatever the gift's level owes**, which **team liaisons** to tell and their email addresses, and passing a tribute to the fund committee. Nothing in it is written out: level items render **verbatim** from `sponsor-tiers.ts` through the same `perkAction` keyword table the sponsorship mail uses (now exported rather than copied), and the liaisons come from the team's own page data via a new `team-liaisons.ts` — so revising the board's sheet revises the email. **The checklist immediately exposed three things the receipt had hidden.** (1) A gift of **$500 or more lands on a *sponsorship* tier** by `levelForGift` — the #159 combined ladder — so a $2,500 *donation* was about to be told to order banners and issue six All-Sport passes. It now asks **FIRST** whether the gift is even meant as a sponsorship, and the perks item is explicitly conditional on the answer; the question is **suppressed entirely for a named fund**, because the Hall of Fame page promises 100% to the induction and inviting banners against it contradicts what the donor was told. (2) **The acknowledgement letter cannot say "no goods or services were provided"** when the level hands over passes worth real money — that overstates a donor's deduction on the club's own letterhead — so a level carrying benefits switches the wording to a **quid pro quo** warning pointing at Trina for the figures. (3) The shared perk note read *"issue via GoFan to the contact below"*, which is true in the sponsorship mail and **false in this one**, where the contact block is above; reworded direction-neutral. Two formatting fixes fell out of the same reading and moved to a shared `notification-format.ts`: the sponsorship mail had been printing **raw ISO timestamps** and **E.164 phone numbers** at a volunteer — invisible until now only because that mail's phone line had said *"not provided"* on every sponsorship since #177. Exercised against the real route with a stubbed Orders API: **eight cases** — team gift, anonymous gift on a different cardholder's card, Hall of Fame with tribute as a board TEST charge (checklist shown but explicitly disowned: *"DO NOT WORK THE LIST BELOW"*), a $2,500 team gift reaching Silver Sponsor, a gift to a team with **no liaison on file** (says so rather than going quiet), a sponsorship regression for the shared perk table, a donation with no designation (**refuses to send** rather than mailing a blank), and an off-website card-reader payment (acknowledged, ignored). #177's four signature cases re-verified unchanged. 🔴 **None of this sends yet** — the same `RESEND_API_KEY` / `EMAIL_FROM` / webhook variables that have kept the sponsorship email inert since #177 keep this inert too, and that row is now blocking two features rather than one. | Erik + Claude ✅ |
| 2026-08-24 | 186 | **The AD approved the page and changed its scope: it is a standing Hall of Fame fund, not a Class of 2026 drive — and alumni are the audience.** His words: *"I like this page. I do believe the goal is to fund the Hall of Fame in general and not just this class. The alumni are an integral part of the Tiger Community and we should continually reach that audience to stay involved with SLOTAB."* Two changes follow, and the second is the one that matters. **(1) Scope.** Every string naming a single class is gone from the band — kicker, lead, body, the $2,500 level, the levels note, and the thermometer's own sentence, which used to read *"for the Class of 2026"* and is now driven by a new `goalLabel` field. The reason to care is a date, not a preference: a page built around one class is stale the morning after that class is inducted, and it quietly tells an alum who graduated in 1994 that this year's drive is not about them. The headline he liked — *"The night ends. What they take home doesn't."* — was left alone, because "the night" was never specific to one class. **(2) Alumni.** They now get their own block, gold-ruled, sitting between the ladder and the buttons: *"Tiger alumni: this one is yours."* Placed there deliberately — it is the reason-to-give for the audience he named, so it belongs against the button, not buried in body copy. **What was deliberately NOT built:** an Alumni Membership tier. `alumniMembership` has sat in `hof.json` behind an empty `joinUrl` since the page was built, because the 2026-27 sheet has no Alumni level (E8, still open) — and inventing a tier on the strength of "reach that audience" would promise a product the club cannot deliver. The alumni ask is therefore the fund itself plus the induction, both of which are real today. **Also now data, not code:** the donate button label (`ctaLabel`, used by the band *and* the save-the-date strip so two buttons on one page can never disagree) — it has been renamed twice in three days, which is exactly the sort of string that should never need a deploy. The 75/25 line moved out of `body` into its own `splitNote` field, because as a third paragraph it orphaned under a two-column grid. **The thermometer is still hidden** (`goalDollars: 0`) but the AD is supplying a figure; the render path was exercised end to end with a temporary $6,400 / $25,000 to prove the bar, the percentage, the ARIA values and the new sentence all come out right before that number lands. | Erik + AD + Claude ✅ |
| 2026-08-21 | 185 | **The AD's review copy is an unlisted page on production, because a Vercel preview link is unusable on the school network — twice over.** Erik asked for a URL to send the Athletic Director, who will open it on SLOHS district wifi. The obvious answer, the PR's Vercel preview deployment, fails for two independent reasons: that network **blocks the `vercel.app` address** — which is precisely why the `slotab.ravens-peak-consulting.com` CNAME alias exists, and `docs/editor-onboarding.md` already lists it as the fix when "the editor page won't load at all" — and preview deployments sit behind **Vercel SSO**, which the AD has no account for (confirmed: fetching the preview returned a `302` to `vercel.com/sso-api`). So the review copy has to be served by the **production** deployment, which the alias also serves, and the URL to send is `https://slotab.ravens-peak-consulting.com/preview/hof-fund`. **What that means for what shipped:** `main` now carries the whole of #184 **except** the two changes that would make it public — the band is **not** wired into `/hall-of-fame` and the home page link is **not** in. Those stay in PR #6 until the AD blesses it, so the live site is unchanged apart from a Hall of Fame option appearing in the donate designation dropdown, which is real and correctly tracked either way, and which the review page's own buttons need in order to demo the flow. **Hidden means unlisted, not secret:** nothing links to it, it is excluded from `sitemap.ts`, and it sets `robots: index:false` for itself because the proxy only marks non-canonical hosts `noindex` and this route answers on `slotab.org` too (same belt-and-braces as `/donate/preview/[slug]`, #149). An unlisted URL is not an access control, so the page opens with a loud **draft banner** stating the figures are unconfirmed — that is what makes a stumbled-upon copy harmless rather than a page quoting engraver prices nobody has quoted. The banner also does the asking: the goal figure and the per-level costs, the two things the AD is the one to settle. The Booster Bash strip was extracted to `HofCeremonyStrip` so the review page shows the **real** strip rather than a copy that would drift the next time the venue moves — it already moved once (#106). **Delete `/preview/hof-fund` when the band goes live**; a stale second copy of a fundraising page is exactly the kind of thing that gets found and donated against a year later. | Erik + Claude ✅ |
| 2026-08-21 | 184 | **The Hall of Fame is now something you can give to — a designation, not just a page.** The AD wants to fund trophies and commemorative gear for the Class of 2026, inducted at the Bash on **October 10**. Two halves. **(1) The plumbing.** `hall-of-fame` is a real donation designation, and a *third* shape of gift: not a team (so **no 75/25 split**) and not the general fund (so it must never be reported as one). Getting that wrong in any one of the four places that decide it would book three quarters of a Hall of Fame gift against some team's allocation, so the fund list and the split rule now live in one file (`src/app/data/special-funds.ts`) that `DonateForm`, the payment-link route, the Treasurer's report and `/thank-you` all read. **Trina's QuickBooks class — `Donation: Hall of Fame` — is carried verbatim** into the Square payment note, the board report and the CSV (new `quickbooks_class` column), so the row she reconciles names the class she posts to instead of a slug. The form also collects an **"in honor of"** name, which rides in order metadata; giving in a coach's or teammate's name is the whole lever on a memorial-style fund and nothing else in the checkout could capture it. **A latent trap was found and closed while wiring the fallback:** the storefront has no Hall of Fame tile, so a gift taken on the 503 path lands under GENERAL ATHLETICS with the designation *and* the tribute lost — the donor is now told, and both are added to the pre-written email, which is #140's warning brought back for the case it was written for. **(2) The design.** `/hall-of-fame` gains its only dark band — black ground, the archival black-and-white helmets-up shot, gold rules — because an ask styled like the paragraph above it gets skimmed with the paragraph above it. The ladder is written as **objects, not amounts** ($50 nameplate · $100 medallion · $250 award · **$500 one inductee fully honored** · $1,000 · $2,500), each card deep-linking its own figure via a new `?amount=` parameter, since the gap between "I should give $250" and a typed 2-5-0 is where donations die. Four photos already in the library were used, three of them previously unreferenced. **`goalDollars` ships at 0 and the thermometer is therefore hidden** — the code is built and one number turns it on, but an invented goal on a 501(c)(3) donation page is worse than no goal. Everything is Decap-editable at *Hall of Fame → Hall of Fame Fund*. **Revised the same day on Erik's feedback:** the band was below the fold, so it is now the **first block on the page** — the save-the-date sits directly beneath it and the mission copy moves below both, since context above an ask is what pushed the ask off the screen. The CTA reads **"Fund the HOF Class of 2026"** throughout, and a second copy of it now sits in the band's head: the band runs ~1500px on a laptop and every button was at its foot, a full ladder's scroll from the headline that did the persuading (measured: primary CTA moved from y=1795 to y=872). The ceremony strip's own button stopped pointing at `#fund` and goes straight to the donate form — that band is now above it, and a button that scrolls a reader back to what they just read is worse than no button. **Because the checkout is live (#181), this designation takes real money the moment it merges** — the first Hall of Fame gift is worth spot-checking in Square and on `/board/square-report` to confirm it lands on its own row, unsplit, carrying the QuickBooks class. | Erik + Claude ✅ |
| 2026-08-21 | 183 | **Merch store link recovered, and the per-item linking Erik asked for turns out to be impossible — the store doesn't stock those designs.** Erik found the URL: **`slotab-apparel.square.site/slotab-merch`**. Note the host — **`slotab-apparel`, not `slotab-3`** — so this is a **third** Square storefront, on a **second** Square account, and neither of the two already in play. `STORE_URL` is set and verified `200`; the *Shop SLOTAB Merch* button is back and the "store not open yet" placeholder is gone. **But the images cannot link to their items.** The store's 39 products were checked against the gallery one by one: it stocks **general Tiger gear** — hoodies, hats, spirit tees, cursive tees, blankets, a stadium chair, a license-plate frame — and carries **not one** of the nine sport-specific designs. The single "Baseball" match is a **snapback cap**, not the shirt. That is consistent with the page's own copy: sport-specific apparel is ordered **through the coach and ParentSquare** in a window at the start of each season, not from the online store. So all nine `url` fields stay empty and render as plain images — a design wrapped in a link to an unrelated product would be worse than no link. The structure from #182 stands ready if any design is ever listed. | Erik + Claude ✅ |
| 2026-08-21 | 182 | **`/merch` rebuilt to link each design to its own product page — and the store URL turns out to be genuinely lost.** Erik: the merch store link is gone, the club is reusing last year's storefront, and the design images should link **directly to the item**, not to a store homepage a parent then has to search. **The structure is in:** each shirt now carries an optional `url`, rendering as a linked tile with a *"Shop this design →"* affordance when set and as a plain image when not — an unlinked design must not look clickable, and wrapping one in a dead anchor is the worse failure. Adding the links is now a pure data edit. **What could not be recovered:** the URL itself. `STORE_URL` was **born empty** in the production rollover (`6c5c281`), so git never held it; the Internet Archive has **no capture** of the old WordPress `/apparel/`, `/slotab-merch/` or `/shop/` pages; and — the decisive check — **`slotab-3.square.site` carries 37 products and not one item of apparel**, only memberships, sponsorships, sport passes, team donations and Hall of Fame. So the merch store is a **separate Square Online site**, almost certainly attached to the `SLOTAB Merch` location in the club's *other* Square account (`1RZ6X80KDPPNR`, #162) — which is exactly where the merch item library was found. **A fourth club asset this month whose location nobody could state from memory.** Meanwhile the page stays honest rather than shipping a dead button: all nine designs render, no `href="#"`, and the note reads that the 2026-27 store link posts here once it opens. New merch photography is coming but not yet supplied. | Erik + Claude ✅ |
| 2026-08-21 | 181 | **Checkout went live, and `/membership` was cut from eight routes to the donate page down to two.** A real general-membership donation completed through the new flow — the first money the club has taken through its own checkout rather than the storefront. **Erik's spelling catch:** the thank-you page read *"enrols"*, British spelling on a US high school's site at the exact moment a parent has just given money. Swept rather than spot-fixed: exactly one donor-facing instance existed (`DonateForm` already said *enrolls*, so the thank-you page was the outlier), plus two strings in the sponsorship handoff email the Membership VP reads — *FULFILMENT CHECKLIST* and *"needs actioning"*. Identifiers, the `sponsor-fulfilment.ts` filename and `SPONSOR_FULFILMENT_EMAIL` were left alone deliberately: **that env var is already set in Vercel, and renaming it would silently drop the recipient back to the default**. Nobody reads a symbol name. **Then the buttons.** With the flag on, `/membership` was rendering **eight** ways to reach `/donate`: five per-tier *Sponsor at $X* checkout buttons, a *Join or Donate Online* button in the How-to-Join block, a page-level *Donate & Become a Member*, and the two new tier-group CTAs. Now two, one under each tier group, each preselecting the matching tab. **Removing the per-tier checkout was a correctness win, not only tidiness:** it posted `tierId` and `sports` and nothing else, so a business paying from `/membership` gave Square a cardholder name but **no company** — the gap `businessName` closes in the donate box. Consolidating on one flow means every sponsorship now carries the details the Membership VP needs. `checkoutEnabled` plumbing, the `SportPicker` import and the local state all went with it; `"use client"` stays because the *print this sheet* button still needs it. Mailing address, print link and every tier card are untouched. | Erik + Claude ✅ |
| 2026-08-21 | 180 | **Weekly report emailed to the Treasurer, deep links from `/membership` into the donate form, and a flyer QR code.** All three from the Trina call. **(1) Weekly email** — `/api/cron/weekly-report`, Mondays 14:00 UTC, trailing seven days, to `slotabtreas@gmail.com` alone; the wider board pulls the same figures on demand at `/board/square-report`. **Run as a Vercel Cron rather than a GitHub Action on purpose**: the repo already schedules work in Actions, but an Action would mean copying the **production Square token into GitHub secrets**, and that token is a *personal* access token Square documents as unrestricted — refunds and payouts included. It already lives in Vercel; a second home doubles the places the club's most dangerous credential can leak from for no gain. Guarded by `CRON_SECRET`, failing closed when unset — verified 401 with no header, 401 with a wrong secret, 200 with the right one. Reuses the `sendEmail` mailer the other session added in #177, so no new provider. **A design fix along the way:** the route originally returned early when email was unconfigured, which threw away `email.ts`'s deliberate behaviour of logging the full body so the content stays recoverable; it now lets the mailer handle that case. **And `designationLabel` moved into the shared lib** — the email was printing raw slugs (`girls-volleyball`) where the board page printed *Volleyball (Girls)*, which is two reports to trust instead of one. **(2) `?tab=general` / `?tab=sponsorship`** deep-link either half of the donate box, with a CTA under each tier group on `/membership` — the page is the catalogue, `/donate` is where you act, and naming the tab in the URL means a sponsor lands on the right form regardless of which tab is default that week. **(3) A flyer QR code** to `https://slotab.org/donate?tab=general`, generated with error-correction **H** (30% damage tolerance, for something that will be photocopied and folded) and **decoded back with an independent reader to confirm it resolves to the exact URL** — a QR that does not scan is only discovered after the print run. SVG for print, PNG for layout tools, plus a `&src=flyer` variant so the board can measure what the flyer actually did. | Erik + Claude ✅ |
| 2026-08-21 | 179 | **Outcomes from Erik's call with Trina: default tab flipped, and the "sponsorship is broken" report diagnosed as the flag, not a bug.** **(1) The donate box now opens on the Sponsorship tab** at her request — business sponsorship is the board's current fundraising push. Trade-off recorded rather than buried: parent gifts are the overwhelming majority of traffic, so this costs the many a click to save the few one, and the initial value is a single word to flip back if the numbers say it is not paying for itself. **(2) The reported failure — *"Card payment isn't switched on yet"* — is not a sponsorship defect.** Verified against the live site: **both** paths return the same `503`. General membership only *appears* to work because `DonateForm` catches the 503 and quietly hands off to the old Square storefront; the sponsorship panel has no such fallback **by design**, because #143 refused to send businesses to storefront items that were then a full tier mispriced. So the honest statement is that neither path is going through the new checkout, and the one that looks fine is the one hiding it. Confirmed the cause by running with `SQUARE_LIVE_DONATE=true`, where **both** paths mint links immediately — no code change needed, the flag is the whole difference. Worth noting the original reason for withholding the sponsorship fallback **has now expired**: the storefront was re-tiered correctly in #163, so a fallback could be added if launch slips. **(3) A weekly report emailed to `slotabtreas@gmail.com` is requested** and is not yet built — it needs a sender decision and raises a credential question worth settling deliberately rather than by default (see External Inputs). | Erik + Claude ✅ |
| 2026-08-21 | 178 | **Both sponsor discrepancies resolved in the VP's favour — Crocker is Silver, SLO Safe Ride is Gold and back on the wall.** #176 built the wall from the list Erik was handed, which disagreed with Dannene's 17 Aug "Business Sponsors" email in two places; that list was newer, so it was used, and both differences were logged rather than silently accepted. Dannene confirmed **her email was right on both**. **Crocker Coastal Construction** moves Tiger Pride → **Silver** (the direction that made it worth querying — the newer list had it dropping $2,500 → $1,000). **SLO Safe Ride** is restored to **Gold** with the website it already carried; its logo and transparent variant were both still in `public/sponsors/gold/`, exactly as #176 predicted, so the restore was four lines. Wall is now **9** sponsors: Gold 2 · Silver 2 · Tiger Pride 2 · Varsity 3. **The lesson worth keeping:** the newer document lost a paying Gold sponsor outright. Newer did not mean more reliable, and the only reason it was caught is that #176 cross-checked the list against the VP's email instead of taking it as authoritative. | Erik + Dannene ✅ |
| 2026-08-21 | 177 | **Square now hands new sponsorships to the Membership VP automatically — by webhook, not by the thank-you page.** Erik's ask: when a purchase confirms, email `slotabmembership@gmail.com` everything needed to start fulfilling the perks, and say explicitly whether we already hold a logo and website URL. **The trigger had to be a webhook.** `/thank-you` only renders if the buyer returns to the site after paying, and plenty don't — tab closed, or the redirect eaten by a mail client. A sponsorship generating no handoff because someone closed a tab is precisely the silent failure this removes; Square retries a webhook, and signs it. New `POST /api/square/webhook` verifies Square's HMAC-SHA256 over `notificationUrl + rawBody`, refuses outright unless **both** `SQUARE_WEBHOOK_SIGNATURE_KEY` and `SQUARE_WEBHOOK_URL` are set (an unauthenticated endpoint that sends mail on request is a spam relay aimed at a volunteer's inbox), and acknowledges everything that isn't a COMPLETED `payment.updated` so Square stops retrying. Verified against the running server: **bad signature 401, absent signature 401, valid signature 200, and a body tampered with after signing 401** — that last one is the proof the signature actually covers the payload. **The checklist is not a second copy of the perks.** It renders `sponsor-tiers.ts` **verbatim**, so when the board revises the sheet the email follows on its own; a hand-kept "what a Gold sponsor gets" is the drift that produced #143 and #145. Per-perk action notes (banner locations, GoFan passes, scoreboard artwork, HUDL) are keyword-matched and **additive** — a keyword that stops matching loses its note, never its line. **The logo/website check needed a real matcher.** The name a buyer types and the name on the wall are entered by different people months apart. My first attempt used substring containment and **silently failed the very case it claimed to handle**: "Crocker Construction" is not a substring of "Crocker Coastal Construction" because the extra word lands in the middle. Now token-subset, with a **one-word name requiring exact equality** — "Center" is a subset of both *The Center Studio & Boutique* and *Primary Eyecare Center*, and picking wrong would report someone else's logo as on file. Nine matcher cases pass, including that guard. **Privacy call:** the unconfigured-mailer path logs the subject but **withholds the body in production**, because it carries a named person's email and phone and Vercel logs outlive the request and are readable by anyone with project access. Full text still prints in development, where it needs reading. Board test charges (`metadata.test`) still send, subject-prefixed `[TEST]` and headed with a do-not-action banner, so the flow can be exercised without inventing work. Zero new npm dependencies — `fetch` and `node:crypto`. **Inert until four env vars are set**, and it says so loudly rather than failing quietly; README carries the setup. | Erik + Claude ✅ |
| 2026-08-20 | 176 | **The sponsor wall is rebuilt for 2026-27 — 54 sponsors down to the 8 the club actually has.** `sponsors.json` already said `season: 2026-2027` while holding last season's roster, which is the worst of both: it looked current and wasn't. Now Gold: SLO Credit Union · Silver: Premium Meat Market · Tiger Pride: Primary Eyecare Center, San Luis Obispo Heart, Crocker Coastal Construction · Varsity: The Center Studio & Boutique, Carstairs Energy, Britton Real Estate. **Champion has nobody yet**, so both walls now filter empty tiers rather than printing a heading over nothing, and the Champion carousel already self-hid. **Four of the eight have no artwork, which the data model forbade** — `Sponsor.logo` was required. Now optional, and a logo-less sponsor renders as a **wordmark of their business name**: they have paid, and being absent from the wall is the one outcome that is actually wrong. Applied to all three consumers (membership wall, homepage wall, Champion carousel) and keyed on `name` rather than `logo`, which would have collided on `undefined`. **A sparse wall exposed two latent layout bugs that sixteen logos per tier had been hiding:** `minmax(X, 1fr)` has no upper bound, so a one-sponsor tier stretched its tile across the full 1160px row — that hit the **Gold logo**, not just the wordmarks; and nothing bounded logo height on the membership wall, so Tiger Pride rendered as a **108/202/259px** row. Tiles are now capped and centred, and per-tier `max-height` matches what the homepage wall already did — every tier measures a **zero height spread**. **Two fixes found in passing:** the Decap tier dropdown offered only Champion/Gold/Silver/Varsity, so **the board could not select "Tiger Pride"** — the tier three of these eight sit in; and the wall's intro still read "2025–2026" and described the wall as last season's, now driven off `SPONSOR_SEASON`. Logos for the ~48 non-renewing sponsors stay in `public/sponsors`, so a returning business is a one-line change. | Erik + Claude ✅ |
| 2026-08-20 | 175 | **#174 reverted — the Teams menu shows the current season only, and All Teams carries the rest.** Erik's call, made after seeing the all-teams menu live: out-of-season sports are reached through the **All Teams** link, not the dropdown. This reverses #174 in full, so that row stands as written but its change is no longer in the product. **What I had flagged as the cost of season-filtering — Baseball being unreachable from the nav in September — is accepted, deliberately**, because `/teams` lists all 22 team pages grouped by season (verified: Baseball under Spring, plus 6 "Coming soon" entries for the pageless sports). The menu's job is the current season; the index's job is everything. `TigerNav.tsx` and `tiger.css` were restored to their pre-#174 state and are now **byte-identical to the baseline** — checked with `git diff`, not assumed — so none of the grouping machinery lingers: no `NavHeading`/`isHeading`/`isLink`, no `orderedSeasons` import, no `.grouped` 3-column CSS, no right-edge anchoring, no heading selectors in either the desktop panel or the mobile one. **Nothing else from #173 was disturbed** — the two commits shared a changeset, so the revert was scoped to the two nav files by hand rather than reverting the commit, which would have taken the Cheer and Flag Football rosters (and `slotab.css`'s roster styles) down with it. Menu now: All Teams + the 10 Fall teams, Dance included, Baseball correctly absent. | Erik ✅ |
| 2026-08-20 | 174 | **The Teams menu now lists every team with a page, grouped by season — it previously showed only the in-season ones.** Erik noticed Dance was missing from the menu and asked for **all** teams to be listed. Two things were true at once: the nav was working exactly as written (Dance *was* in the local Fall list), and the live site does not have Dance at all because nothing has been pushed — but the underlying complaint was real and broader. `teamChildren()` filtered on `teamInSeason(t, navSeason())`, which kept the menu short and timely at the cost of making **Baseball unreachable from the nav in September**. Now it walks `orderedSeasons()` and emits every `hasPage` team under a **season heading**, current season leading. **Teams without a page stay out** — `/teams/<slug>` does not exist for them, so a link would 404; "All Teams" leads the panel and covers those. **A multi-season team is listed once**, under the first season it competes in, so Dance does not appear twice in one menu (the `/teams` index still repeats it per season, which is right there — that page is browsed a season at a time). **The layout needed real work, not just more rows:** 23 links in one column runs off the screen, so the grouped panel is **3 CSS columns** with `break-inside: avoid` welding each heading to its teams — without that a column break lands right after "Winter" and strands the heading at the foot of one column with its teams at the head of the next. It is also **anchored to the trigger's right edge**: "Teams" is the last dropdown in the bar, and a 560px panel opening leftwards ran **13px off a 1280px viewport** — measured, not guessed, and easy to miss by eye. Verified at 1280 and at **1025px, the tightest width the desktop nav renders at** (`.tiger-nav-links` is `display:none` at 1024 and below) — fits with no clipping, all 23 links present. A 2-column fallback I wrote for narrow screens turned out to be **dead code** for that same reason and was removed rather than left in. Mobile panel gets the same headings in one flat column. `isActive` now skips headings — they carry no `href`, and testing one would have thrown. | Erik + Claude ✅ |
| 2026-08-20 | 173 | **Rosters published for Cheer (48) and Flag Football (39), which needed the roster type loosened in three places.** Erik confirmed the club is cleared to list **first name, last name and grade**. Both rosters came from links inside coach mail rather than attachments — Coach McDonald's Google **Doc** and Coach Monroe's Google **Sheet** — which is why these were reachable when the XC roster was not (see below). **The existing `RosterEntry` demanded `number`, `name`, `year` and `position`, all required, and no team had ever used it** — so first contact with real data broke it immediately. Three changes: **`number` is now optional** (cheer issues no jersey numbers at all; flag football's three practice players have no uniform yet), **`position` is optional** (Monroe's JV list has most positions blank — he said so: "don't have definitive positions yet"), and a new optional **`squad`** groups rows, because both programs field more than one team. Squad grouping follows the same shape as `teamPhotos` labels; absent everywhere it renders one flat list exactly as before. Sorting is per squad: numbered athletes by number, unnumbered by name **after** them, so flag football's JV reads as a jersey-ordered list with the not-yet-issued names trailing rather than leading. **`year` renders through a new `formatYear`** — a bare "12" alone in the corner of a card reads as a jersey number or a count, so a numeric grade is labelled "Gr. 12" while free text ("Senior", "Sr.") passes through, mirroring `formatCost` in the same file. CSS: a `no-num` row modifier drops the empty 3.2rem jersey gutter for cheer, and squad blocks get a gold-ruled heading with a count. **Published:** Cheer — Varsity 23, JV Black 12, JV Gold 13, each athlete with stunt position (Base/Flyer/Backspot/Tumbler) and grade. Flag Football — Varsity 17, JV 22. **Not published, deliberately:** the **Dance** roster from the #172 flyer (names only, no grades, and it is *last* season's), and the **boys XC** roster, which is a `.xlsx` **Gmail attachment** and so unreachable — the same wall as the photos in #171. **The flag football roster settled an open question** (see below) and forced a rename: the page was titled "**Varsity** Girls Flag Football" while now listing a JV roster, which contradicts itself. Renamed to **"Girls Flag Football"**, matching what `teams.json`, the nav and the `/teams` card have said all along. | Erik + Claude ✅ |
| 2026-08-20 | 172 | **The Dance and XC photo drops landed the same evening — 5 of 9 files used, and the #111 re-export trap fired exactly as predicted.** Erik dropped the mail attachments into `photo-inbox/` (main checkout — it's gitignored, so it does not exist per-worktree), including `fwddancepic (1).zip`. **The dup check earned its keep immediately:** `SLOHigh_DanceTeam_2025-2026_7RV06387.jpg` (5400x3600, 15 MB) is **the same image as `gdance-team-2025.jpg`**, confirmed at **RMSE 0.0017** against the library copy. `photo-intake`'s byte-level check would have waved it straight through under a new name — the precise failure #111 and #112 describe. Not re-imported. **Every file was opened before use (#112: the filename is not evidence).** Two earned action slots: **`Dance National Showcase-266.jpg`** is a crisp 6278x4185 pom routine — now the **hero**, which retires the posed exception `team-photos.ts` had carried for about three hours in #171 — and **`IMG_1548.jpg`**, a hip-hop routine's closing pose, into the gallery so "In Action" renders. **`IMG_6436 2.jpg`** — the full squad in a kick-line with the mascot at sunset, packed stands — is a second `teamPhotos` entry, *posed*, so it correctly went to a portrait slot rather than the gallery. **Three were rejected on inspection, not on filename:** `Dance National Showcase-137.jpg` is a **multi-team group at Cal Poly** (Cal Poly Dance, CORE DANCE and SLOHS together) — not a SLOHS squad photo and not action; `IMG_9030.JPG` is five dancers posed at a night game, not the squad and not action; `IMG_6548 3.jpg` is a soft 2332x969 wide video still, the same downgrade #138 rejected. **`1.PNG` is not a photo at all** — it's the program flyer, and it carries **the full 19-name 2025-26 roster** plus a fuller Mandi bio. Deliberately not published: the `roster` type needs number/year/position and we have only names, and it is **last season's** roster on a 2026-27 page. Logged under External Inputs instead. **Its EXIF settled half an open question:** both Showcase frames are dated **2026-02-17**, so the **Cal Poly Showcase is a Winter event, not Spring** — `seasons: ["Fall","Winter"]` from #171 stands, and only the AGHS Dance Show is still undated. **Cross country got two 2026 camp shots** (`bXC-camp-run-2026`, `gXC-camp-run-2026`, both 2026-07-27, Nancy Steinmaus running with the girls' squad in the second) — the first *current-season* XC photos on a page whose other 16 are all 2025 state-meet, so they lead the gallery. The second arrived as `image (5).png` and was converted to JPEG before intake. `team-audit`: missing action heroes **4→3**, galleries **7→6**, still **no rule violations**. `photo-usage`: no new duplicate groups, no page showing a photo twice. Hero crop verified by rendering the exact 1200x334 band rather than trusting the preview — `center 40%` puts the dancer's face above the title, where `center 30%` had it behind. | Erik + Claude ✅ |
| 2026-08-20 | 171 | **`/teams/dance` is the Dance team's own page at last — which meant deleting a *permanent* redirect, not just adding a file.** Closes the question open since #98 and reframed in #132: Dance had no page and Coach **Mandi Mettler** was listed nowhere on the site, while the board's own 2026-27 sheet lists her. She wrote directly on 2026-08-18 ("thank you for getting Dance Team added to SLOTAB"), pointed at the school's Dance page as a content source, and named the team's Instagram — so this is built from **the coach's own material**, not inference. **The blocker was `next.config.ts`, not the data.** #132 left a `permanent: true` (308) redirect `/teams/dance → /teams/cheer`, whose own comment said it existed to free the slug "if that page ever gets built". Adding the page while that rule stood would have shipped a route that silently served Cheer — and it did exactly that in testing until the redirect came out. **The rule is now replaced by a comment explaining why it must not come back**, including the honest cost: it was a 308, so anyone who hit `/teams/dance` between 2026-08-16 and today has it cached and still lands on Cheer until their cache clears. Four days' exposure on a site live since 2026-08-15; unavoidable once a permanent redirect ships. **First real use of the `seasons` machinery** — `seasons: ["Fall", "Winter"]`, so Dance lists under *both* season blocks on `/teams` and in the nav. That array is what `seasons.ts` was built for in the Tiger Cheer case, but Cheer ended up solved with two separate team entries, so nothing had exercised it until now. Verified: Dance appears under Fall Season *and* Winter Season. Fall + Winter are stated outright on the school page (football games, basketball games); **Spring is deliberately not claimed** — the Cal Poly Showcase and AGHS Dance Show sound spring-ish but neither is dated, and guessing would put the team in a season block it may not compete in. **Gender deliberately omitted**, using the optional-gender path #122 built for Tiger Cheer. E16 recorded the AD confirming the spirit squads girls-only in June, but the 2025-26 portrait appears to include at least one student who does not present as female, and asserting "Girls" on a public page against the photo is the more damaging error of the two. Logged as a question for Mandi rather than resolved by guess. **Photos: one, and the action slots are empty on purpose.** `gdance-team-2025.jpg` — the very file Mandi re-sent, processed back in #82 and left on disk by #123 precisely for this page — is the portrait, captioned as the *2025-26* squad since the section header hardcodes "2026-27". Per #108 the hero and gallery stay empty rather than take a posed shot: the hero renders brand-black (verified `rgb(10,10,10)`) and "In Action" self-hides. **`ccheering-taryn.jpg` was NOT used despite being confirmed dance in #123** — it is a posed line-up at a football game, not a performance shot, and #108 forbids exactly that substitution. It stays unreferenced on disk. `team-photos.ts` takes the portrait as its **third standing posed exception** (with baseball and TCC), since the fallback is a generic student-section photo. **The seven photos Mandi attached could not be ingested** — no Gmail attachment-download tool exists, and Drive files run to 15 MB, which cannot be round-tripped through a model context. They need a manual drop into `photo-inbox/`; tracked under External Inputs. `team-audit` now reports dance needing action hero, action gallery and liaison — accurate. `photo-usage`: no page shows a photo twice. `tsc` clean, `next build` clean (54 static pages), eslint unchanged at 90 pre-existing problems. | Claude ✅ |
| 2026-08-20 | 170 | **Tier data rebuilt from the board's final 2026-27 sheet, and the black *For Businesses* band removed from `/membership`.** Erik supplied the PDF the board is calling final; it **supersedes several instructions from earlier the same day**, which is worth recording plainly rather than quietly overwriting. Against the site's previous state: **Gold's banner goes from ONE location to THREE**; **Silver's banner returns** at **two** locations, having been withdrawn that morning under *"only Champion and Gold get banners"* (#168); **sports designation flattens to 3 / 3 / 3 / 0 / 0**, where the site carried 5/4/3/2/1 read off the storefront a day earlier — **Tiger Pride and Varsity now credit no sport at all**; **video on HUDL** is a new perk on Champion and Gold; **Silver loses scoreboard ads**, so it loses the ad-perks flag; and **no monthly price appears anywhere on the sheet**, so the $95 / $45 / $11 / $5 figures the site carried are gone. **Tiger Friend needed nothing** — when Erik said it would not be posted, that was read as a storefront decision and the site's data was left alone, so its return is a no-op. **Zero-credit tiers are handled end to end** rather than by rendering a picker capped at nothing: no perk bullet, no fieldset, and the route refuses with *"Varsity sponsorships aren't credited to a specific sport"* instead of the nonsense *"at most 0 sports"*. **The `/membership` layout was decoupled from `adPerks`** in passing — the top row is now the first three tiers by position, because Silver losing ads should not reflow the page. **The black *For Businesses* band is gone**, along with its component; the sponsorship path it offered now lives in the donate box's **Sponsorship** tab, which does more (tier selection, sport picker, card *or* invoice). | Erik + Claude ✅ |
| 2026-08-20 | 169 | **The Treasurer's report moved from a CLI script into the Board Hub — and the script was deleted rather than kept.** Prompted by preparing Erik for his approval call with Trina: the pitch included *"you get a per-sport report"*, and #161's report was **`npm run square-report`**, needing a terminal and Square credentials. The person who needs it is a volunteer Treasurer. Building a Treasurer tool the Treasurer cannot run is not building it. **Now at `/board/square-report`** inside the password-gated hub: pick a date range, read the table (gross · 75% team · 25% general per designation, with credited sports shown for sponsorships), download CSV. Linked from the hub's Operations list. **The CLI script and its npm entry were removed**, and `CLAUDE.md` updated — Erik's call that the page is the better implementation going forward, and keeping both would have meant two copies of the allocation logic, which is the drift this session has paid for repeatedly. Logic now lives in `src/lib/square-report.ts`, imported by page and CSV route alike. **🔒 The security detail worth remembering:** the proxy gates on `pathname.startsWith("/board")`, so a route at **`/api/board/square-report` is not covered by it** — without an explicit check the club's donation figures would have been downloadable by anyone guessing the URL. Board session validation was extracted to `src/lib/board-auth.ts` and is now shared by the proxy and the route rather than hand-rolled twice. Verified by request: unauthenticated page → **307** to login, unauthenticated CSV → **401**, **forged cookie → 401**, and a real session → 200 on both with correct figures. **Also renamed** the donate box's second tab from *Business sponsorship* to **Sponsorship**, at Erik's request. | Erik + Claude ✅ |
| 2026-08-20 | 168 | **Banner perks revised: Champion and Gold only, both for one year.** Board change relayed by Erik. **Champion** keeps three locations but drops from **two years to one**; **Gold** keeps one location and drops from **two years to one**; **Silver's banner is withdrawn entirely**, leaving it website recognition and six passes. Tiger Pride and Varsity never carried one. Both surfaces that read `sponsor-tiers.ts` — the `/membership` tier cards and the gold perks box in the donate box's business tab — pick this up automatically, which is the payoff for having moved the ladder into data. **🔴 The Square storefront does NOT.** Its item descriptions are independent text and still describe the old arrangement — the item now named *Silver Sponsor* carries the description that came across with the rename, which explicitly offers *"Business banner at one sporting location of your choice for one year"*, and Champion and Gold still say two years. Left unedited, the storefront sells a banner the site no longer offers, one day after #163 got the two surfaces agreeing for the first time since #88. **This is the recurring shape of the problem**, not a one-off: the storefront is a second copy of the offering maintained by hand, and every board change now costs two edits until `SQUARE_LIVE_DONATE` is switched on and the storefront leaves the critical path. | Erik + Claude ✅ |
| 2026-08-20 | 167 | **The business tab now shows what each tier actually buys.** Erik: list the perks dynamically as you click a tier. A gold-bordered box sits under the selector and swaps contents with the selection — tier name, the *Includes Ad Perks* badge on the top three, and the full perk list, with `aria-live="polite"` so switching tier is announced rather than silently changing under a screen-reader user. The sports-credit line is **generated from `sportsCredit`**, the same field the picker directly below enforces, so the promise and the limit cannot disagree. **Monthly prices are deliberately omitted** even though the sheet carries them for Tiger Pride ($95) and Varsity ($45): this box sits immediately above a card button that can only charge the **annual** amount, and advertising a monthly option beside it would promise something checkout cannot honour — the same fault that had monthly giving pulled from this form in #154. **On placement**, Erik asked whether the box should sit to the right instead. Measured rather than argued: the donate card is **608px** wide at a 1280px viewport, because it is already the right-hand column of a two-column page. Splitting it would leave ~270px a side — below the tier grid's 144px minimum column width, so the five tiers would stack into a single column, and the longest perk string (**66 characters**) would wrap to four lines, turning Champion's eight bullets into a tall ragged rail. Full width below keeps every bullet on one line. | Erik + Claude ✅ |
| 2026-08-20 | 166 | **The donate box split into General membership and Business sponsorship, with an invoice route beside the card button.** Erik's design. Businesses were being funnelled through a form built for parents — a sport dropdown, a $50 default, a personal-giving ladder — with **no way to name a tier at all**; `/membership` had the catalogue but a business landing on `/donate` could only read, not act. The box now opens on tabs. **General membership is the default and stays untouched**, deliberately: it is the overwhelming majority of traffic and a parent should never dismiss a business form to give $50. **The business tab** carries the five tiers as selectable cards, the shared sport picker sized to the chosen tier, and business/contact/email/phone fields. **Two ways out, and this is the point of the change:** sponsorship is a relationship the club invoices as often as it charges, and a business may need a PO, a W-9 or its own paperwork before money moves. A card-only button would turn *"we'd like to sponsor you"* into a dead end for precisely the sponsors worth the most — so **Request a sponsorship invoice** sits beside **Pay with Square** as a peer, not a footnote, and reuses the enquiry panel from #146 rather than a bare `mailto:` that fails silently. **The picker was extracted to a shared `SportPicker`** used by both `/membership` and the donate box — two copies of the same perk is the drift this session has repeatedly paid for. **A subtle case handled:** downgrading tier after choosing sports **trims the selection** rather than letting it overrun — verified by picking five at Champion, switching to Varsity, and watching it settle at *1 of 1 chosen*. Being rejected by the server for something you cannot see is the worst kind of form error. **`businessName` now rides in the note and `metadata.business`**: Square's checkout collects a *buyer* — a person with a card — and has nowhere to record which company they are paying for, which would have left the Treasurer guessing the company behind a $5,000 sponsorship. **Worth noting for future debugging:** Square's order search is **eventually consistent** — a read immediately after creating a link returned the *previous* order and briefly looked like lost metadata. | Erik + Claude ✅ |
| 2026-08-20 | 165 | **Multi-sport designation built for business sponsorships — the integration no longer regresses on the storefront.** Erik's call, and it closes the gap #164 flagged: `/membership` told a Champion sponsor they could credit **five** sports while our checkout accepted **one**, so replacing the storefront would have taken a perk away. Clicking a tier now opens a picker on the card — all 27 designations, labelled exactly as `/donate` labels them, with the tier's allowance enforced and the remaining boxes **disabled rather than hidden** once it is reached, so the limit reads as a limit instead of a glitch. Selection is **optional**, matching the storefront. **The allowance moved from prose into data.** `sportsCredit` is now a field on `SponsorTier`, the bullet added in #164 is **generated from it**, and the route validates against it — a display string and a validation constant that can disagree is the #145 failure in a new costume, a card promising four while the server takes three. **Server-side enforcement is the point:** the UI disabling a checkbox is a courtesy, and the perk is worth real money, so the server checks membership of the list *and* its length. Verified by request rather than by clicking: Varsity with 2 sports → *"Varsity may be credited to at most 1 sport"*; Champion with 6 → refused; an invented sport → *"Unknown sport: quidditch"*; five duplicates of one sport → accepted as one, deduped. **What reaches Square:** the line item stays **stable** (`Gold Sponsor — 2026-27 business sponsorship`), because it is the reporting key and appending sports would fragment it the way #160 already fragmented donations. The sports ride in `metadata.sports` as **slugs** — the field the Treasurer's report groups on, and labels have changed once this week already — and in the payment note as readable names: *"credited to Football (Boys), Baseball, Volleyball (Girls)"*. **A regression caught in my own change:** the generated bullet was first wired to the `sponsor` prop, which is gated on `SQUARE_LIVE_DONATE` — so the perk would have vanished from `/membership` whenever checkout was off, which is the state the live site is in **right now**. How many sports a tier credits is true whether or not you can pay online, so it reads from the tier itself. | Erik + Claude ✅ |
| 2026-08-20 | 164 | **Production credentials re-pointed at the correct Square account, and the sports-credit perk finally reaches the website.** Erik swapped the token and location to the account that owns the storefront: the config panel now reports **merchant `6YKJFPPASVNW3`**, location **"SLOTAB General Memberships" (ACTIVE, card processing)**, mode production. **That clears the #162 launch blocker** — donations will land in the same account they always have, which is the one Trina's QuickBooks connector watches, so `SQUARE_LIVE_DONATE=true` is a real option again rather than a way to silently move the club's income. Public surfaces verified unchanged while the flag stays off: both endpoints `503`, all pages 200, `/membership` still saying *"We confirm and invoice you"*. Incidentally the second location on that account is named **"Jen's House"** (INACTIVE) — a personal name on a club payment account, and one more entry for the login inventory this month keeps arguing for. **The perk work (#163's open question):** each sponsor tier now carries a bullet for how many sports the sponsorship credits — **Champion 5 · Gold 4 · Silver 3 · Tiger Pride 2 · Varsity 1** — read off the live storefront items rather than inferred, then diffed against them programmatically. It had been missing from the site since the #88 transcription, so `/membership` described banners, passes and scoreboard ads while saying nothing about the one perk a business actually has to make a decision about. **A trap recorded in the code for whoever re-reads these:** the wording is not uniform. Four tiers render *"Sport(s) … (choose up to N)"* but **Varsity** renders *"Sport … (choose 1)"* — singular, no "up to" — so a pattern matching only the first form concludes Varsity has no picker at all. It cost two wrong reports here before the looser check caught it. | Erik + Claude ✅ |
| 2026-08-19 | 163 | **Storefront re-tiered to match the site — the #143 mispricing is closed, and a perk the website never mentions surfaced.** Erik recovered the credentials for the second Square account (#162) and worked through the correction list. **Verified by diffing the live storefront against `sponsor-tiers.ts` programmatically rather than by eye**: all seven levels now agree to the dollar — Champion $10,000 · Gold $5,000 · Silver $2,500 · Tiger Pride $1,000 · Varsity $500 · Family $125 · Individual $50. **This closes #143**, which had been live since the 2026-08-11 re-tier: a business could buy "Champion Membership" for **$1,000** while `/membership` sold Champion at **$10,000** with scoreboard ads and ten passes. **Seven retired tiers are gone** (Platinum, Bronze, Coach, Century, and the three colliding memberships), and the three section headings read **2026-27** — including the one that had been inconsistently formatted as `2025-2026`. Pass counts were corrected too: Champion now reads *Ten*, Gold *Eight*, Varsity *Two*, matching the board's 2026-08-11 ladder. **A useful diagnostic worth reusing:** Square Online keeps the old product URL after a rename, so `/product/platinum-business-sponsorship/15` now serving *Gold Sponsor* proves a rename, while a URL resolving to a page with no item name proves a deletion. That is how the one genuine gap was found — the old *Silver Business Sponsorship* had been **deleted rather than renamed**, leaving the ladder jumping $500 → $2,500 with no Tiger Pride. Also worth knowing: **Item Library edits go live instantly, but site-section edits need Publish**, which is why the headings lagged the prices by a round. **🟡 The open question this raised:** every sponsorship item carries a *"Sport(s) to get credit for — choose up to N"* picker, scaling with tier (sampled: Champion **3**, Gold **2**, Varsity **1**). **Nothing in `sponsor-tiers.ts` mentions this perk at all** — not for any tier — so there is no authoritative number to align to, and `/membership` is silently under-selling what a sponsor actually gets. Two things follow: the ladder grew from four tiers to five while the range stayed 1–3, so some value must now repeat; and if the board does offer this, the perk belongs in the site's tier data. Needs the printed sheet or a board answer, not a guess. | Erik + Claude ✅ |
| 2026-08-19 | 162 | **🚨 The club has TWO Square accounts, and everything built so far points at the one that has never taken a donation.** Erik went to fix the school year on the storefront, found the sponsorship category empty, and then found **only merch in the entire item library** — while the storefront's membership, sponsorship and per-sport donation items were demonstrably still live and purchasable. That contradiction had one explanation and it checks out: **`slotab-3.square.site` belongs to merchant `6YKJFPPASVNW3`**, while the account Erik is signed into — and generated the **production access token** from — is merchant **`1RZ6X80KDPPNR`**, holding the `SLOTAB` (INACTIVE) and `SLOTAB Merch` (ACTIVE) locations. Confirmed by reading the embedded merchant id from three separate storefront product pages, including the per-sport donation item `/product/volleyball-girls/89` that `/donate` currently falls back to. **Every donation the club takes today lands in `6YKJFPPASVNW3`. Every payment link the new integration mints would land in `1RZ6X80KDPPNR`.** **This is a launch blocker, not a curiosity.** Setting `SQUARE_LIVE_DONATE=true` in its current state would silently move donation income to a different Square account: different bank deposits, different history, and — since Trina's QuickBooks connector is presumably attached to the account that has actually been receiving money (#161) — **quite possibly invisible in her ledger entirely**. `npm run square-report` reads whichever account the token belongs to, so it would show the new gifts and none of the old. **It also explains two earlier puzzles**: why the `SLOTAB` location is `INACTIVE` (#152) — the account it lives in appears to be the secondary, merch-oriented one — and why Erik cannot see the sponsorship items to edit them. He does not have them; another account does. **The likely correct resolution is to re-point the integration at `6YKJFPPASVNW3`**, the account already receiving donations and already wired to QuickBooks, which means generating the production token there instead — a credentials change, not a code change. Consolidating the other way would drag in bank accounts and the QuickBooks connection and is a board decision. **Echoes the GoDaddy pattern**: club assets spread across accounts nobody has inventoried, discovered only when someone needed to change one. Worth an explicit inventory of who holds what. | Erik + Claude ✅ |
| 2026-08-19 | 161 | **Trina reconciles from a QuickBooks connector — so the designation work never reaches her, and the fix is a report, not a naming scheme.** Erik answered the question open since #144, and the answer reframes it. The common Square→QuickBooks connectors post a **daily summary invoice with no line detail**, and Intuit's own guidance is that customer names and emails are not imported — so the sport a donor chooses, which the site works hard to put *into the transaction* (#144, #160), very likely **never appears in the ledger she works in**. Two consequences. **First, #160's level suffix costs her nothing** and stays: if the item name doesn't reach QuickBooks, neither does the suffix, so the donor-facing win is free. **Second, and more useful — the real answer to "keep it easy for Trina" is not a better name, it is the allocation itself.** The club promises donors **75% to their sport, 25% to shared programs**, and nothing produced those numbers; somebody had to turn a month of Square payments into them by hand. New **`npm run square-report`** does it: gross, team share and general share per designation for any date range, plus CSV. It keys on **`metadata.designation` / `metadata.level`, never the line item name** — names are for humans and have already changed twice this week (gendered squads, the level suffix), whereas metadata is a stable field we control. It excludes **DRAFT** orders (abandoned checkouts) and anything carrying `metadata.test` (#153), so board test gifts cannot reach an allocation. **A bug the testing caught, and the reason to run these things rather than reason about them:** the first cut filtered on `COMPLETED`, but paying a Square payment link leaves the order **`OPEN`** — confirmed by putting a payment through the sandbox and watching the panel report `Order state OPEN`. That filter would have reported **zero for every real gift**, which for a report a Treasurer trusts is the worst available failure. Now matches `OPEN` + `COMPLETED`, dates on `created_at` (an OPEN order has no `closed_at`), and independently checks each order for an actual tender — state is bookkeeping, a tender is money. Verified against 52 sandbox orders: 50 abandoned drafts excluded, the 2 genuine payments counted, split correctly ($200 volleyball → $150/$50; $50 general → all general). The same run also confirmed the **redirect back to `/thank-you` fires with the right query string**. | Erik + Claude ✅ |
| 2026-08-19 | 160 | **The Square checkout heading now carries the membership level as well as the sport and amount — and there was only one way to do it.** Erik asked for the level on Square's page. **Three approaches were tested against the sandbox rather than assumed, and two were dead ends:** `variation_name` and the line item `note` are both **accepted by the API and never rendered to the buyer** — not in the heading, not in the order summary, not even with the summary expanded; and moving the level to a **second $0 line item** (which Square does accept) **replaces the heading with a generic "Checkout"**, losing the sport from the top altogether — strictly worse than before. Square's hosted checkout displays the **line item name and nothing else**. So the level is appended there: *"Volleyball (Girls) — SLOTAB donation (Gold Sponsor)"*, *"SLOTAB General Fund — donation (Family)"*. Verified across the ladder from $25 (Tiger Friend) to $12,000 (Champion Sponsor). Sponsorship checkouts are untouched — their line item already *is* the tier. **🟡 The cost, which belongs in front of Trina before it surprises her:** Square's item-sales report now keys on **sport *and* level**, so a single designation can appear under several names where it previously appeared under one — the exact reporting surface #144 established as the reconciliation basis. **Mitigated, not ignored:** the level is also carried in `metadata.level` alongside `metadata.designation`, invisible to the donor but the clean field for the **reconciliation sidetool** to group on, which is where per-sport totals should come from in the end. This makes the unanswered question from #144 — *does Trina reconcile from Square's item report or a QuickBooks connector?* — materially more urgent, because it decides whether this trade is cheap or annoying. If it turns out to be annoying, dropping the suffix is a one-line revert. | Erik + Claude ✅ |
| 2026-08-19 | 159 | **Business and general levels harmonised into one ladder, because businesses currently enrol through the same donate form.** #158 fixed the invented tier names but left a real gap it could only flag: the general memberships stop at **$125**, so a $5,000 gift read "Family" and the five levels above it were **unreachable from the page where the money is actually given**. Erik supplied the missing context — the board currently puts businesses through this same flow — and asked for the levels harmonised until he can argue them onto a separate path. `levelForGift()` now walks **both halves of the sheet**, richest first: **$50 → Individual · $125 → Family · $500 → Varsity · $1,000 → Tiger Pride · $2,500 → Silver Sponsor · $5,000 → Gold Sponsor · $10,000 → Champion Sponsor**, with anything below $50 still enrolling as **Tiger Friend** (#37 — every donation makes you a member). Boundaries verified continuous with no gaps: $49→Tiger Friend, $124→Individual, $499→Family, $999→Varsity, $4,999→Silver, $25,000→Champion. **Placed in `sponsor-tiers.ts` beside the data rather than inside `DonateForm`**, because it is a fact about the offering rather than about that form — and because two pages disagreeing about the offering is precisely the bug #158 existed to fix. **If the board later splits business enrolment onto its own path — the change Erik is arguing for — that function is the single place to revisit**, and the comment there says so. **One expectation risk handled rather than left:** a $5,000 personal donation now reads *"Gold Sponsor"*, a package that carries scoreboard ads, a two-year banner and eight All-Sport passes. Rather than silently imply all of it, the callout now links to `/membership#sponsorship-tiers` — **"See what that includes"** — so the donor reads the actual package instead of inferring one. Monthly matching skips Silver, Gold and Champion, which publish no monthly price, so a recurring pledge can never be credited against a figure the club does not offer. | Erik + Claude ✅ |
| 2026-08-19 | 158 | **`/donate` was promising membership levels that appear nowhere on `/membership`, including one that overstated the gift sixfold.** Erik spotted that the level shown against a chosen amount didn't match the membership page. It didn't, because `DonateForm` carried its own ladder — **Supporter / Fan / Booster / Champion / Patron** — from a *2026-05-06 research draft*, and **not one of those five names appears on `/membership`**, whose general memberships are **Family, Individual and Tiger Friend**. The dangerous part was the overlap: both ladders used **Champion**, and disagreed about it by a factor of six. `/donate` told a **$1,500** donor they were "Champion-level" while `/membership` sells **Champion at $10,000** as the top *business sponsorship* — stadium and gym scoreboard ads, banners at three venues for two years, ten All-Sport passes. A donor could reasonably have believed they had just bought that package. Now derived from the same `GENERAL_MEMBERSHIPS` array `/membership` renders, so the two cannot drift again — the same reasoning that put sponsorship prices in one place in #145. **Business sponsor tiers are deliberately not consulted**: those are packages sold to businesses through a different flow, and a large personal donation does not buy one. Copy reworked too — *"Family-level SLOTAB member"* reads badly because these names are nouns rather than grades, so it now reads **"Your gift enrols you at the Family membership level."** Verified by clicking every amount button in a browser: $25 → Tiger Friend, $50 and $100 → Individual, $200 and up → Family. **🟡 What this exposes, and it is a board question not a code one:** the general membership ladder **tops out at $125**, so a **$5,000** individual donation now reads "Family" — accurate, and an anticlimax. The old ladder papered over this with invented tiers. Options are to let large personal gifts map onto the existing Varsity/Tiger Pride/Silver/Gold ladder (currently framed as business-only), or to add an individual tier above Family. Either is an offering decision for the board. | Erik + Claude ✅ |
| 2026-08-19 | 157 | **The handoff panel was being destroyed by its own redirect; fixed with a countdown that knows when not to run.** Erik saw the confirmation box appear and vanish, and suggested a five-second timer. The instinct was right and the diagnosis went further: `window.location.href` fired the moment the payment link came back, so the panel rendered and was immediately navigated away from. **What was being lost is not just reassurance** — the panel carries the pre-written email for choices the checkout cannot convey, so a donor who had asked to stay anonymous was shown the way to tell us and then yanked away from it. **A fixed timer alone would not have fixed that**, only moved it from "couldn't read it" to "pulled away mid-task", so the behaviour is now conditional. **The condition took a correction.** The first cut auto-advanced only when the panel was empty of extra content — but `unrecordedIntent` includes *"Qualifies for the Supporter membership tier"*, which matches **nearly every gift**, so in testing the countdown never appeared and every donation became a two-click flow. The right test is not *is there text* but **does the donor have a task**: a tier is derivable from the amount and nobody needs to be told, whereas anonymity cannot be derived from anything and the default if unstated — name on the donor wall — is the one outcome here that is awkward to undo. So `needsDonorAction` is anonymity alone. **Result:** an ordinary gift shows *"Taking you there in 5 seconds…"* with a **Continue to secure checkout** button for anyone who has finished reading; an anonymous gift shows no countdown at all and waits, with the email button and an explicit *"We'll wait"*. The submit button is disabled once a checkout is pending, so the pause cannot produce a second link. **Both paths verified in a browser**: the countdown ticks 5→4 and the anonymous path was still on the page after seven seconds. **Also corrected an assumption of Erik's:** donors are *not* returned to `/donate` afterwards — Square's `redirect_url` sends them to `/thank-you`. | Erik + Claude ✅ |
| 2026-08-19 | 156 | **Only the email was carrying across to Square; name and phone now do too — and the phone nearly couldn't.** Erik noticed the donate form collects name, email and phone but Square's page arrived with only the email filled in. Both were fixable: `PrePopulatedData` takes `buyer_phone_number`, and although Square has **no top-level name field**, the name rides in `buyer_address.first_name` / `last_name` (no street address is sent — `ask_for_shipping_address` is off and a donation has nothing to ship). **The phone is where it got interesting.** Square **rejects the format a parent actually types**: tested against the sandbox, `805-555-1212`, `(805) 555-1212` and even bare `8055551212` all fail with `INVALID_PHONE_NUMBER`, while `+18055551212` and `18055551212` are accepted and normalised to `+1…`. A country code is mandatory. Since our field is free text, the naive implementation would have failed for **every US donor who typed a phone number normally** — and worse, a rejected phone takes down the *whole* prefill, so they would have lost the working email prefill too, over a field they filled in helpfully. Now normalised to E.164 server-side, **deliberately conservatively**: 10-digit US/Canada numbers get `+1`, an explicit `+…` international number passes through, and anything we cannot confidently assign a country code to is dropped — a missing prefill is a small loss, a guessed country code is a wrong one. Names split on the **last** space, so *Mary Anne Ramberg* keeps *Mary Anne* as the given name and a mononym still fills something. The retry safety net was generalised from email-only to any `pre_populated_data` field. **Verified on the real checkout page**, not just in the API echo: `firstName=Erik`, `lastName=Ramberg`, `phone=805-555-1212`, `email=erik.ramberg@gmail.com` all prefilled, and the coupon box from #155 confirmed gone. *(One thing not confirmed: `merchant_support_email` does not appear on Square's preview page — it may only surface on the live checkout or the receipt.)* | Erik + Claude ✅ |
| 2026-08-19 | 155 | **Square's checkout was showing retail furniture on a charitable donation; three `checkout_options` fixed it.** Erik spotted an **Add coupon** field on the hosted checkout and asked whether it could go. It can: `CheckoutOptions.enable_coupon` exists precisely for this. SLOTAB has never issued a discount code and cannot, so the box only invites a donor to wonder whether they are missing one — or whether they are *buying* something rather than giving. Set to `false`. **Two more in the same category, included because they are the identical one-line fix and the identical mistake:** `enable_loyalty` put a **REWARDS** opt-in on the page, and inviting someone to earn points on a charitable gift is the wrong note entirely — also off. And `merchant_support_email` is now set to the membership address: **Square's page is the only screen in this flow that isn't ours**, which makes it the one place a confused donor has nowhere to turn, and this puts a real contact in front of them without navigating back. None of the three is a payment control, so nothing is lost. **Verified visually, not just in the API response:** Square echoed all three back set, and the buyer-facing checkout now runs straight from *Order total* to *Express checkout* with no coupon box, where the earlier screenshot of the same flow had one. | Erik + Claude ✅ |
| 2026-08-19 | 154 | **Monthly giving removed from the donate UI, live page and preview alike.** Erik's call, and the right one: the recurring backend is outstanding work (#144 phase two), and the reason it now *has* to come out is sharper than it was a week ago. While the button merely handed off to the storefront, a "Monthly recurring" tab that gathered intent into an email was an honest stopgap. Since #145 the button **actually takes money** — so leaving the tab up would accept a monthly pledge and **silently bill it once**, which is worse than not offering monthly at all and is the same class of failure as the sandbox-checkout incident in #148: the donor believes one thing has happened and something else has. **Implemented as a single `RECURRING_ENABLED` flag rather than a deletion.** Every monthly branch — the tier ladder, the $10 floor, the 4-year lock-in commitment, the intent-capture email line — is left intact and gated, so switching it back on when the Subscriptions API work lands is one line rather than rebuilding from memory. **Copy cleaned to match:** `/donate`'s intro no longer promises "$10/mo" gifts, and its bullet list drops *Monthly recurring minimum* and *Cancel recurring any time* in favour of the 75/25 split. The amount legend now reads simply **"Amount"** — *"One-time amount"* invites the question "as opposed to what?" once there is no alternative — and the too-low warning drops the same qualifier. **Verified in a real browser** rather than by grepping HTML, because `/donate` renders its form client-side behind Suspense so the served markup proves nothing: no mode toggle, no lock-in checkbox, and **no occurrence of "monthly" or "recurring" anywhere in the rendered page**, on both `/donate` and `/donate/preview/<slug>`. **Left alone deliberately, and flagged for the board:** the sponsor tier cards on `/membership` still read *"or $95/mo recurring"* (Tiger Pride) and *"or $45/mo"* (Varsity), which come from the official printed sheet. Those sit beside a *Sponsor at $1,000* button that can only charge the annual amount, so a business could reasonably expect a monthly option that checkout does not offer. Whether to remove them is a question about what the club actually offers — monthly sponsorship may well be available by invoice — so it needs a board answer, not a code change. | Erik + Claude ✅ |
| 2026-08-19 | 153 | **Board test gifts now identify themselves, because review is happening with real money in the real account.** Erik emailed Trina to ask why the `SLOTAB` location is deactivated (#152) and is pointing `SQUARE_LOCATION_ID` at **`SLOTAB Merch`** meanwhile — a sound call: it is one variable, reversible, and unblocks review today. **The consequence worth heading off:** the site is on **production** credentials, so a reviewer's $25 is a genuine charge landing in the club's live Merch location, visually identical to a parent's donation. That is how a test gift gets booked as revenue and never refunded — and it would land in *merch* reporting, where a donation has no business being. Orders minted through the preview route now carry **`*** TEST — board review, refund this ***`** at the front of the payment note, plus `test=true` in metadata. **Deliberately not in the line item:** Erik's original ask was that review look exactly like production, and a `[TEST]` prefix on the checkout heading would defeat that — the payment note is visible on the payment in the Square dashboard, which is where anyone reconciling actually looks. Verified end to end on sandbox: a preview-token order carries `test=true` with the line item still reading cleanly as `Volleyball (Girls) — SLOTAB donation`, and the note renders in full. Public requests are unmarked, and still `503` while the launch flag is off. **Reminder for whoever reconciles:** these will need refunding in Square, and they will appear under *SLOTAB Merch* until the location question is settled. | Erik + Claude ✅ |
| 2026-08-19 | 152 | **Root cause of "not accepting payments": the `SLOTAB` location is `INACTIVE` — and #151's diagnostic reported it as healthy.** The config panel showed a **green box** — token authenticated (merchant `1RZ6X80KDPPNR`), location matched, *"can take payments ✅"* — for a configuration Square was actively refusing. **My check was wrong.** It keyed on the `CREDIT_CARD_PROCESSING` capability alone, and **a deactivated location keeps its capabilities**: `24D20G3N4NGKW` — *SLOTAB* — is `INACTIVE` and still advertises card processing. Square requires **both** `ACTIVE` status and the capability, so the check now demands both, and the two failure modes are reported separately (*deactivated* → reactivate it; *no card processing* → an account-level problem). Verified against the real account state: the old logic passed both locations, the new logic blocks the configured one, which matches what the donor saw. **A diagnostic that reports green on a broken config is worse than no diagnostic**, since it redirects the search away from the actual fault — this one sent us hunting for account activation problems when the account was fine. **The decision this leaves Erik**, and it is a Treasurer question rather than a technical one: the account holds `SLOTAB` (INACTIVE) and `SLOTAB Merch` (ACTIVE, working). Pointing donations at *SLOTAB Merch* is a one-variable fix that works today but **lands every donation in the same Square location as merchandise sales** — commingling two revenue streams at exactly the point #144 established Square's own reporting as the reconciliation surface. **Reactivating `SLOTAB` is the better answer** (Square dashboard → Account & Settings → Business → Locations) and keeps donations reporting separately. Line-item naming is unaffected either way, and the choice is reversible with one variable and a redeploy. | Erik + Claude ✅ |
| 2026-08-19 | 151 | **The production checkout reached Square and Square declined: "This business is currently not accepting payments."** Important read of that error — **it is not a failure of our integration**. The payment link was *created successfully*, which means the production token authenticated and `SQUARE_LOCATION_ID` matched a real location; Square refuses later, at the buyer-facing checkout page. The Locations API explains why: a location carries a **`capabilities`** array, and card payments require **`CREDIT_CARD_PROCESSING`**. A location can be `ACTIVE` and still lack it — so a link is minted happily and only the donor ever sees the refusal, which is the worst place for this to surface. **The config panel now reports it** — mode, token, location match, and *can take payments* as four separate lines, plus **every location the token can see with its status and whether it can process cards**. That distinguishes the two very different fixes: if some other location can take payments, it is a one-variable change (`SQUARE_LOCATION_ID` + redeploy); if none can, the **Square account has not finished activation** and it is settled in the Square dashboard, not in this repo. **Worth noting for diagnosis:** SLOTAB's existing storefront at `slotab-3.square.site` *does* take real money, so the organisation can evidently process payments — which points at the production token belonging to a **different Square account or location** than the storefront rather than at a dead account. The panel's location list settles which. Square's own developer forum offers no better answer than "contact Square support", so surfacing the capability flag ourselves is the faster path. | Erik + Claude ✅ |
| 2026-08-19 | 150 | **Erik swapped in the production token; the preview page now tells him whether it actually works.** He replaced `SQUARE_ACCESS_TOKEN` with the production token and added `SQUARE_LIVE_DONATE` with **no value** — which is exactly right, since anything but the literal `true` is off, so the public site stayed on the storefront (verified live: still `503`). **But the swap is a three-variable change, and two were still on sandbox settings.** `SQUARE_ENVIRONMENT` must become `production` or a production token is sent to the sandbox host and returns `UNAUTHORIZED`; `SQUARE_LOCATION_ID` must become the **Live** Location ID, since the sandbox location does not exist in the production account — that one authenticates cleanly and then fails on every payment link, which is the nastier of the two. **The gap that made this worth building:** the public endpoint answers a flat `503` whether the credentials are perfect-and-waiting or completely broken, because the launch flag gates it either way — so nothing on the site could distinguish "configured correctly" from "quietly misconfigured". The slug-gated preview page now renders a **Square configuration** panel: mode, whether the token authenticates, which location matched, and — when it fails — **the list of locations the token can actually see**, so the right ID can be pasted straight in. A `401` is specifically called out as an environment/token mismatch rather than left as a bare error code. Both failure modes were tested by deliberately inducing them. Reports names and ids only, never the token, and it is `force-dynamic` so it reflects what Vercel holds now rather than a stale build. Goes when the preview route goes. | Erik + Claude ✅ |
| 2026-08-19 | 149 | **Erik asked for the test page to look exactly like production so the board isn't confused by Square's testing panel. It can't — in sandbox — and the honest fix was a launch flag instead.** Investigated rather than assumed: a sandbox payment link's `long_url` is a **`sandbox-testing-panel` URL**, and that panel offers exactly two doors, neither of them a normal checkout — *Preview Link* ("cannot be used to complete a payment") or *Test Payment* (a simulation with no UI). **Square's sandbox never serves a payable, production-looking checkout**, so no amount of code on our side can make it look like production. Confirmed the panel is a **sandbox-only artefact** — in production `long_url` is the real checkout URL — so donors will never see it. **A hack was tried and rejected:** the panel's *Preview Link* is a `checkout.square.site/preview#order=…` URL built from base64 of the order, and it can be reconstructed server-side — the first ~120 characters matched Square's byte for byte. But Square's version is **3,789 characters against 2,993 reconstructed**, so its order object carries fields `related_resources` doesn't return. Shipping a subtly-incomplete preview to reviewers is worse than one extra click, and the format is undocumented, so it was dropped. **What shipped instead — `SQUARE_LIVE_DONATE`.** The real blocker behind the request was that a production token was all-or-nothing: saving it made the new flow live for every donor instantly, with no way to look at the genuine production checkout first. The flag now separates *"Square works"* from *"donors use it"*. A production token can go in while the public site stays on the storefront; the board reviews the **real** production checkout via `/donate/preview/<slug>`, and launch becomes a deliberate, instantly reversible one-value change. Defaults off, and **anything other than the exact string `true` is off**, so a typo fails safe. **Verified the flag cannot be used to do harm:** with sandbox credentials and the flag set to `true`, the public endpoint still answers **503** — the launch flag cannot override the #146 sandbox guard, so no flag flip can hand donors a fake checkout. **And for reviewing today, before any production token:** the preview page now names the grey testing panel explicitly, tells reviewers to click *Preview Link*, states that Square won't let them complete a payment and that this is Square's limitation rather than our bug, and links the thank-you page directly so they can still see where donors land. | Erik + Claude ✅ |
| 2026-08-19 | 148 | **Correction to #147 — the sandbox checkout reached the live donate page, and Erik rolled it back.** #147 recorded that the production domain was pinned to an older build and inferred this was an accident of Vercel configuration, concluding the #146 hazard *"never reached a donor"*. **Both halves were wrong**, and the row is left standing per the append-only rule. Erik supplied what the deployment API could not: after `47ab893` deployed, he **saw the sandbox checkout live in the main donate workflow** and **manually redeployed the prior build to restore it**. So the older build serving `slotab.org` was a deliberate **rollback**, not a stuck setting — which also retracts the advice in #147 to go hunting for a manual-promotion or staged-rollout setting. There is nothing misconfigured; Vercel's Instant Rollback moves the production alias to an existing deployment without creating a new build, which is exactly the signature observed (alias on an older deployment, newer ones READY and serving nothing). **Why this matters beyond the record:** the guard shipped in #146 was not defensive programming against a hypothetical. **The failure it prevents actually happened in production**, and the only thing that stopped it reaching donors was a human noticing and reverting within the evening. That is the strongest possible argument for the guard existing, and for the rule behind it — the live site must refuse sandbox credentials rather than trusting deployment discipline. **What is still unknown:** whether any donor hit the window between `47ab893` going live and the rollback. Runtime logs do not reach back that far on this plan (`ExceedsBillingLimitError` for the 18 Aug evening), so it cannot be answered from here. If a donor did, they saw a checkout that took no money and told them nothing was wrong. **Also confirmed:** auto-promotion has since resumed normally — the guard build took the domain on its own after push, and production now correctly answers `503`. | Erik + Claude ✅ |
| 2026-08-19 | 147 | **A push error surfaced that the live site had not been serving any pushed commit for ~24 hours.** Erik hit a git error and asked whether he had already pushed. He had — seven of eight commits were on `main`. The rejection was a plain non-fast-forward: the **GitHub Actions cron** pushed `4256835` (weekly events refresh) at 07:24, so the branch and `main` diverged by one commit each. Resolved the way `CLAUDE.md` prescribes — **fetch + rebase, never force** — which was clean because the cron only touches `weekly-events.json` and this work never does. **The real find came from checking the live site rather than trusting the push:** `/api/square/payment-link` and `/thank-you` both returned **404 on slotab.org** while `/api/board/login` returned 401 and `/donate` returned 200 — so production was serving an *older build than the deployment list implied*. The domain was aliased to `dpl_FjJ9q8ar…`, a **manual redeploy of `9078a90`** — the redeploy Erik ran when adding the Square env vars. Two newer production builds (`47ab893`, `4256835`) sat READY and took no traffic. **That accident is the only reason the #146 hazard never reached a donor:** `47ab893` carried the Square integration *without* the sandbox guard, and had it been serving, every donor would have been handed a sandbox checkout that took no money. Worth checking whether the project has manual promotion or staged rollout enabled, because a deploy silently not serving is a trap that will recur. **After pushing the guard, the new build took the domain on its own and production was verified end to end:** the payment-link endpoint answers **503** for both donations and sponsorships, `/donate` falls back to the correct per-sport storefront URL, and the handoff panel shows the *storefront* wording — **“Please enter $100 yourself”** — rather than the minted-link wording that would have told donors not to do the one thing they had to. `/donate/preview/<slug>` correctly 404s until `SQUARE_PREVIEW_SLUG` is set, `/membership` shows no checkout buttons and the honest “We confirm and invoice you” copy, and **the sponsorship button Erik reported as dead now opens the enquiry panel on the live site**. | Erik + Claude ✅ |
| 2026-08-18 | 146 | **A deploy before the production token would have silently taken nothing — fixed, plus a safe way to validate on the live site.** Erik asked the right question before pushing: would this break donations? **It would have, in the worst way available.** `isSquareConfigured` only checked that a token and a location existed, and Vercel already held **sandbox** values — so nothing would have errored. The route would have cheerfully minted a link against `connect.squareupsandbox.com`, and a real parent would have been handed a sandbox checkout that accepts only test cards, taken no money, and left them believing they had donated. A silent failure on a fundraising site's primary conversion path is worse than a loud one and worse than no change at all. **The live site now refuses sandbox credentials outright** (`VERCEL_ENV === "production"` is the only signal that separates the live site from previews — `NODE_ENV` is "production" for preview builds too). The route answers `503`, the client falls back to the #140 storefront, and **shipping ahead of the token is a no-op for donors rather than a trap**; the new flow switches itself on when the real credentials arrive. **A safe way to validate anyway** (Erik's follow-up): `/donate/preview/<slug>` runs the new flow on the live site, gated by `SQUARE_PREVIEW_SLUG` — env-driven and absent from the repo, because a slug in source control is not a secret; unset, the route 404s. It is the one request shape permitted to use sandbox on production, carries a red **no money moves here** banner with Square's test card, and is `noindex`. **Delete it once the token lands** — a forgotten test checkout on a fundraising site is a liability. **A correctness bug caught alongside:** the handoff panel had been rewritten to say *"the amount and designation are already set"*, which is true of a minted link and **false of the storefront fallback**, where the donor must key the amount in. On the live site the fallback is the *normal* path right now, so every donor would have read it — they'd have been told not to do the one thing they had to do. The copy now branches on which route was actually taken. **And the bug Erik hit:** *"I click Start Your Sponsorship and nothing happens."* Correct, and my fault — it was a bare `mailto:`, which is a request that the operating system do something and **fails silently when it can't**: no registered mail handler, webmail-only users, locked-down machines. Survivable on a "questions?" link, not on the enrolment path for a **$500–$10,000** sponsorship. The click no longer depends on it: it opens a panel showing the address and the full message in a copyable box, offering the email app as one of three ways out. Verified by exercising the failure — the clipboard API was blocked in the test context and the fallback selected the text and said press ⌘C. Reused on the sponsor thank-you page, which had the same flaw. **Also gated `/membership` on real availability**, so it stops advertising a checkout that would `503` — note this resolves at **build** time, so **adding the production token requires a redeploy**, not just an env-var save. | Erik + Claude ✅ |
| 2026-08-18 | 145 | **Phase one of the Square integration shipped: the storefront is out of the donation path.** Board go-ahead given, so #144's research became code. `/api/square/payment-link` mints a Square-hosted checkout with the amount **and** the designation already set; `/donate` and all five sponsor tiers on `/membership` call it; `/thank-you` catches the return. **The rule the endpoint exists to enforce:** a price is never taken from the browser unless the browser is *allowed* to choose it. Sponsorships have a published price, so the client sends a **tier id** and the server looks up the cost — verified by posting `amountCents: 100` alongside `tierId: "gold"` and confirming Square recorded **$5,000**, not $1. Donations are genuinely donor-chosen, so the amount is accepted but bounded ($25 floor from #29, $50,000 ceiling as a fat-finger guard) and re-validated server-side. Tier prices moved to `data/sponsor-tiers.ts` because they are now **charged, not just displayed** — the page renders and the API bills from one array, so they cannot drift the way the storefront drifted from the sheet in #143. **A real bug caught in verification:** the first cut named the line item from `team.name` alone, which collides for **16 of the 20 teams** — eight sports field a boys' and a girls' squad under one name, so both would have billed as "Volleyball — SLOTAB donation" and been indistinguishable in the Treasurer's reporting. That is the exact collapse #140 had to warn donors about, nearly reintroduced by the thing meant to fix it. Line items now carry the label the donor actually picked — **"Volleyball (Girls) — SLOTAB donation"** — with co-ed and gender-less teams (Tiger Cheer, #122) correctly showing no parenthetical. **Verified end to end against sandbox:** a donor choosing $200 for Girls Volleyball lands on Square showing that exact line and **$200.00**, nothing to retype; six designations confirmed distinct in Square's own order search; six invalid-input classes rejected; the rate limiter releases exactly 10 per minute then returns 429 with `Retry-After`. **Also corrected:** the follow-up email no longer claims the designation is intent Square cannot carry — it now carries it verbatim, so that line was false. **What this does NOT do:** recurring is untouched and still collects intent by email, per #144 — a monthly donor silently charged once would be worse than an honest wait. The storefront fallback survives for the case where Square is unconfigured (`503`), so a missing credential degrades instead of dead-ending. **Not live:** sandbox credentials only. Production needs a production token, and the in-memory rate limiter is a per-instance speed bump, not the shared counter #73 calls for — both flagged in External Inputs. | Erik + Claude ✅ |
| 2026-08-18 | 144 | **#31 answered after three months open, and the answer changes the design: Square metadata will never reach QuickBooks — the line item is the only thing that carries.** Erik has board go-ahead to accelerate the real Square integration, so the question gating Q1 since May finally got researched against Square's own docs rather than assumed. **The finding:** Square's `Order.metadata` is capped at **10 key-value pairs, keys ≤60 chars, values ≤255**, and Square states it is **“private and can only be read or modified by the same application”** and that **“Square doesn't process this field; it just stores and returns it.”** It is application storage, not reporting data. **So the transaction-ID scheme as originally conceived in #31 — a short hash like `B-FB-S25` riding in metadata into a QB report — cannot work.** Nothing in metadata appears in Square's reports, exports, or any QuickBooks sync. **What does carry is the line item name**, which is exactly what #140 stumbled onto empirically when it found the per-sport catalog items put the designation *in the transaction*. That was the right instinct for the wrong-seeming reason, and it is now the documented basis of the design. **A caveat worth putting in front of Trina before anyone builds on it:** how much granularity survives into QuickBooks depends entirely on the connector. Intuit's own guidance is that QuickBooks **does not import customer names or emails** from Square, and the common connectors post a **daily summary invoice** rather than per-order line detail — so the dependable reconciliation surface is **Square's own item-sales reporting**, not QB. Nobody has confirmed which of these Trina actually uses, and it decides whether line-item naming alone is sufficient. **The architecture this implies (Q1):** a **hybrid**, not the either/or the question was framed as. **One-time gifts and sponsorships** go through the **Checkout API's payment links**, created server-side from an `order` carrying **ad-hoc line items** — arbitrary name and price, no catalog object required — so we set the amount *and* the designation ourselves, prefill the donor's email and phone via `pre_populated_data`, attach a `payment_note`, and bring them back to a thank-you page with `checkout_options.redirect_url`. **This also dissolves the #143 blocker without waiting for the board**: generating our own line items from `MembershipTiers` means the storefront's stale Platinum/Gold/Silver/Bronze items stop being on the critical path at all. **Recurring is a materially bigger build and should not be bundled into phase one** — the Subscriptions API requires a Catalog **subscription plan plus a variation per price point**, a **Customer** record and a **card on file**, and payment links cannot create subscriptions, so card collection needs the **Web Payments SDK** (card data never touches our server, so PCI scope stays low, but it is a real build). Minimum $1, and amounts are fixed per variation, so the $10/$25/$50/$100/$200/$500 monthly ladder becomes six variations and “other” has nowhere to go. **Serverless is already available** — the repo runs `/api/board/login` and `/api/springly/*` on Vercel — so this needs no new infrastructure, only `SQUARE_ACCESS_TOKEN` / `SQUARE_LOCATION_ID` / `SQUARE_ENVIRONMENT` and a sandbox token to start. **Not yet built:** this is research, and the one hard blocker is credentials. Note also that a public endpoint minting payment links is exactly the abuse surface the deferred rate-limiting item in #73 describes, and should not ship without it. | Claude ✅ |
| 2026-08-18 | 143 | **Businesses had no way in, and the storefront they'd be sent to is selling last year's ladder at last year's prices.** Erik: the Sponsorship lead directs businesses to the website to enrol. They were landing on `/membership` — a page that opens with parent-facing 501(c)(3) copy, shows a wall thanking *other* businesses, and offers exactly one action, **“Donate & Become a Member”**, into a donation form with a sport picker and a $50 default. **There was no business path at all.** `/membership` now opens with one: a black band under the header — deliberately looking nothing like the membership copy beneath it — carrying the tier range, a three-step process (pick a tier → send details → we confirm and invoice), and a pre-written enquiry email that asks for the things the club actually needs and cannot get from a checkout: business name, tier, **preferred banner location**, featured-game sport, and a logo file. `/sponsors`, `/sponsor`, `/sponsorship` and `/sponsorships` now redirect there too — the lead sends businesses to “the website” verbally, `/sponsors` is the obvious guess, and it was a 404. **The blocker, and why enrolment deliberately does NOT deep-link to Square:** the storefront *does* carry a **Business Sponsorships** category, which would have been the obvious wiring — the same per-tier deep link the sports donations use (#140). It is still stocked with the **pre-#88 ladder**. Verified item by item: **“Gold Business Sponsorship” is $2,500**, with a **one-year** banner and **four** passes, where this site sells **Gold at $5,000** with a **two-year** banner and **eight** passes; **“Platinum” is $5,000** carrying the perk list this site sells as **Champion at $10,000**. The storefront is a full step down the ladder and roughly half the package at each name. Deep-linking a tier card to those items would have sold a $10,000 package for $5,000, or handed a business a receipt contradicting the page they had just read — so enrolment goes through the board until the storefront is re-tiered. It also still carries **Bronze**, a tier this site no longer offers. **Separately, Erik asked whether Square's landing page can be pre-filled with the amount chosen on our page. It cannot** — tested `?variation=`, `?variationId=` and `?amount=` against a live product page and **none had any effect**; nothing was preselected and the custom-amount box stayed empty. That is by design: a URL that can set a price is a URL that can set it to $1. The real fix is the **Q1 Checkout/Payment Links decision already on the board's plate** (#31/#140), where the link is created server-side with the amount and designation baked in. **One cheap partial win surfaced meanwhile:** the storefront's own amount buttons are **$10 / $20 / $50 / $100**, while our ladder is **$25 / $50 / $100 / $200 / $500 / $1,000 / $5,000** — only $50 and $100 line up, so most donors arrive at a page with no button matching what they just picked and must use the $0.00 box. Aligning the two ladders needs no code from us, only a storefront edit. | Claude ✅ |
| 2026-08-18 | 142 | **The donation designation can no longer be skipped.** Erik: donors were reaching the Square storefront confused, having not picked a sport — the dropdown “isn't obvious for some people”. It sat at the same visual weight as every other control and read as an optional refinement, so it kept its **“SLOTAB General Fund” default** and gifts meant for a team landed undesignated. That is the one field that cannot be recovered later: it rides into the Square transaction itself and is what the Treasurer reconciles against, and once the donor has left for Square there is no way to ask again. Two changes, both to Erik's brief. **The designation fieldset is boxed in gold** — 2px gold border, gold wash, gold-bordered select, its legend sitting on the rule — while every other fieldset on the form stays borderless, so it is the only thing on the card asking to be noticed. **And it now starts unchosen**, on a `SELECT A SPORT OR GENERAL DONATION` placeholder (disabled, so it cannot be chosen back), with submission blocked until a designation exists: an inline `role="alert"` message, a red border, focus moved to the select and scrolled into view. **The button is deliberately left enabled** rather than disabled — a dead control gives no reason, and the missing reason is the entire problem being fixed. A `?team=` link from a team page still arrives pre-chosen, so donors who already said where the gift goes are untouched. Also fixed in passing: the allocation preview fell through to **“75% to your team”** before any team existed, which read as though one had been chosen; it now asks for a designation instead. **Verified in a real browser**, closing the gap #140 recorded — the first preview tab was wedged (React never attached), but a fresh tab exercised the full flow: submit with no designation opens **no** Square tab and raises the error; picking Football clears it and opens the correct `/product/football/81` with the handoff naming **“FOOTBALL”**. | Claude ✅ |
| 2026-08-18 | 141 | **The 404 reports and the “still seeing the old site” reports were the same problem.** Erik reported both three days after launch. **DNS was ruled out first:** the apex TTL is 600s and the record has pointed at Vercel since 2026-08-15, so propagation was long finished. The real cause is a **browser cache poisoned before the cutover** — the old host serves its HTML with `Cache-Control: public, max-age=2678400`, **31 days**, so anyone who loaded slotab.org shortly before the switch still holds a *fresh* copy and their browser serves it **without ever contacting the network**. Nothing on our server is consulted, which is why there is no way to simply push the new site to them; **worst case they self-heal around 2026-09-15**, 31 days after the last possible poisoning. **The two symptoms are one problem:** of the eight internal links on that cached homepage, **five pointed at URLs that 404’d here** (`/about-2`, `/contact-2`, `/membership-2`, `/booster-bash-2`, `/slotab-merch`), so a stale visitor who clicked anything hit a dead end and stayed stuck on the old site. **Verified rather than assumed:** the Vercel runtime log caught a real visitor on `/membership-2` at 19:51 on 2026-08-18, and the old URL inventory was rebuilt from the **Internet Archive’s** record of the domain instead of guessed — 12 WordPress-era paths and 13 pre-WordPress `.htm` paths, **all 25 verified end to end**, trailing-slash forms included. **The one server-side lever that can actually evict a browser cache** is `Clear-Site-Data: "cache"`, now attached to every WordPress-era redirect: a request for one of those paths is near-proof of pre-cutover state, so we drop this origin’s cached responses — including the stale `/` that is the reason they were seeing the old site at all. That forced a structural choice: **Next.js config redirects are emitted with no headers whatsoever** (verified — those 308s carry not even the site’s security headers), so the WordPress redirects moved into `src/proxy.ts` where the response is ours to build, while the `.htm` set stayed in `next.config.ts`, where the header would be wasted anyway (a decade-old bookmark is not a warm cache) and where the proxy’s dot-excluding matcher cannot reach. **Caveat: Safari ignores Clear-Site-Data entirely**, so iPhone visitors still need one manual reload. Also replaced the **stock Next.js 404** — a bare white “This page could not be found” with no masthead, no nav and no way onward — with a branded page that states the site moved and offers the six destinations people actually come for; it catches the old URLs that cannot be enumerated (printed flyers, deep links the Archive never captured). Its button row is a fixed 3-across grid rather than a flex row, which otherwise wrapped 5 + 1 and stranded one button alone — the same failure #107 exists to prevent, in buttons instead of words. **A workaround needing no instructions:** `www.slotab.org` is a different cache key and serves the new site `200`, so pointing someone there sidesteps a poisoned apex cache without asking them to clear anything. **Surfaced and now urgent: the old WordPress site is still running** at the old GoDaddy IP, answering `200` over HTTPS with a valid slotab.org certificate — the runbook’s “cancel GoDaddy hosting 48h after go-live” step was never done. | Claude ✅ |
| 2026-08-17 | 140 | **🔴→✅ The live Donate button was a demo stub. Wired to the existing Square storefront as an interim.** Erik asked how the old site took donations; the answer surfaced that **`/donate` shipped at launch with its submit button stubbed** — a `window.alert()` reading *"Demo prototype — Square integration pending board decision (Q1)"* that recited the donor's own details back at them. Confirmed live by finding the string in the production JS bundle. On a fundraising site two days past launch, that was the primary conversion path. Erik's call: collect the money now, sort the splits later. **The old site's mechanism was documented — in one line, in the backlog** (*"currently one-at-a-time on `slotab-3.square.site/#YtTGmq`"*), which is why it never surfaced in the register. Inspected the live storefront: it carries **a per-sport donation item for all 25 sports plus GENERAL ATHLETICS**, and each exposes a **$0.00 "enter your own amount"** option, so the "$10.00 - $100.00" label on the listing is not a ceiling. **That makes the designation carry authoritatively** — linking to the right item puts the sport *in the Square transaction itself*, which is exactly what the Treasurer reconciles against, and is stronger than any note passed alongside. New `data/square-donate.ts` maps all 27 team slugs to product URLs; **every one of the 25 distinct URLs verified 200**. Three collapses are unavoidable and are shown to the donor rather than hidden: all three cheer programmes land on `CHEER`, both wrestling teams on `WRESTLING`, and the panel names the exact item their receipt will show. **Intent that Square cannot carry is captured, not discarded** (Erik's follow-up): recurring/monthly, the 4-year lock-in, the anonymity preference and tier qualification have nowhere to go in a one-time hosted checkout, so a post-handoff panel lists precisely those choices and offers a **pre-written email to the membership address**. Inelegant, but durable — an email in an inbox — and it needs no backend, which is the binding constraint until Q1. **Verification caveat:** the preview pane could not exercise the click-through — scrolling is frozen and React did not respond to synthetic input events — so URL mapping, build and lint are verified but **the donor-facing flow wants a real-browser spot check after deploy**, the same gap as #101's sponsorship marker. | Erik + Claude ✅ |
| 2026-08-17 | 139 | **The JV photo gap explained: JV Black / JV Gold is a 2026-27 split, so the squad note field now carries what the photo can't.** Erik supplied the missing context — Junior Varsity **splits into two squads this year**, JV Black supporting **JV Football** and JV Gold supporting **Flag Football**. That resolves the puzzle from #137, where the coach named four Fall teams but the photographer delivered three portraits: the split is *new*, so last season's shoot has one combined JV squad and no separate Black/Gold photos exist yet. His call was to state the split now and post photos when they're taken. **Needed a field, not just copy.** `TeamPhoto` had `photo` and `label` only, and the caption is styled small-uppercase with 0.15em tracking — right for "VARSITY", unreadable for a sentence. Added an optional **`note`**, rendered inside the same `<figcaption>` but resetting case, tracking and weight and held to a **52ch measure** so it doesn't run the full 1100px photo width. Generalises beyond this case: a photo from a prior season, a group that isn't quite what its label says. Decap's `teamPhotos` list gains the field with a hint pointing editors at exactly that use. **Nice side effect:** the Flag Football connection is now visible from the cheer page, which is the kind of cross-program detail the site had no way to express before. Verified rendering — uppercase label, sentence beneath in normal case at a comfortable measure. *(Pixel measurement was unavailable: the preview pane reported `innerWidth: 0` throughout, so this was confirmed from computed styles plus a screenshot rather than layout metrics.)* | Erik + Claude ✅ |
| 2026-08-17 | 138 | **Video stills ruled out as a TCC action source — the footage is sideline, not competition.** Erik proposed pulling stills from the seven stunt clips to fill TCC's missing action shot. Good instinct, wrong source, and the checking was the point: sampled **start, middle and end of all seven clips — 14 frames — and every single one is on the running track beside the football field**, daylight, stadium and hills behind, crowd in the stands. That is *fall sideline* cheer at a game. **TCC competes indoors on a mat**, so a still from these would misrepresent the team in exactly the way the dance photos did in #123 — the filenames ("Double Rewind Pyramid with Bajas", "Quick Toss to Cupie with Corkscrew") describe elite skills and sound competition-ish, which is precisely why they needed looking at rather than assuming. **A second, independent disqualifier:** the clips are **768x432**, some 432x768, against the 1200px photo standard. Even setting the subject aside, a hero band built from a 768px frame would be visibly soft, and the Fall page already carries four high-res action photos from that same setting — a still would be a straight downgrade. **No change made.** TCC's hero stays photoless and its card stays a posed portrait until a competition photo arrives, which only the coach can supply. Recorded so the idea is not re-proposed: nothing currently in hand can fill that slot. | Erik + Claude ✅ |
| 2026-08-17 | 137 | **Coach McDonald's photo drop lands: 12 correctly-labelled cheer photos close four open items at once.** Three zips in `photo-inbox/`. **Every file was self-labelling**, which made this the opposite of the #112 guessing problem — `Fall Cheer FROSH 2025.jpg`, `Winter Cheer VARSITY 25-26.jpg`, `TCC Team 1 25-26.jpg` and so on. Processed all 12 through `photo-intake` (the `-auto-orient` fix from #127 mattered here — several originals were 5400-7346px camera files). **Closed by this drop:** *(1)* **the Frosh squad**, unrepresentable since #132 for want of a field — Owen Main shot three Fall squads, so `/teams/cheer` now carries **`teamPhotos` captioned Varsity / Junior Varsity / Frosh**. Worth noting the coach names **four** Fall teams (JV Black and JV Gold separately) but the photographer shot JV as a single "JR VARSITY" squad, so three portraits cover four teams. *(2)* **Action shots for the heroes** — all three cheer pages had photoless black bands; Fall now leads with a dusk stunt group on the football field, Winter with a gym stunt under "LET'S GO TIGERS" signs. That answers her "elite and athletic, not spirit squad" brief directly. *(3)* **My winter portrait guess was wrong, and is now moot** — #136 assigned `ccheer-team.jpg` on the reasoning that a ~13-athlete squad matched "its own separate Varsity sideline team". The real `Winter Cheer VARSITY 25-26.jpg` is a different photo entirely. Flagging it in code as unconfirmed was what made this cheap to correct. *(4)* **TCC's portrait** is now its official squad photo rather than the retired-page leftover. **Still not closed:** TCC has **no competition action shot** — everything supplied is *sideline* cheer, fall on the field and winter in the gym, while TCC competes on a mat. Its hero stays photoless and its card stays posed. **⚠️ Seven `.mp4` stunt clips came in the third zip and were NOT used** — B E A T with Stunts, Kick Full Basket Toss, Double Rewind Pyramid and others. Team pages have no video support and #59 deliberately made Hudl the video platform, so dropping 20 MB of MP4 into `public/` would be inventing a feature. Logged as a question. Six older cheer files are now unreferenced, including both dance photos pulled in #123 — kept on disk, since `gdance-team-2025.jpg` is the Dance team's portrait if that page ever gets built. | Erik + Claude ✅ |
| 2026-08-17 | 136 | **Fall and Winter cheer split into separate pages — #132's multi-season approach was the wrong read.** #132 gave Tiger Cheer `seasons: ["Fall", "Winter"]` so it appeared in both blocks on `/teams`. Erik pointed out the flaw: **both entries pointed at the same page**, so a parent clicking Cheer from the Winter block landed on a page describing the Fall program — Fall squads, Fall captains, Fall coaching staff. Coach McDonald's words were *"distinct teams for each season"*, and distinct teams want distinct pages. Now three separate cheer pages: **`/teams/cheer`** (Fall, the Varsity / JV Black / JV Gold / Frosh sideline program), **`/teams/winter-cheer`** (Winter, the separate Varsity sideline squad), and **`/teams/competitive-cheer`** (Winter, CIF-sanctioned TCC). **Deliberately not copied to the winter page:** the wishlist, because those items (uniforms $8,000, camp $15,000) are **program-level** and duplicating them would double-count in any aggregate; the assistant coaches, because that roster is Fall-team specific (two JV Black, two JV Gold) and the winter staff is unknown; and the captains, which are Fall squad captains. Winter carries the program-level items only — head coach, liaisons, team site and Instagram. **Photo hunt found two unreferenced cheer portraits**, both verifiable as cheer rather than dance: `ccheer-team-2025.jpg` has **"SAN LUIS OBISPO HIGH SCHOOL 2025 VARSITY CHEER" printed in the image itself**, which is far stronger evidence than a filename (the #112 lesson) — it becomes the Fall page's `teamPhoto`, **partly closing the photo gap left when the dance photos were pulled in #123**. The other, `ccheer-team.jpg`, a single ~13-athlete squad, goes to Winter on the reasoning that it matches "its own separate Varsity sideline team" where the other is a ~25+ group on a football field — **flagged in the code as unconfirmed**, worth a word with the coach. **The `seasons` field added in #132 is now unused by any team.** The `teamSeasons()`/`teamInSeason()` helpers stay, since they are the read path for nav, carousel and index and handle the single-season case identically; only the field has no consumer. Left in place rather than reverted — removing it would mean touching four call sites for no behavioural gain, and a genuinely two-season team may yet appear. | Erik + Claude ✅ |
| 2026-08-17 | 135 | **Triaged the inherited External Inputs backlog against the actual repo — five rows closed, four re-measured, four turned into questions.** The register had grown to ~50 rows, roughly half predating launch and carried forward without re-checking, which made the whole list untrustworthy. Verified each against the code and data rather than the notes. **Closed as already done:** HOF **ceremony date + venue** (Oct 10 2026, The Octagon Barn — corrected back on 2026-08-11); the HOF **Booster Bash tickets URL** (a real Auctria vanity link; `hof.json` contains **zero `#` placeholders**, so the "replace the dead link" framing was obsolete); the **Platinum-tier sponsor list** (renamed **Champion** at the re-tier and populated); and **Spring team portraits** — **18 of the 20 teams with pages carry one**, and the sports that row named (softball, beach volleyball, boys volleyball, boys swim & dive, boys tennis) **have no team page at all**, so they were never going to show a portrait. Also corrected a stale "resolved" note that still gave the repo as `eramberg/slo-tab-website` two months after the transfer to `SanLuisObispoTAB/website`. **Re-measured with real numbers, because "outstanding" hid the actual state:** team liaisons are **12/20 done**, not untouched; sponsor websites are **5 of 54**, i.e. barely started rather than nearly finished; team wishlists are **4/20**; and "Amber Whighton's email" is really **11 board rows with no email**. **Turned into explicit questions rather than left as data gaps:** the **thermometer goal** (there is *no thermometer on the site* — the row asks for the input to a feature that was never built), the **shared events Google Sheet** (the weekly department-sheet cron plus CMS events may already be the answer in practice), the **monthly team-totals report** (`/impact` was built from year-end QuickBooks exports instead, so it is unclear the cadence ever started), and the **wine meeting outcome** — listed open here while **struck through as resolved earlier in the same document**, with #88 having shipped the tier ladder it was meant to decide. **Also verified genuinely still open, with the framing corrected:** the HOF donate URL and alumni join URL are empty strings whose CTAs *self-hide* rather than broken `#` links, the HOF committee array is empty so its section self-hides, `STORE_URL` is still empty so the Shop nav link lands on a page with nothing to buy, and `/admin-portal` is reachable but verifiably empty (stub mode, 0 records). | Claude ✅ |
| 2026-08-16 | 134 | **TCC was falling through to the generic student-section fallback; given its own photos from the existing library.** Erik flagged the `/teams` card. Cause: #132 created the `competitive-cheer` slug but added **no entry to `TEAM_PHOTO_BY_SLUG`**, so `teamPhotoFor()` returned `FALLBACK_TEAM_PHOTO` — a crowd shot of the student section, which says nothing about competitive cheer. The library already held two unreferenced files from the competitive-cheer page retired in #92: **`ccompcheer-2025.jpg`** (gym group, 1200x801) and **`ccompcheer-buchanan-2025.jpg`** (outdoor line-up, 661x418). **Both are posed portraits, and that is a deliberate exception rather than a violation** of the action-shot rule (#72/#85): the library has **no competitive-cheer action shot at all** — every `ccheering-*` file is *sideline* cheer, a different program — which is exactly the "posed is acceptable when no action shot exists" carve-out already covering baseball and girls wrestling. Commented as such, to be swapped the moment an action shot arrives. **Split across the two surfaces so they don't repeat:** the index/carousel takes `ccompcheer-2025` (higher resolution and a tighter group, so it survives the 270px card crop), and the team page takes `ccompcheer-buchanan-2025` as its `teamPhoto`, which also gives TCC a "Meet the Team" section it didn't have. **Hero left photoless on purpose** — the black band from #108 — because filling it with a posed portrait is precisely the bug #96 removed from the cross country gallery. Verified no card anywhere still resolves to the fallback. | Erik + Claude ✅ |
| 2026-08-16 | 133 | **Weekly schedule feed: the scraper is NOT broken — the sheet is empty. Correcting my own diagnosis, and hardening so the difference is visible.** I first reported the scraper as broken, on the grounds that its `htmlembed` URL now returns a Drive shell with no `<table>`. **That was wrong**, and worth recording because the reasoning was superficially sound. `htmlembed` is used **only for tab discovery**, by regex over JS embedded in that page — which still works and correctly found the live tab. The actual event data comes from a **different endpoint**, `export?format=csv`, which returns **200** and parses cleanly. Verified directly: the CSV for the current tab contains exactly **two rows — a title and the `Sport | Date | Day | Opponent | …` header — and no data rows**. So the pipeline is healthy end to end and **the source sheet genuinely has no games in it**: the department clears the tab once a week ends, and hasn't posted the next. The cron has also not missed a run (last Wed 12 Aug, next Mon 17 Aug, exactly as scheduled). **The real defect was that these two situations were indistinguishable.** `fetchTabEvents()` did `if (headerIdx < 0) return []`, so a genuine structural break — sheet re-laid-out, export format changed, wrong document served — produced the same silent empty result as a quiet week, and the Action went green either way. Two changes: a **missing `Sport` header now throws**, naming the tab, gid, row count and URL, so a real break fails the job loudly; and **zero events across all tabs now prints a prominent warning** while still succeeding, since an empty week is legitimate and must not fail the build. Verified the warning fires against the live empty sheet and the run still exits 0. The throw path is a five-line guard whose branch I did not exercise directly, having no 200-response-without-a-header to test against. **No change to the endpoints** — the CSV one works, and swapping it as I originally proposed would have been a fix for a problem that did not exist. | Claude ✅ |
| 2026-08-16 | 132 | **Tiger Cheer restructured to Coach McDonald's brief: own URL, two seasons, and TCC as its own team.** Her 13 Aug email listed five requirements; #122 and #123 had handled two (the name, the dance photos). This closes the other three. **(1) URL.** She flagged it directly — *"the current URL & photos on the page are actually for our Dance team"*. Moved `/teams/dance` → **`/teams/cheer`** (`git mv` on both the data file and the route, plus `teams.json`, the `team-photos.ts` key, and the route's hardcoded title). A **308 redirect** keeps the old public URL working, following the pattern set by the cross-country merge; it also frees `/teams/dance` for the actual Dance team if that page is ever built. **(2) Two seasons.** *"Tiger Cheer is not just a fall program!"* — Fall runs Varsity, JV Black, JV Gold and **Frosh** sideline teams, Winter a separate Varsity sideline team. The schema allowed exactly one season, so Cheer would have **silently dropped out of the nav every December**; `"Year-round"` was the only escape hatch and would have falsely claimed a Spring season. Added optional **`seasons: string[]`** with `teamSeasons()` / `teamInSeason()` helpers in `seasons.ts`, following the same singular→plural pattern as `headCoach`→`headCoaches` and `teamPhoto`→`teamPhotos`. Nav, home carousel and the `/teams` index all read through the helpers, and the index lists a multi-season team **under each of its seasons** so a parent scanning Winter still finds it. Verified: Tiger Cheer now appears in both the Fall and Winter blocks and its carousel badge reads "FALL · WINTER". **(3) TCC.** *"It deserves its own dedicated page under Winter Sports rather than being folded into sideline Cheer."* New **`/teams/competitive-cheer`** — CIF-sanctioned, Winter, State qualifiers in its first season (from her bio in #125), with the three distinct needs she named as an unpriced wishlist rendering "Pricing TBD", which is exactly what that fallback is for. Coach and liaisons carried over; no photos yet, so it runs the photoless black hero from #108. **Still outstanding from her brief:** the Frosh squad and the four-team Fall structure aren't represented anywhere — there is no field for squad names short of rosters or per-squad photos — and she wanted the page to read as *elite and athletic* rather than "spirit squad", which is a photo-selection question still waiting on her action shots. | Claude ✅ |
| 2026-08-16 | 131 | **SLOHS Athletics + SLOTAB President's announcement incorporated — and it contradicts the site on the September board meeting.** **🔴 Conflict, not resolved here.** The President's announcement says the next board meeting is **"September 16th at 6 pm at Cannon Corp"**, joinable **via Teams**. The site says **Monday September 14**, "(Zoom available)", carrying the note *"moved a week for Labor Day"*. **September 16 is a Wednesday**, which breaks the first-Monday cadence every other meeting in the file follows, so one of the two is wrong and it is not mine to guess — a parent acting on the wrong one turns up on the wrong day. Left as-is pending confirmation; also unresolved whether the meeting is Zoom or Teams. **Applied: the Fall Sports Parent Meeting**, Mon **17 Aug**, 6:00-8:00 PM, mandatory for at least one parent/guardian per athlete, starting in the Theater with the Athletic Director and SLOTAB, then breakouts with coaches. This **clears one of the three "still needed" events** named in `slotab-events.json` since #97/#98 — physicals night and the spring fundraiser remain. Labelled **"Athletics Event"** rather than "SLOTAB Event" because it is the school's meeting, not ours; the note in the file records why it is carried on our calendar anyway. Verified on both `/upcoming` and the home calendar. It was **the next day** when this landed, hence going in ahead of the rest. **Applied to `/volunteer`:** the two contact addresses the page had been missing entirely — `slotabboosterbash@gmail.com` for Booster Bash volunteering *and* auction donations, `slotab.tigerapparel@gmail.com` for the Tiger Den merch trailer. The page said "Volunteers welcome" with no way to act on it. Added the **new concessions revenue share**: a partner team earns **25% of non-BBQ Tiger Bites sales** on its game night, with Flag Football working the 21 Aug opener — that is a new program worth stating plainly, since it is the clearest answer a team gets to "what do we get for working a shift". **Fixed live staleness:** the page was still advertising *"Back to School Day — Thursday, August 13 … Grab a shift →"* three days after the event. Now gated on the date, following the file's existing `TODAY_MIDNIGHT` idiom, so dated one-off calls to action drop off by themselves. Evaluated at build time like the meeting dimming, so it clears on the next deploy past the date. **Not applied, deliberately:** match results (girls volleyball's two wins, the tennis and football scrimmages) have **no schema to live in** — `events.json` holds fixtures, not scores — and a results feature is a product decision, not a data entry. **Girls Tennis vs Bullard (Fri 21 Aug) was NOT hand-added**: the announcement gives no venue, and `isHome` maps `null` to "@", which would publish an away fixture that may be at home. It should arrive via the weekly sheet cron regardless. | Claude ✅ |
| 2026-08-16 | 130 | **Girls Flag Football intake applied; wishlist costs now accept ranges instead of flattening them.** From Coach Monroe's returned intake doc. **Applied:** his bio (first season leading girls flag, **24th** as a head coach at SLOHS, 17th with girls basketball, teaches Health and AVID); **five assistant coaches** with full titles; and a four-item wishlist. Phone was marked N/A, so the field stays absent. **The wishlist needed a code change, not just data.** `WishlistEntry.cost` was `number | undefined` — one figure or "Pricing TBD" — but **two of his four items are ranges**: a catching machine at "$3,000-4,000" and throwing targets at "$50-100 a piece". Picking one end would invent precision the coach didn't quote, and dropping the cost would show "Pricing TBD" for an item he *had* priced. `cost` now accepts `number | string`: numbers and bare numeric strings format as currency, anything else renders exactly as typed. Coaches naturally quote ranges and per-unit prices, so this will recur. Decap's cost field moved from a `number` widget to `string` for the same reason — and `formatCost()` treats a bare numeric string as a number, so an editor typing `1000` still gets `$1,000` rather than a literal `1000`. Verified both behaviours live: flag football renders `$1,000 · $3,000–4,000 · $50–100 each · $5,000`, and Tiger Cheer's all-numeric list is unchanged. **Explicitly pending on the coach's side, so nothing was invented:** team captains ("working on this"), roster ("mostly complete — some girls still need uniforms and definitive positions"), off-site links, and the team photo (**picture day was 8/19**). **⚠️ Naming inconsistency to settle:** the page is titled **"Varsity Girls Flag Football"** but three of the five staff are **JV** coaches (two Co-JV Head Coaches and a JV assistant), so the page now lists JV staff under a Varsity heading. Either the program runs both squads and the name should lose "Varsity", or the JV coaches belong on a separate page. Not changed unilaterally — it is a naming call. Also minor: the coach signs himself **Dan**, while the site says Daniel. | Claude ✅ |
| 2026-08-16 | 129 | **New Tiger Pride tier on the sponsor wall, four sponsors placed — and my own #128 bug caught in production.** **🐛 First, the self-inflicted one.** #128 added SLO Credit Union's logo to `public/sponsors/gold/` but **not** its counterpart under `public/sponsors/alpha/`. The homepage wall (`TigerSponsorWall`) doesn't render the original logo at all — it maps every path through `alphaPath()` to a transparent PNG generated with `-fuzz 18% -transparent white -trim`. So the logo was **404ing on the live homepage** between #128 and this commit; `curl` confirmed `/sponsors/alpha/gold/SLO-Credit-Union.png` returning 404 while the gold copy returned 200. `next build` cannot catch this — the path is just a string, and a missing file in `public/` fails silently at runtime. Fixed, and a validation sweep over all 54 sponsors then found **two pre-existing breakages of the same kind** (Designing Spaces That Sell, Untamed Petal, both Varsity); alphas generated for both. Varsity is hidden on the homepage in `compact` mode, which is why nobody noticed. **New tier.** Tiger Pride ($1,000, confirmed by Erik) existed on the membership sheet since #88 but had **no equivalent on the sponsor wall**, whose ladder was Champion/Gold/Silver/Varsity. Added end to end: the type union, a `tierSlug()` helper, `TIER_SIZES`, CSS for both the tile min-height and the grid column width, sized to sit between Silver and Varsity. **The trap worth naming:** `TigerSponsorWall` derived its CSS class with `tier.toLowerCase()`, which turns "Tiger Pride" into a string **containing a space** — React would have emitted `class="tiger-sponsors-tier tiger pride-tier"`, silently splitting into two useless class names with no error anywhere. `tierSlug()` strips whitespace and is now used on both walls. **Placements:** SLO Safe Ride → Gold (website verified at slosaferide.com rather than assumed); Crocker Coastal Construction → Silver; San Luis Obispo Heart → Tiger Pride; **Primary Eyecare Center moved Silver → Tiger Pride** — it was **already on the wall**, so this was a tier change rather than an addition, and its existing logo was kept because the newly supplied banner adds a doctors'-names line that is unreadable at a 105px tile. Its file stays under `/sponsors/silver/` — harmless, since folder names already don't track tier labels (Champion lives in `platinum/`, Varsity in `bronze/`). **Ghostscript is not installed**, so ImageMagick could not rasterise the two PDF banners; `qlmanage -t -s 2400` did it natively at good resolution (`sips` only manages 72 dpi). Worth remembering for the next PDF logo. Decap's sponsor description now spells out the full ladder with prices and warns that every logo needs its alpha twin. | Claude ✅ |
| 2026-08-16 | 128 | **SLO Credit Union added to the Gold wall — the first sponsorship the new site can be credited with.** Cheer brought in **$5,000**, which is **Gold** on the 2026-27 sheet (Champion is $10,000). Name confirmed as **SLO Credit Union**, not the "SLO Bank" first reported — their own site and page title say credit union, a distinction those institutions care about and not one to get wrong on a $5k listing. Entry carries `website: https://slocu.com/`, so the logo links out like the Champion entries do. **Artwork handling:** the supplied PNG was 1080x1080 with the mark floating in a wide white margin. Since `.slotab-sponsor-logo` already paints a white card *and* adds 1rem of padding, that baked-in margin would have rendered the mark noticeably smaller than its neighbours. Trimmed the surrounding white (content is 955x613 of the 1080 square) and resized to 640x411 — the lockup itself is untouched, only the empty ground removed. No transparency needed given the white card. Appended rather than alphabetised: the Gold list is two appended batches, not one sorted run, so appending matches how the recent entries went in. **Verified rendering** in the Gold tier at 281x162 in a 196px card, linking to slocu.com. Worth noting Gold now holds both **Banc of California** and **SLO Credit Union** — no conflict, but the board may want to be aware when placing banners or approaching either for renewal. | Erik + Claude ✅ |
| 2026-08-16 | 127 | **17 XC state-meet photos classified from the images, not the filenames — and a real `photo-intake` bug found doing it.** Classified by building a numbered contact sheet with `magick` and actually looking at every frame, per the #112 rule that a filename is not evidence of contents. Result: **9 boys, 7 girls, 1 co-ed** (the coaching staff). Only one frame was genuinely ambiguous at thumbnail size — zoomed in and it was clearly boys, SLO's #1547 among San Diego and Verdes runners. **A corroborating signal worth remembering for future XC drops: bib numbers split cleanly by squad — girls 1535-1540, boys 1541-1547** — which independently confirmed every visual call. **🐛 Bug fixed in `scripts/photo-intake.mjs`:** `resizeInto()` ran `-resize -quality -strip` with **no `-auto-orient`**. Cameras commonly record a landscape sensor read plus an EXIF `Orientation` flag instead of rotating pixels; `-strip` then discards that flag and the photo ships sideways with nothing left in the file to say so. Caught on the coaches shot, which arrived 4000x3000 tagged `RightTop` and would have gone live rotated 90°. `-auto-orient` now runs first, baking rotation into pixels before the strip — verified the output is 1200x1600 portrait and loads correctly in the browser. **This was latent for every photo intake to date**, so it is worth a spot-check of previously processed portraits. Also converted `bXC-state-straight` from PNG to JPEG: **2,076 KB → 240 KB**, an 89% saving for no visible loss — the intake script preserves the source format, so a photo arriving as PNG stays a 2 MB PNG. **Placement follows the standing action-shot rule (#72/#85, and #96 specifically):** the 16 action frames go in `gallery`, interleaved boys/girls so neither squad leads the whole grid; the **posed coaching-staff photo went to `teamPhotos`, not the gallery** — #96 removed a posed portrait from this exact gallery, so putting one back would have re-created the bug it fixed. Hero left as the CIF Championship photo from #111. Verified all 17 render with zero broken images. | Claude ✅ |
| 2026-08-16 | 126 | **Cross country updates from Coach Steinmaus; roster held, and the roster file carries Student IDs.** Applied: **two new assistant coaches** (Jadon Lai, Cooper Lock) joining Kira Abercromby and Tera Dube from #109; **team leaders** as captains, kept split by squad — `"Boys: Will Woffinden, Thad Hough, Kai Liwanag"` and `"Girls: Kasey Abercromby, Eloise Barnickle, Claire Folden, Leah Siciliano"` — because `captains` is a flat `string[]` joined with `·`, and a combined boys+girls program needs the distinction preserved; and an **Athletic.net** external link (`anet.io/Z47VCKSJ`, team code Z47VCKSJ). Coach asked for her bio, which the intake form had marked missing — **it is already live** and has been since #109, sourced from the school's coaches page. **⚠️ The roster spreadsheet's first column is `Student ID`**, alongside last name, first name, grade and t-shirt size for 44 athletes. Student IDs are district identifiers and must never reach the site under any circumstance — if the board later approves publishing rosters, that column gets stripped at intake, not filtered at render. Roster held per Erik's instruction and pending the same policy question Tiger Cheer raised (#125). **Note for the coach:** she asked for the assistants to appear on "BOTH the Girls and Boys sites" — there is only **one** cross country page, co-ed under two head coaches since #96/#110, so a single entry covers both squads. Her spelling "Tera Dube'" was left as the existing "Tera Dube"; worth confirming whether it should be Dubé. She also calls them "Team Leaders" where the page label reads "Team Captains". **17 state-meet photos staged** into gitignored `photo-inbox/` and audited: all 17 need renaming to the `<b\|g\|c><sport>-<descriptor>` convention, most already 1200px wide, two oversized (`girls+start` 2834x1889, `coaches+state` 6.7 MB). **Not classified by guesswork** — only two are self-describing from their filenames, and #112 is the standing lesson that a filename is not evidence of a photo's contents. Awaiting curation. | Claude ✅ |
| 2026-08-16 | 125 | **Tiger Cheer intake applied — with an embargo respected and three items held back.** The Cheer team returned a filled-in intake doc (`Tiger Cheer SLOTAB Website.docx`). **Applied:** Lisa McDonald's full bio (25 years coaching, 7th fall season at SLOHS, grew the program from one varsity team to five, launched the TCC team that qualified for State in its first season); **seven assistant coaches** with their own titles kept verbatim — note the structure is a program head coach *plus* per-team head coaches, so Alexis Aguirre reads "Varsity Cheer Head Coach" under `assistantCoaches`, which is correct rather than a mistake; a five-item **wishlist** totalling $24,800 (Cheer Camp $15,000, uniforms $8,000, AirTrack $1,000, Spirit Signs $500, TumblTrak cradles $300); **external links** to slohighcheer.com and their Instagram; and the tagline replaced with the team's own line, "Be part of the Hype. Spirit. Tradition." — their words beating our placeholder. Head coach phone was explicitly **declined**, so the field stays absent. **⚠️ The catch worth recording: the doc carries an embargo.** Under Captains it says *"The following will not be announced until later this week"* over the Varsity Spirit Captain and both JV squads' captains. Only the two **Varsity Team Captains** (Jazmine Patton, Jillian Davis) were published; the other five names were held. Verified after render that none of the embargoed names appear anywhere in the page text. Anyone applying the rest of this doc must re-read that line first — a bulk paste would have published student names the team had not yet announced. **Held back, needing a decision (see External Inputs):** the **48-athlete roster**, because it would make Tiger Cheer the **first team on the site to publish athlete names** — no team file currently uses the `roster` field at all — and because `RosterEntry` requires a jersey `number`, which cheer does not have (their athletes carry positions: Base / Flyer / Backspot / Tumbler, plus grade). Also held: the **core values list** ("Trust • Integrity • Grit • Excellence • Respect" and the team sayings), which has no field to live in. Two useful notes from the doc: **media day with Owen Main on Sat 22 August** should supply the real hero and portrait pulled in #123, and **Felisha Connolly** — the liaison replaced in #122 — holds galleries of past-season action shots (felishae@gmail.com). One transcription judgement: the doc reads `AirTrack ~$,1000`, recorded as $1,000. | Claude ✅ |
| 2026-08-16 | 124 | **Web Analytics confirmed collecting — resolves #119. The API 404 was my connector, not the site.** Erik enabled it and supplied a dashboard screenshot showing the URL `vercel.com/eramberg-9955s-projects/slo-tab-website/analytics` — **byte-for-byte the team and project the connector queries**, and the one serving slotab.org, so no account or project mismatch. **Analytics** and **Speed Insights** are separate sidebar entries and the correct one is selected, ruling out that mix-up too. Read **10 visitors / 52 page views** over the last 7 days, up from 4 / 20 minutes earlier in the same sitting, with the curve lifting off zero exactly at Aug 15-16 — consistent with the toggle date and with data actively flowing. So the pipeline from #101 is working end to end and the earlier `404 Web Analytics not found` was the Vercel connector's token lacking analytics read scope, **not** the un-enabled state I inferred from it. **Recording the misread honestly:** a `not_found` was reasonable evidence but not conclusive, and I stated the "not enabled" reading more firmly than the evidence carried. The dashboard is the authority here; the API is a convenience. **Two caveats before the board leans on these numbers.** (1) **Custom events are unconfirmed.** The visible panels are Pages / Routes / Hostnames and Referrers / UTM — no **Events** panel in frame, which is where `section_first_view` and `returning_visitor` land, and those four section counts are the entire reason #101 chose Vercel Analytics over a bare cookie. The project is on **Hobby**, and custom events may be a paid-tier feature; the `/event` beacon returning `200` proves nothing either way, exactly as the `/view` `200` didn't. Needs a look at that page for an Events section — a plan question, not a code one. (2) **Hobby retention and event quota** are narrower than Pro, so data can age out before a full-season or year-over-year comparison is possible. **Reading note for whoever presents this:** the counts are **consenting visitors only** — decline or ignore the banner and the script never loads — so every figure is a floor, not total traffic. The `+3` beside `slotab.ravens-peak-consulting.com` means four hostnames are aggregated; the Hostnames tab separates real slotab.org traffic from the alias and board admin visits. Some of the early count is verification traffic from this session. | Erik + Claude ✅ |
| 2026-08-16 | 123 | **Tiger Cheer's hero and team photo pulled — both were the dance team.** Erik confirmed the first two photos on `/teams/dance` (the banner and the posed team portrait) are **dance**, not cheer, so `heroPhoto` (`ccheering-taryn.jpg`) and `teamPhoto` (`gdance-team-2025.jpg`) are removed from the team file. Worth noting the hero was named with a `c` (co-ed) prefix under the #77 convention and still turned out to be dance — **the filename prefix is not evidence of what a photo shows**, which is the same lesson as the `ctrack-action-2023.jpg` misfiling in #112. **No new code needed** — both paths were already built for this: a photoless hero paints itself brand-black instead of going transparent over cream (#108), and the "Meet the Team" section self-hides when no `teamPhoto`/`teamPhotos` is set. Verified: hero renders `slotab-team-hero no-photo` at `rgb(10,10,10)` with the kicker, name and tagline legible, and the page reads as deliberate rather than broken. **The `/teams` card and home carousel are unaffected** — they source from `TEAM_PHOTO_BY_SLUG` in `team-photos.ts`, whose `dance` entry is the *third* photo (`ccheering-sideline-2025.jpg`, also the remaining gallery item), so the index still shows an image. **Both files deliberately left in `public/photos/`** rather than deleted: they are now unreferenced, but if Dance becomes its own team (still open below) `gdance-team-2025.jpg` is precisely its team portrait, and deleting it now would mean re-sourcing it later. `npm run photo-usage` will flag them as unused — that is expected, not a defect to fix. Coach has been asked for real cheer photos; until they land the page runs photoless above the fold. | Erik + Claude ✅ |
| 2026-08-16 | 122 | **Cheer is "Tiger Cheer", with no gender designation — which needed a code change, not just a data edit.** Straight from the Cheer team: the name is **Tiger Cheer** and **no gender is indicated**. Liaisons are now **Belinda Rollins** (belinda@therollins.family) and **Sally Ross** (sally.ross@parable.com) — this **replaces Felisha Connoly**, who was the sole liaison on the team file; flagging that explicitly since it is a removal, not just an addition, and #98's sheet had listed Connoly + Olsen. **Why this wasn't a one-line edit:** every display path assumed a team *has* a gender. Setting it to `Co-ed` would have been wrong (it asserts a designation the team didn't give) and would still have printed "Co-ed" on the `/teams` card and in the team-page kicker. So `gender` is now **optional** end to end — absent means no designation and the label is simply omitted: `TigerNav` and `TeamsCarousel` show the bare name (same branch co-ed teams already took), the `/teams` card drops the gender chip entirely rather than rendering an empty one, `DonateForm` shows the name with no empty parenthetical, and `TeamPage` builds its kicker by filtering out absent parts so it reads `SLOHS · Fall` instead of `SLOHS ·  · Fall`. Four sort comparators that called `.localeCompare` straight off `gender` would have thrown on an absent value — all guarded. The Decap schema now marks gender `required: false` on both the teams list and the team file, with a hint saying blank = no designation, so an editor can do this without code. **Slug deliberately left as `dance`**: renaming it changes the live URL and the Cheer-vs-Dance question from #98 is still open — not a change to make in passing. Also updated the hardcoded route title, which was still "Cheer — SLOTAB". **Verified** on every surface: team page (`h1` Tiger Cheer, kicker `SLOHS · FALL`, both new liaisons, no "Girls", Connoly gone), `/teams` card (no gender chip while Football still shows BOYS and Cross Country CO-ED), nav dropdown ("Tiger Cheer" beside "Girls Flag Football"), donate dropdown (no empty parens anywhere), home carousel. `tsc` clean, `next build` clean, eslint unchanged at 3 pre-existing. **Not touched: the photos.** The team says some current shots are of the *dance* team and needs culling — Erik is awaiting the specific list, so `teamPhoto` (`gdance-team-2025.jpg`) and the gallery are untouched by design. Tracked under External Inputs. | Claude ✅ |
| 2026-08-16 | 121 | **Sponsorship scroll event confirmed working — closes the last open item from #101.** #101 shipped the `section_first_view` sponsorship event but could not exercise it: the preview pane freezes scrolling and rAF, so `IntersectionObserver` callbacks are never delivered. Retried against production this session and hit the **same wall, but this time with proof rather than a suspicion** — attaching an independent control `IntersectionObserver` to the very same `#sponsorship-marker` element also never fired, and `window.scrollTo` left `scrollY` at 0, so the failure is unambiguously the harness and not the code. What *could* be verified in production: the marker is present and is **1px tall**, so the zero-area fix from #101 did ship correctly. **Erik then confirmed it works in a real browser**, which retires the item. Verification lesson, same shape as the bot-mitigation one in #115: scroll- and viewport-driven behaviour on this site has to be checked in a real browser, and a control observer is the cheap way to tell "my code is broken" from "this environment can't run the test". | Erik + Claude ✅ |
| 2026-08-16 | 120 | **`first` visit date was stamped in UTC, so evening visitors saw tomorrow's date on `/privacy`.** `recordVisit()` and `recordSection()` both used `new Date().toISOString().slice(0, 10)` — the **UTC** date. California is UTC-7/-8, so from ~4-5pm local onwards every new visitor's record stamped the *next* day. Caught while verifying the live site: the cookie read `"first":"2026-08-17"` at 5pm on the 16th. Small, but it lands on `/privacy` under **"First visit"** — the one page whose entire job is showing visitors accurately what we hold about them, so a date that is visibly wrong undercuts exactly the trust that page exists to earn. Replaced with a `todayLocal()` helper building the string from `getFullYear`/`getMonth`/`getDate`. **Not backfilled:** records already written keep their off-by-one date. There is no server-side copy to migrate from, the cookie is the only store, and rewriting people's stored records to "fix" them is a worse trade than letting it self-correct for new visitors. Verified locally: stored `2026-08-16` where UTC would have written `2026-08-17`, and `/privacy` renders it. | Claude ✅ |
| 2026-08-16 | 119 | **Vercel Web Analytics was never enabled — every custom event since #101 has been discarded.** Erik asked how to get at the cookie data, which surfaced that the reporting half of #101 has been dark since 2026-08-11. **Client side is faultless:** on production, `/_vercel/insights/script.js` loads `200` and a `view` beacon POSTs `200` once consent is granted. **But the Analytics query API returns `404 Web Analytics not found`** for `slo-tab-website` — confirmed via the Vercel API as the project serving `slotab.org`, `www.slotab.org`, `slo-tab-website.vercel.app` and the ravens-peak alias, so this is not a wrong-project mixup. That combination — collection endpoint accepting with a `200`, query API reporting no dataset — is what an **un-enabled** Web Analytics looks like: the beacon is taken and dropped. The `200` is precisely why this never surfaced as an error. **This is the whole reason #101 chose Vercel Analytics:** the cookie alone records data nobody on the board can read, and without the dashboard toggle we are back in exactly that position, having shipped the consent banner, the privacy page and the tracking for it. **Fix is one toggle** (Vercel → project → Analytics → Enable); nothing is retroactive, so the counter starts at zero whenever it is flipped. Caveat worth keeping: the API cannot fully distinguish "never enabled" from "enabled, zero rows" — but `not_found` rather than an empty result set is the strong signal, and the dashboard settles it at a glance. Owner is Erik: it is a dashboard action, not a code change. | Erik 🔴 |
| 2026-08-16 | 118 | **`/board/cms-access` shipped; the stranded May work re-derived rather than replayed.** Clears the last item from #117 — the uncommitted work parked in the `hopeful-ramanujan` worktree since 2026-05-18, 100 commits behind. Reviewed hunk by hunk against current `main` rather than rebasing wholesale, and it split three ways. **Shipped:** the `/board/cms-access` page (241 lines — editor access = *Write* on the GitHub repo, the chair's invite flow, what a first-time GitHub user sees, the two admin URLs, and removing an outgoing officer) plus an **Operations** section on the board hub linking to it. **Dropped as superseded:** the `public/admin/config.yml` hunk pointing `backend.repo` at `SanLuisObispoTAB/website` — already done in #67 — and the `docs/editor-onboarding.md` hunks, where **`main`'s #87 version is the more accurate one**: it says the invite mail reads *"@eramberg has invited you to SanLuisObispoTAB/website"*, which is how GitHub actually addresses org-repo invitations (from the inviting person, not the org), whereas the May draft had *"@SanLuisObispoTAB has invited you to website"*. **Still applicable and applied:** `README.md` and `docs/decap-setup.md`, both **byte-identical to their May base** — `main` never touched either in 100 commits — so the diffs applied cleanly. That mattered more than it sounds: `decap-setup.md` still described **the wrong repo entirely** (`eramberg/ravens-peak-consulting`, a copy-paste from another project) in its intro, OAuth-app setup, and admin URL, so a board member following it would have been sent to a repo that has nothing to do with this site. **Drift caught during verification:** the new page carried the same inaccurate invite-mail wording the onboarding doc had already been corrected away from — fixed on the page so the two agree, since the page tells the chair to send the editor that very doc. Page admin URLs deliberately left on `slo-tab-website.vercel.app` (with the ravens-peak alias for school wifi): they match what `editor-onboarding.md` currently tells editors, and both move together in the still-pending paired OAuth-callback/`base_url` swap. **Verified:** `tsc --noEmit` clean; `next build` clean with `/board/cms-access` prerendering as static; eslint unchanged at 3 pre-existing errors; gate confirmed working (`/board/cms-access` 307s to `/board/login?next=%2Fboard%2Fcms-access` when unauthenticated); page and the hub's Operations link both render with no console errors. | Claude ✅ |
| 2026-08-16 | 117 | **Post-launch repo hygiene + baseline tags; a 765 MB junk commit dropped off `main`.** First push after go-live, so the outstanding local work got inventoried before anything was sent. Local `main` had diverged: **1 commit ahead, 36 behind**. The one local commit, `7db81d9`, was labelled *"incorporate sponsor-lead feedback — re-tier wall + portal, two-ways, seasonal Donate, Tiger Friend"* — but its actual contents were **57 binary files, 765 MB of raw team portraits, zero lines of code**. The real sponsor-lead work was already live as `8993102` (identical message); this was a duplicate that swept in the untracked `Team Photos/` folder. It could never have been pushed anyway: it carried a **375 MB zip**, and GitHub hard-rejects any file over 100 MB. **Dropped it** (reset `main` to `origin/main`) after moving the folder out of the repo first — the files were *tracked* in that commit, so a plain `reset --hard` would have deleted 765 MB off Erik's disk. Photos are back in place, now invisible to git. The commit stays reachable from `claude/adoring-jones-8f6063` and `claude/cookie-data-access-3c97ae` if it's ever wanted. **`.gitignore` gained `/Team Photos/`** (same rule as `photo-inbox/`: originals stay local, only the resized 1200px versions are committed) **and `~$*`** for the Word lock files the `.docx` guides leave in `docs/`. **Baselines tagged:** `launch-2026-08-15` on `d3f3fa8` — the exact verified-live state from #114/#115 — and `baseline-2026-08-16` on this cleanup. Prior tagging convention was a single `backup/photos-2026-05-25`. **Still parked:** uncommitted `/board/cms-access` work in the `hopeful-ramanujan` worktree, 100 commits behind, under separate review — see External Inputs. | Claude ✅ |
| 2026-08-16 | 116 | **Deleted the dead pre-Tiger `Header.tsx` / `Footer.tsx`.** Both had zero importers — live chrome is `components/tiger/TigerNav.tsx` + `TigerFooter.tsx` via `LayoutShell.tsx`. Leftover from the 2026-04 refactor preview, and actively misleading: `Header.tsx` carried a stale hardcoded 5-team Teams dropdown contradicting the season-driven auto-injected nav (#74/#79), plus a drifted footer link list — a trap for anyone grepping for a nav link. Originally committed 2026-08-11 as `75f977a` claiming decision #90, but it never reached `origin/main` (it sat on top of the junk commit in #117) and #90 has since been taken by the baseball-photo fix — hence the renumber to #116. Cherry-picked code-only; the stale status-doc hunk was dropped in favour of this entry. `tsc --noEmit` clean, `next build` clean with all routes still prerendering, `npx eslint src` down to 3 pre-existing errors (`CoedCardFlip` ×2, `/watch` namespace) — none in touched files. | Claude ✅ |
| 2026-08-16 | 115 | **Apex made the serving host; www left serving with a canonical, deliberately.** After the cutover, `www.slotab.org` had ended up the primary with `slotab.org` 308-redirecting to it — inverting every canonical signal already shipped (`metadataBase`, the canonical tag, `robots.txt` `Host:`, and all 34 sitemap URLs name the apex). Vercel's control turned out to be a radio pair on the domain row — **"Connect to an environment" vs "Redirect to Another Domain"** — not the "set as primary" affordance the docs describe; the apex row was set to redirect. Flipped it to *Connect to an environment → Production*, so `slotab.org` now serves directly. **Vercel would not allow editing the `www` row afterwards**, so www still serves rather than redirecting. **Left as-is on purpose:** both hosts emit `canonical: https://slotab.org/`, so search engines consolidate on the apex anyway, and both are in `INDEXABLE_HOSTS` so neither is suppressed. A 308 from www would be marginally tidier but buys nothing the canonical isn't already delivering — not worth further dashboard churn. **Also corrected a false alarm from this session:** `www` appeared to 403 during checks, which I initially reported as an outage. It was **Vercel's bot mitigation** (`x-vercel-mitigated: challenge`, `x-vercel-challenge-token`) responding to rapid automated curl requests — real browsers were never affected, confirmed by loading the page. Lesson for future verification of this site: use the browser for liveness checks and space out any scripted ones. **Verified live on slotab.org**: `/membership` renders the 2026-27 tier sheet with the corrected pass counts (Ten/Eight/Six/Four/Two), the sponsorship tracking marker is present, titles and canonicals correct. | Claude ✅ |
| 2026-08-16 | 114 | **🚀 slotab.org is LIVE on the new site — DNS cutover complete.** Erik recovered the GoDaddy account via a Customer ID the Treasurer located, then pointed DNS at Vercel: `A @ → 216.198.79.1` and `CNAME www → 1415f8f3520238e1.vercel-dns-017.com` (Vercel's newer per-domain values, not the generic `76.76.21.21`/`cname.vercel-dns.com` the docs publish — which is exactly why the runbook said to use the dashboard's own values). **Two snags worth recording.** (1) Vercel's config panel appeared to offer "only a CNAME" — he was expanded on the `www` row. An apex domain *cannot* be a CNAME (the zone apex must hold its own SOA/NS, and GoDaddy offers no ALIAS/ANAME flattening), so the apex row is the one carrying the A record. (2) Immediately after the DNS save the **site was briefly down**: the apex had a valid cert and 308-redirected to `www`, which had *no* cert yet, so visitors hit an SSL error. It resolved on its own once Vercel issued the www certificate. Suspected contributor: three stale records left behind by the old GoDaddy Managed WordPress + Cloudflare CDN — `CNAME _acme-challenge → …dcv.cloudflare.com`, `TXT _acme-challenge.www`, `TXT _cf-custom-hostname.www` — which delegate ACME validation to Cloudflare; flagged for deletion regardless, since cert validation for our domain should not route through infrastructure we no longer use. **Verified live**: correct title and build; `slotab.org` and `www` both indexable with valid certs; **`slotab.ravens-peak-consulting.com` still returns `x-robots-tag: noindex, nofollow`**, so the host-aware indexing from #100 is doing its job in production; `robots.txt` and the 34-URL `sitemap.xml` serving; `/`, `/contact`, `/membership`, `/booster-bash`, `/teams/cross-country`, `/privacy`, `/watch`, `/upcoming` all 200; `/board` still 307s to login; `/admin` reachable. **Open:** `www` ended up the primary domain with the apex redirecting to it, which inverts every canonical signal already shipped (`metadataBase`, the canonical tag, `robots.txt` `Host:`, and all 34 sitemap URLs name the apex) — recommended flipping Vercel's primary to `slotab.org`, one click and no deploy, rather than rewriting the code to www. Also still open: Search Console submission, the paired OAuth-callback/`base_url` swap, deleting the stale Cloudflare records, cancelling GoDaddy hosting after 48h, and **confirming auto-renew before the 2026-09-19 expiry**. | Claude ✅ |
| 2026-08-11 | 113 | **Diagnosed the empty GoDaddy DNS panel by resolving the domain instead of guessing at the UI — and found a five-week expiry.** Erik got GoDaddy access but "DNS Management" listed no domains. Rather than walk through menus blind, looked the domain up: registrar is **GoDaddy.com, LLC direct** (not a reseller brand, which would have meant a different login portal) and the nameservers are `ns07/ns08.domaincontrol.com` — so **GoDaddy really does host this zone**, and an empty panel is an account/permissions problem, not a "DNS lives elsewhere" one. Most likely cause documented with the exact path: **Delegate Access without switching accounts** (you land in your own empty account; the domain sits in the grantor's), plus `account.godaddy.com/access` and the `dcc.godaddy.com/control/portfolio` deep link, and how to tell whether the board simply has two accounts (hosting in one, registration in the other). **Two findings that change the cutover's risk profile.** (1) **No MX and no TXT records** — the domain carries no email at all, so the single biggest cutover hazard (nameserver delegation silently killing mail) does not apply. The entire zone is one A record (`160.153.0.90`, a GoDaddy hosting IP) plus a `www` CNAME. Softened the runbook's MX cautions to match reality rather than leave boilerplate that doesn't apply. (2) 🔴 **`slotab.org` expires 2026-09-19 — 39 days out.** Nothing on the Vercel or code side matters if the registration lapses; the DNS records go with it. Added "confirm auto-renew and that the card on file is valid" as the runbook's first action, flagged as especially worth checking since the board just changed hands. | Claude ✅ |
| 2026-08-11 | 112 | **Photo library audited as a whole; six duplicate-on-page bugs fixed, four of them mine from #111.** Erik spotted boys water polo showing the same action shot twice, asked about more flag football, and asked for a full inventory of `public/photos`. All three turned into the same root problem: **`photo-intake`'s duplicate check is byte-based**, so a re-export or re-crop of a photo already in the library sails through under a new name. New `scripts/photo-usage.mjs` + `npm run photo-usage` compares a **16x16 grayscale signature** of every file, and reports (a) unused photos, (b) same image under two filenames, (c) any single page showing a photo twice. First run on 133 files: **38 unused, 13 duplicate groups, 6 pages showing a photo twice.** All 12 flagged pairs were confirmed genuine by eye before anything was deleted — the signature had no false positives. **Three real errors, all introduced by me in #111:** (1) `ctrack-action-2023.jpg` is pixel-identical to `bbball-huddle-bw.jpg` — the source `010923Trc-107.jpg` is a **basketball** huddle, not track; I guessed "Trc" = track from the filename and put a basketball photo in track & field's gallery. Removed; track's gallery is empty again. (2) Four galleries (boys soccer ×2, girls soccer, girls tennis, girls water polo) got a second copy of a shot they already had. Removed. (3) `ccheering-stunt-2022.jpg` was both a duplicate *and* a posed team portrait in an action slot — two rule breaks in one file. Removed. **Not my error but worth recording:** the source file `SLOHS 2025 VARSITY VOLLEY TEAM.jpg` **contains the JV team** — its own burned-in banner reads *2025 JV GIRLS VOLLEYBALL*. The "Varsity" portrait added in #110 was therefore the JV squad under a varsity label; removed, and girls volleyball is back to JV + Frosh with **varsity still needed**. Also fixed the pre-existing bug Erik found: `bwpolo-2.jpg` and `bwpolo-teitge-2022.jpg` are the same image and both sat in the boys water polo gallery (`bwpolo-2` stays in the library — the home carousel points at it). **Flag football:** Erik was right that action shots existed — **five** were in `public/photos` and **four were unused**, the same wired-up-nothing pattern as the volleyball portraits in #110. Gallery 0 → 4. Ten redundant files deleted. After: **pages showing a photo twice 6 → 0**, missing galleries 7 → 6. Three duplicate groups remain by design (`bfball-helmets`/`bfball-vsPR-2022`, `bwpolo-2`/`bwpolo-teitge-2022`, `gtennis-2022`/`gtennis-team`) — each pair is used on different surfaces or one side is unused. | Erik + Claude ✅ |\n| 2026-08-11 | 111 | **19 action shots wired in from Erik's dropped folders — cross country finally has a hero.** Erik reported `photo-intake` "isn't working" on two folders he'd added. **It wasn't broken:** the script scans `photo-inbox/`, while the folders went to `public/photos/originals/` — which is the script's *output* archive for full-res sources (`--keep-originals`), not an input. Both folders were already gitignored (`.gitignore:54`), so nothing was at risk of deploying. Reviewed all **180 files** by generating contact sheets with ImageMagick (`montage` needs a font this box lacks, so the grids were assembled with `-thumbnail` + `+append`/`-append`) and classifying by filename pattern. Findings: the set is far more *game-day* than *game-action* — a large share is homecoming court, mascot, crowd, and **files with text burned into the image** ("Tiger Nation", "How 'Bout Them Tigers?", "Not in Our House", senior-night captions). All overlay files were excluded; they're graphics, not photographs. Also corrected two filename-classification traps: the `SLOHS2022-*` batch is **boys soccer**, not generic, and `JVFootball093022-308/309` are **cheer stunts**, not football. Selected 19, staged through `photo-inbox` under the naming convention, and processed with `npm run photo-intake -- --process`. **Cross country's hero** is now the **CIF Championship celebration** — both squads, at the meet, with the Division III plaque — which retires the plain black band from #108, ties visually to the CIF title in Berkeland's bio (#109), and **removes cross country from the three posed-portrait exceptions** listed in `team-photos.ts`, so the home carousel slot is now a real landscape action shot per Erik's request. Galleries filled: **football 0 → 7** (sideline huddle, helmets up, night huddle, celebration, helmet close-up, a kick, student section), girls volleyball 0 → 1, track & field 0 → 1, boys soccer 2 → 4 (incl. a genuine 50-50 challenge), girls soccer 2 → 4 (two keeper shots), girls water polo 3 → 5, girls tennis 2 → 4, cheer/dance 1 → 2. Audit after: **missing action heroes 3 → 2**, missing galleries 8 → 7. **Still no action shots exist for baseball, girls wrestling, boys wrestling, girls golf, flag football or stunt** — those slots stay empty by the #108 rule, and baseball + girls wrestling remain the last two posed-portrait carousel exceptions. | Erik + Claude ✅ |\n| 2026-08-11 | 110 | **Cross country made the template for the other teams; gaps now tracked by a script instead of memory.** Erik: XC is the bar every team should hit, **except football and track & field** — a three-level program and an ~18-event program with a 10-coach staff don't fit the single-squad shape. **(1) Gap tracking.** New `scripts/team-audit.mjs` + `npm run team-audit` reports per team what's missing (action hero, action gallery, squad portrait, head coach, bio, email, liaison), flags posed portraits in action slots, and with `--write` regenerates `docs/team-page-gaps.md`. The two large programs are held to a lower bar via `LARGE_PROGRAMS` and reported separately; multi-squad expectations live in `EXPECTED_SQUADS` so a missing JV or Frosh photo is *reported*, not silently absent. Baseline: **1 of 17 complete** (cross country), 15 missing coach bios, 8 missing action galleries, 7 missing liaisons, 3 missing action heroes. **(2) Co-ed card flip.** Cross country and track & field run co-ed, so their `/teams` card now shows one squad at random and crossfades to the other on hover or keyboard focus — neither squad is permanently the face of the program. Randomising during render would break hydration, so the server always emits boys-first and the coin flip happens in an effect. Touch devices get a slow auto-cycle instead (no hover); reduced motion keeps the swap and drops the fade. Track & field is wired but inactive until it has per-squad photos. **Cascade bug found doing this:** the crossfade silently did nothing because `.slotab-teams-season-section.is-focused … img { opacity: 1 }` (0,4,1) — the focus-bloom effect, live whenever a section is on screen, i.e. always — outranked my (0,3,0) rule, so the trailing squad just covered the leading one. A reduced-motion rule at (0,3,1) would have done the same. Rules rewritten long enough to outrank both, with a comment saying so, since shortening them would break the flip invisibly. **(3) Erik's photo folder.** Checked `Team Photos/2025-26 SLOHS TEAM PHOTOS/` — 58 files, all posed portraits (no action shots; those still come from Nancy). Pulled the four that closed real gaps through `photo-inbox` → `npm run photo-intake --process`: football **Varsity/JV/Frosh** and the volleyball **Varsity** that completed a set whose JV and Frosh portraits had been sitting unused in `public/photos` while the page showed **no portrait at all**. Football and girls volleyball now render three captioned squads each. **Naming follow-on:** with three squads on the page, `football.json`'s name *Varsity Football* and `girls-volleyball.json`'s *Varsity Girls Volleyball* were wrong — both renamed (football now matches `teams.json`, which already said "Football"). Baseball, flag football and track & field keep their `Varsity` prefix; they genuinely show only that squad. Still outstanding: track & field boys/girls portraits — not in the folder. | Erik + Claude ✅ |\n| 2026-08-11 | 109 | **Cross country coach bios sourced from the SLOHS coaches page; two assistants added.** Erik pointed at `sites.google.com/a/slcusd.org/slohs-track-and-field/cross-country/coaches` as the source of truth, then noted the assistants are listed there too. Fetched it and replaced both head-coach bios with the school's own text: **Nancy Steinmaus** (M.A. exercise physiology UC Davis, 3× NCAA Div. II All-American, ex-head coach at MiraCosta, two-time Pro National Champion duathlete, at SLOHS since 2018) and **Dan Berkeland** — whose stored bio was an abridged version missing the decade in Italy and Sweden, the family, and, notably, **the girls' CIF Championship in his first season as head coach**. The page also confirms the boys/girls split assigned in #108. Added **Kira Abercromby** and **Tera Dube** as assistant coaches — the page lists no bio or email for either, so they render as name + role cards, which the existing markup already handles. Linked the source page as the first external link so the bios have a visible provenance and the board can see where to update them. **Two styling bugs caught in the process:** (1) the multi-coach bio block I added in #108 reused the class `.slotab-coach-bio`, which already existed at **0.9rem** for the small assistant staff cards — so both head-coach bios were rendering at assistant size inside a prose section. Renamed to `.slotab-headcoach-bio`; verified 18px, and zero elements left on the old class. (2) The coach-name heading was set at 1.15rem = **18.4px against 18px body text** — a 0.4px difference that read as body copy, not a heading. Raised to 1.3rem (20.8px). No emails are published on the school page, so the board-supplied addresses already in the file were kept. | Erik + Claude ✅ |
| 2026-08-11 | 108 | **Cross country page fixed, and the action-shot rule made consistent across all 19 teams.** Erik: the combined XC page (merged in #96) showed only one coach and one squad photo, and its "In Action" gallery held a posed portrait. Three fixes, generalized rather than special-cased. **(1) Two head coaches.** Nancy Steinmaus (boys) and Dan Berkeland (girls) were modelled as one `headCoach` plus an "assistant" whose role field read *Head Coach* — so the facts band named one and demoted the other. Added `headCoaches: Coach[]` with a per-coach `role`, normalized in `TeamPage` to one shape (`team.headCoaches ?? [team.headCoach]`), so the band now reads **HEAD COACH — BOYS** / **HEAD COACH — GIRLS** and the bio section becomes *Meet the Coaches* with both. **(2) Two squad photos.** Added `teamPhotos: [{photo, label}]`, same normalization; "Meet the Team" now shows the boys and girls portraits captioned *Boys* and *Girls*. Both plural forms override their singulars, so the other 18 teams needed no data migration. **(3) The action-shot rule.** Audited every team JSON for posed portraits (`-team-` filenames) sitting in action slots: **4 violations across 3 teams.** Cross country had its boys portrait as both hero *and* the sole gallery item; **baseball and girls-wrestling each used the identical file for `heroPhoto` and `teamPhoto`, rendering the same photo twice on one page.** All now dropped — no XC action shots exist, so the gallery is empty and the section self-hides, exactly as asked. **Bug this surfaced:** removing `heroPhoto` left the hero band transparent over the cream page while its kicker/title/tagline are white — contrast measured **2.4:1, effectively invisible**. Added `.slotab-team-hero.no-photo { background: var(--slotab-black) }`, re-measured at **19.8:1**. Verified all 19 team routes return 200, the `no-photo` class appears on exactly the three affected pages and no others, and cross country renders 2 captioned portraits, 2 head coaches, 2 bios, and no "In Action" section. CMS hints on `heroPhoto`/`gallery` now say **action shots only** and explain that empty is preferred to a wrong photo; `teamPhotos`/`headCoaches` added to the collection. Rule recorded in `CLAUDE.md` with a grep to re-audit. **Gap flagged:** only `football.json` has a Decap collection — the other 18 teams, cross country included, are code-edit-only and not editable at `/admin`. | Erik + Claude ✅ |
| 2026-08-11 | 107 | **Line-balancing rule: centered text never ends on a short line.** Erik's rule — if a trailing centered line carries fewer than ~4 words, pull words down from the line above, biasing toward complete phrases. Measured the damage first with a Range-rect script that counts words per rendered line (no DOM mutation): on `/booster-bash`, **3 of 4** centered blocks violated it, the worst being a 3-line paragraph breaking `14 / 14 / 1` — a single word alone in the middle of an empty line. Implemented in CSS rather than per block, since `text-wrap` **inherits**: `.slotab-prose` → `text-wrap: pretty` (keeps greedy wrapping, pulls words down to kill widows) and every centered container → `text-wrap: balance` (evens all lines; right for centered text, which is short here). Set on containers, so paragraphs inherit and **new copy is covered automatically** with no per-element edits. One selector list in `slotab.css` covers the 17 centered `slotab-*` classes, the 5 `tiger-*` ones, and — via `[style*="text-align:center"]` — all 15 inline `style={{ textAlign: "center" }}` sites; React serialises with no space after the colon, and the selector is property-specific so it can't match `justify-content: center`. Ordering matters: the centered rule sits *after* `.slotab-prose` because elements carrying both are the same specificity (0,1,0). **Result:** `/booster-bash` 3 orphans → **0** (`14/14/1` → `9/10/10`), `/impact` 0, `/watch` 0, `/membership` 0 real (its one hit is the `.slotab-mail-address` nowrap block from #89 — the whole address held together as one unbreakable unit, which is intended). At 375px, 5 of 6 clean; the one flagged is the card's `Saturday, / October 10, 2026` breaking `1 / 3`, which trips the word count but lands exactly on the phrase boundary — left as-is, since the rule's *intent* is satisfied. **Caveats recorded in the CSS comment:** browsers cap `balance` at ~6 lines and silently fall back past that (fine for this site's short centered blocks); CSS cannot see phrase boundaries, so specific word pairs that must never split still need `&nbsp;`. Both values are progressive enhancement — unsupported browsers ignore them and nothing breaks. Rule added to `CLAUDE.md` so future sessions add new centered selectors to the list. | Erik + Claude ✅ |
| 2026-08-11 | 106 | **Hudl venue list corrected a second time — no pool; softball is in.** #104 fixed the "every team" over-claim but got the venues themselves wrong, listing *stadium, baseball field, gymnasium, and pool*. Erik confirmed with Hudl: coverage is **Holt Field (stadium), the Big Gym, and both the baseball and softball fields — the pool is not covered.** Corrected the `/impact` line, and this also resolves the discrepancy flagged in #104: `/watch` named only *Holt Field and the Big Gym*, missing two venues, and is now updated to the confirmed four. (The unrelated **$7,000 pool swim timing system** line is a separate purchase and stands.) Deliberately left alone: the `/watch` multi-camera upgrade section still says the *new* rig covers Holt Field and the Big Gym — that's a claim about the forthcoming install (#96), not current coverage, and Erik confirmed venues rather than rig scope. Venue list added to `CLAUDE.md` so it stops being re-derived wrongly. Totals untouched; `/impact` still reconciles at $210,414.79. | Erik + Claude ✅ |
| 2026-08-11 | 105 | **Booster Bash page gets the Comms lead copy + the Tiger's Den logo.** Comms supplied the 2026 announcement copy and the event logo; Erik asked for the copy "at the top." The existing `/booster-bash` layout ran artwork → date/venue card → body prose, so body copy would have landed *below* the logistics box — wrong place for a welcome. Added an optional **`intro`** field to `FeaturedEventPage` that renders between the artwork and the card, with the first paragraph set larger as the lead (`.slotab-event-intro`, centered, 680px, 1.35rem lead dropping to 1.15rem under 480px). Comms' four paragraphs went in verbatim apart from joining the date and doors-open lines into the venue sentence. **Logo:** the supplied 1024×1024 PNG was 1.1 MB; quantized to 128 colors and stripped → **276 KB** at identical dimensions (flat crest art, so no visible loss), installed as `public/photos/booster-bash-tigers-den.png` with descriptive alt text. Dimensions declared 1024×1024 so it takes the square (non-`wide`) 600px flyer frame. The field is generic, not Bash-specific — every featured event page gets it — so the Decap collection gained a matching **"Lead copy (shown at the top, above the date/venue box)"** list with a hint explaining the first-paragraph-is-larger rule, and `body`'s label was clarified to "(shown below the date/venue box)" so editors can tell the two apart. **Two wording notes left for Erik, not silently reconciled:** (1) Comms writes "the *iconic* Octagon Barn" while the venue field carries Trina's "The **Historic** Octagon Barn" (#98) — both now appear on the page, prose vs. proper name; (2) the intro's "Saturday, October 10th · doors 5:00 PM" restates the card directly beneath it. Normal editorial practice for a lead, but easy to trim either side if it reads redundant. Verified: `tsc` clean, DOM order artwork → intro → card → body confirmed, four intro paragraphs, no horizontal overflow at 1280px. Browser-pane screenshots were unreliable this session (blank captures, `clientWidth: 0`), so layout was verified by geometry rather than by eye — worth a look before it goes public. | Erik + Claude ✅ |
| 2026-08-11 | 104 | **Hudl line on `/impact` corrected — the streaming description over-claimed coverage.** Erik caught it in the #103 data: the line read "streaming **for every team** — game film for coaches and athletes, plus the live broadcast portal families watch from anywhere," but the Hudl cameras only cover **four venues — the stadium, baseball field, gymnasium, and pool**. Tennis, cross country, golf, and anything else competing off those sites gets no coverage, so the copy was promising donors something the $23,000 doesn't buy. Rewritten to name the four venues rather than assert blanket coverage, letting the venue list itself convey the limit. Also re-labelled the row `All Sports` → **`Home Venues`** and re-categorized `all-athletes` → **`facilities`**: leaving "ALL ATHLETES" as the tag next to a four-venue description would have re-introduced the same over-claim in the meta line. Total unchanged and still reconciled at $210,414.79. `/watch` was checked and needed no edit — it already says coverage varies by sport and "if their team isn't listed, it isn't being streamed" (#96). **Open discrepancy for Erik:** `/watch` names only *Holt Field and the Big Gym* as camera sites, which is narrower than the four venues above — one of the two is out of date, and the multi-camera rig from #96 may explain the gap. Left alone pending his read. | Erik + Claude ✅ |
| 2026-08-11 | 103 | **Real FY 2025-26 ledger data landed on `/impact` — the page is restructured around what the ledger can actually prove.** (Authored against the pre-rollover tree and rebased onto #94–#102; the decision number moved 90 → 103 and the watermark-removal credit belongs to #94.) Erik supplied the two year-end QuickBooks exports (`Transaction Report` July 2025–June 2026 + `Statement of Activity`), finally clearing **E_impact** / decision #4, open since 2026-04-23. All 111 line items transcribed and reconciled **to the penny** against the report's stated total: **$210,414.79** disbursed to SLOHS athletics. **The central finding: this is a disbursement ledger, not a purchase ledger**, which invalidated the page's core assumption. The old `impact.json` schema wanted item-level rows ("Molten volleyballs, set of 12 — $1,780"), but the money breaks down as **$117,050.97 (55.6%) in per-team allocations** — checks written to the SLOHS Athletic Dept per program, which then spends them, with *no record of what was bought* — plus **$84,086.08 (40.0%) in identifiable purchases**, **$6,500 (3.1%) in 26 coach stipends**, and **$2,777.74 (1.3%) in reimbursements**. Only that middle 40% is describable at all, and even then loosely ("25/26 Starting Expenses, $8,000"). **Erik's call on the allocations: aggregate, don't itemize.** The per-team spread is wide enough to be politically live — Football $24,917.73 and Boys Basketball $19,681.63 at the top against Softball $225 and Girls Golf $300 at the bottom — and publishing a public ranking invites parent-vs-parent comparison the board doesn't want to defend. So the page now headlines the total, shows the named purchases individually (Hudl $23,000 · football uniforms $10,000 · fall t-shirts $8,607.90 · season start-up $8,000 · pool swim timers $7,000 · senior banners $6,720 across three seasons · cheer uniforms $5,000 · warm-ups $5,000 · softball scoreboard $4,855 · gym equipment $3,563 · banners/signage $1,870.50), rolls the allocations into one $117,050.97 line, and lists all **24 funded programs as a name-only chip band with no dollar figures** so every team gets visible credit without a leaderboard. **Privacy:** the ledger names individuals on all 26 stipends and all 4 reimbursements (fingerprinting, a stunt-coach conference, football coach clinics, softball shed paint) — those are aggregated into two unnamed lines; **no individual's name reaches the public site**. Also withheld: the Statement of Activity's **net revenue of −$13,140.17** ($299,419.04 revenue − $97,489.31 COGS − $4,655.11 expenditures − $210,414.79 disbursed), a reserve drawdown that reads as board-internal, not donor-facing. **Code:** `impact.json` rebuilt (new `fiscalYear` + `summary` + `programs` keys; `cost` now `float` — the old `int` would have silently truncated the cents that make the reconciliation provable); `ImpactLedger.tsx` dropped the By Year / By Team toggle (meaningless with one real year and no per-team detail) and gained the programs band; the watermark/ribbon CSS was already gone (#94 removed it at the rollover) — this commit kept #94's `.slotab-impact-pending` empty-state styles and its zero-items branch intact, so the page still degrades gracefully if the ledger is ever cleared; `/impact` intro copy now pulls the live total instead of promising "year by year and team by team"; CMS collection updated to match with a hint recording the no-per-team-dollars rule. **Flagged for the board:** decision #32's thermometer anchor ("~$75K covers Huddle + T-shirts + senior banners") is roughly **2× the real figure** — those three line up at $38,327.90 actual. The Phase-1 goal number should be re-derived before the thermometer ships. Verified: `tsc` + `next build` clean, `/impact` prerenders static, no console errors, 24 chips render, no horizontal overflow at 375px or 1280px. | Erik + Claude ✅ |
| 2026-08-11 | 102 | **Cutover runbook written against the verified account state.** Erik asked for explicit step-by-step instructions. Rather than write them from the status doc's description, queried the Vercel API for ground truth — which surfaced a discrepancy worth knowing: **#67/#69 describe the deploy target as a separate "Raven's Peak Vercel team", but there is only one account** (`eramberg-9955s-projects`) holding both the `slo-tab-website` and `ravens-peak-consulting` projects. Also confirmed the latest push (`c82ece4`) is deployed and READY, so the pre-flight "is the new site healthy" check has a known-good answer. New `docs/dns-cutover-runbook.md` covers pre-flight (**`dig MX slotab.org` first** — the domain's email is the classic cutover casualty), adding the domains in Vercel *before* touching DNS so the cert can provision and Vercel reveals the exact records, the registrar edit, verification, Search Console, the paired OAuth change, and the GoDaddy cancellation. **Two rules called out hard**, because both are one-click disasters: **change records, never nameservers** (Vercel offers nameserver delegation, which would drop MX/TXT and kill email), and when cancelling GoDaddy, **cancel the hosting product, never the domain registration** (the DNS records live with the registration). Deliberately did **not** hardcode the A/CNAME values as authoritative — Vercel's docs say the published `76.76.21.21` / `cname.vercel-dns.com` are general and to use the per-domain recommendation, so the runbook prints them only as a sanity-check and tells Erik his own domain screen wins. | Claude ✅ |
| 2026-08-11 | 101 | **Opt-in cookie consent + engagement tracking, wired to Vercel Web Analytics.** Erik asked for a cookie tracking revisits and views of the Membership / Sponsorship / Donate / Volunteer sections, plus a banner. Flagged first that a cookie alone records data **nobody on the board can ever read** — there's no analytics backend — so he chose Vercel Web Analytics for real reporting, and opt-in (nothing before Accept) for the consent model, which is the right posture for a site whose audience is parents of minors. **Two cookies, no more:** `slotab_consent` (the choice itself — strictly necessary, so not gated) and `slotab_engagement` (visit count, first-seen date, sections reached), written **only** after Accept and **deleted** on withdrawal. No third parties, no ad identifiers, nothing personally identifying. Analytics only mounts under consent, firing `section_first_view` and `returning_visitor` events. **Sponsorship is tracked separately from membership on purpose:** both live on `/membership`, so route-matching alone would make the two numbers identical and useless — a marker above the sponsor wall fires only when that part of the page is actually reached. New `/privacy` page in plain English, linked from the banner and the footer, that **shows the visitor their own stored record** and lets them change or withdraw the answer. **Two bugs caught during verification.** (1) The consent state lived in local component state, so accepting in the banner left the `/privacy` panel reading "You haven't answered yet", and sections recorded while browsing never refreshed it. Rewrote both components on `useSyncExternalStore` over the cookie store with a `null` server snapshot — the correct primitive for this, and it also cleared two `react-hooks/set-state-in-effect` lint errors the first version introduced. (2) The sponsorship marker was a zero-height div at `threshold: 0.4` — a zero-area target has an undefined intersection ratio and can never satisfy a ratio threshold, so that event could never have fired. Now `threshold: 0` on a 1px element. **Verified end to end:** no cookies exist before a choice; Decline stores only the preference and browsing tracked pages while declined writes nothing; Accept records `visits: 1` + first-seen; visiting `/donate` adds `donate` and the `/privacy` panel updates live; Decline afterwards deletes the record and empties the panel; banner is legible at 375px. **One path I could NOT exercise:** the sponsorship scroll event — the preview pane runs with rAF and programmatic scrolling frozen, so IntersectionObserver callbacks are never delivered there. Needs a real-browser spot check after deploy: open `/membership`, scroll to the sponsor wall, then check `/privacy` lists "sponsorship" under Sections reached. | Claude ✅ |
| 2026-08-11 | 100 | **slotab.org cutover — the code side. Indexing is now host-aware rather than an all-or-nothing flag.** Erik verified the build and asked to initiate the cutover. The obvious move — delete `robots: noindex` and push — has a race in it: between the deploy and DNS actually propagating, the `*.vercel.app` URL and the `slotab.ravens-peak-consulting.com` alias would both be indexable, which is the exact thing the flag existed to prevent. And that alias **keeps serving the site after cutover** (it's the SLOHS-firewall path, #51), so it would stay in the index as a duplicate of the real site indefinitely. So instead of a flag flip: `layout.tsx` drops the blanket `robots` block and gains `metadataBase: https://slotab.org` + a canonical; `src/proxy.ts` now sets `X-Robots-Tag: noindex, nofollow` on any request whose Host isn't `slotab.org`/`www.slotab.org`. That removes the ordering hazard entirely — it ships before, during, or after DNS with the same result — and permanently protects the alias and preview deployments. The proxy's `matcher` widened from `/board/:path*` to all non-asset paths to do it, so **the board password gate was restructured and re-verified**: `/board` and `/board/president` still 307 to login, `/board/login` still reachable, public pages and the `/admin` rewrite unaffected. Also added `app/sitemap.ts` (15 static routes + 19 team pages, generated from `teams.json`; excludes `/admin`, `/admin-portal`, `/board/*`, and the stale unlinked `/spring-social`) and `app/robots.ts`. In `config.yml`, `site_url`/`display_url`/`logo_url` moved to slotab.org — but **`base_url` deliberately did not**: it's the host the OAuth handshake round-trips through and the GitHub OAuth App is registered with a single callback there, so swapping it before that registration is updated would break `/admin` login for the entire board. slotab.org is already in the postMessage allowlist (#65), so `slotab.org/admin` works today regardless. **Verified by spoofing the Host header**: localhost and the ravens-peak alias both return `x-robots-tag: noindex, nofollow`; `Host: slotab.org` returns none; no `<meta name="robots">` in the HTML; canonical resolves to `https://slotab.org`. **NOT done, and not doable from here — Erik-side:** the DNS change itself (registrar + Vercel domain attach) and cancelling the GoDaddy WordPress hosting 48h after. | Claude ✅ |
| 2026-08-11 | 99 | **`/contact` board roster re-laid out for scanability.** Erik, ahead of the production cutover: role left-justified, name + email right-justified, and a wider column so every row fits on one line. Previously the row was `Role: Name` on the left and the address on the right, which split the *person* across both edges. Now the role is a standalone left label (colon dropped, it's no longer inline) and name + address form one right-hand block joined by a thin `·`. The list moved out of the shared 820px `.slotab-prose` column into its own 1040px `.slotab-board-wrap` — the "Send Us a Message" copy stays in prose, in its own alt-background section. **Verified all 19 rows render at a uniform 56px with no role/contact collision** (widest spans 1000px of the 1040px column), so nothing wraps. **Caught on the mobile pass:** at 375px four rows overflowed the viewport — `slotabcommunications@gmail.com` and friends are single unbreakable tokens, and flex children won't shrink below content width by default. Fixed with `min-width: 0` + `flex-basis: 100%` on both sides plus `overflow-wrap: anywhere` on the name and address; the stacked layout now measures 375px scroll width exactly.  **Mobile follow-up (same day):** Erik asked for the role on its own line with name + email on the line below, indented — added a 1rem indent on the contact block and eased the address to 0.95rem, since at the prose base size a name + address pair missed a single 375px line by about nine pixels. Half the rows now hold one line; the four longest `slotab*@gmail.com` addresses still wrap, indented, which is unavoidable at that width. Desktop is untouched — verified all 19 rows still render at 56px with the address at full size and no indent. | Claude ✅ |
| 2026-08-11 | 98 | **Trina's Booster Bash notes + the August board meeting recorded, then shipped to `main`.** The Treasurer sent event details: Oct 10, 5 PM, **Historic** Octagon Barn; links for $100 tickets and the $100 two-person cornhole team (winner takes a prize); a `slotabboosterbash@gmail.com` link for donating silent/live auction items **on the home page as well as the event page**; the same address for volunteering (silent-auction coordinators, live auctioneer, MC). All applied: venue renamed, the cornhole entry and two `mailto:` links added to `/booster-bash`, and a new dark **Booster Bash band on the home page** carrying the date, a tickets CTA, and the donate-an-item ask. `FeaturedEventPage` now treats `mailto:` as external (no `target=_blank`, ✉ affordance) so those links behave. Separately, Erik confirmed the **August board meeting moved to Mon Aug 10 and was held**, and the venue is unchanged — added it noted "first meeting of the year — held", closing #97's open question. Past meetings on `/volunteer` now **dim rather than drop** (`.slotab-meeting-past`), so the list stays the year's full schedule while the next meeting reads as next. **Open for Erik:** Trina asked *"Do you want to donate a ticket to a coach link?"* — a product decision, not implemented. | Claude ✅ |
| 2026-08-11 | 97 | **2026-27 board meeting dates + Hudl status.** Meetings are the **first Monday at 6:00 PM**, September moved a week for Labor Day: Sep 14, Oct 5, Nov 2, Dec 7, Jan 4, Feb 1, Mar 1, Apr 5, May 3 — written to `slotab-events.json`, which feeds `/upcoming`, the home calendar, the ticker, **and** the meeting list on `/volunteer` from one entry each. The Labor Day exception needed somewhere to live (that list renders bare dates), so an optional `note` field was added to data, CMS, and the formatter. `/watch` now says the camera install date is **to be announced** rather than staying silent, and its coverage line was rewritten from a generic "not every sport is broadcast" to the real mechanism: **each head coach decides whether their team is streamed**. That also reshapes #60 — the AD deferred the public-vs-no-scout call to coaches, so the drafted defaults are now per-coach choices rather than a policy to ratify. | Claude ✅ |
| 2026-08-11 | 96 | **Real fall schedule drives the ticker; `/watch` gets the multi-camera Hudl news; cross country becomes one team.** The ticker always renders now — next six events plus a standing `GO TIGERS · Watch here for game day updates`. That standing line is both the copy Erik asked for and what stops a lone dated entry repeating three times through the marquee. Rather than describe Thursday's game vaguely, pulled the real slates from MaxPreps: **Girls Volleyball vs Stockdale, Thu Aug 13, 6:30 PM home** plus five more, and all ten football games — which independently **confirmed the six home volunteer dates**. **Girls tennis has no MaxPreps schedule at all**, so nothing was invented for the Friday match. **Two latent bugs found, both due to surface within ~2 weeks:** `EventCategory`/`CATEGORY_ORDER` were spring-only, and since `/upcoming` only renders events whose category has a filter chip, every fall game would have been **silently invisible**; and the scraper's `categorize()` sent *every* volleyball row to `boys-volleyball` with no fall sports at all, so football, water polo, and cross country would have fallen through to `non-sport`. Both fixed and verified. Cross country merged to one Co-ed page at the program's request, both coaches titled **Head Coach** (not the boys/girls split #95 had inferred), old slugs 308-redirect. Sue Karleskint added as Girls Soccer liaison. The 2023-24 Welcome Day sign cropped above the year line — dropping the stale year, date, and grade times in one cut. | Claude ✅ |
| 2026-08-11 | 95 | **Real 2026-27 data replaces the empty states from #94.** From the board's *2026-27 Board & Liaisons Coaches* sheet: `board.json` rebuilt (Andria McGhee President; Liz Moody to Member at Large; AD and Digital Specialist added), releasing the hold from #86 — and the three hardcoded `2025-26` strings now read `boardData.year`, so the year can't drift from the names again. Publishing convention set deliberately: an email appears only where the role owns a SLOTAB or district address; **personal addresses and all phone numbers were left off**, since `/contact` and the repo are both public. Head coach + email added to every team page, confirmed liaisons to ten; Girls Soccer's was **held back** pending confirmation (resolved in #96). Sponsor All-Sport Pass counts re-scaled 2/4/6/8/10 up the ladder, superseding the printed sheet's 2/2/2/4/6. Coach Traverso's Boys Water Polo bio, week-to-week captains, and unpriced underwater camera — which needed `WishlistEntry.cost` to become optional, rendering "Pricing TBD". New `/back-to-school` and `/booster-bash` built as a shared component over CMS-editable data, so pending artwork and copy are an `/admin` edit. **Two corrections:** the Booster Bash was on the site as **Oct 3 at Holland Ranch** — it's **Oct 10 at the Octagon Barn** — and the ticket URL open since #38 already existed; the printed QR tag decodes to a vanity link redirecting to the live Auctria event (decoded from the PNG and the redirect followed, rather than assumed). Two supplied photos were **deliberately not used**: both show 2023-2024, one also the 2023 date and grade times. **Security:** the sheet has a live Gmail password typed into the Fall Fundraiser row — not in the repo, flagged to Erik to remove and rotate. | Claude ✅ |
| 2026-08-11 | 94 | **Production rollover — every placeholder, fabricated figure, and stale demo feed removed; fall surfaces set to self-filling empty states.** Rule applied throughout: **where real data is missing, hide the surface or say plainly it's pending — never ship invented content**, and wire each empty state to the source that will fill it. Removed: a fake live stream (`LIVE` badge, "1,247 watching"), **six fabricated Hall of Fame inductees** (the real 46-name roll was in `hof-inductees.json` all along — the strip now derives from it), invented spend figures and a "$116,800 raised" card, 24 draft Impact line items **plus both DRAFT watermarks** (the second was painted on `.slotab-impact::after`, the ledger *container*, so it would have tiled over the Treasurer's real numbers later), 34 "Player Name" roster rows, 24 "Liaison TBD" entries with non-resolving mailtos, 16 invented wishlists, placeholder Remind/BAND codes, four dead `#` CTAs, and a demo member directory that attached invented emails and tiers to **real-looking local names** on a publicly reachable page. Rebuilt on live data: ticker and home calendar from the real event feed, `/volunteer`'s meeting list from the CMS events file. The weekly scraper was **verified healthy** — the school simply hadn't posted fall games. Also refreshed for fall: `/merch` copy and gallery, the featured-gear strip, sponsor tier names, and the Events nav. | Claude ✅ |
| 2026-08-06 | 93 | **Sponsorship lead's feedback incorporated (5 items; Erik made the 3 open calls).** (1) **Sponsor logo wall re-tiered** to the 2026-27 offering names — resolves **E17**. Since the wall is cleared/re-tiered fresh each season and Erik chose "keep last year's logos for now," did a rank-preserving rename: **Platinum→Champion, Gold→Gold, Silver→Silver, Bronze→Varsity** (4→4, no data merge, sizes preserved). Touched `sponsors.json` (tier values only — **logo file paths left as `/sponsors/platinum/…` etc.; `alphaPath()` derives image URLs from the stored path, not the tier name, so nothing moved**), `sponsors.ts` type union, `SponsorWall.tsx` TIER_SIZES, `PlatinumCarousel.tsx` (now reads the `Champion` tier + labels "Champion Sponsors"; `.slotab-platinum-*` CSS classes + the `PlatinumCarousel` filename kept as internal hooks), `TigerSponsorWall.tsx` compact filter (hides `Varsity` now, was `Bronze`), `tiger.css` + `slotab.css` tier classes (`.platinum→.champion`, `.bronze→.varsity`), `config.yml` select options + description. `/membership` "Business Sponsors" kicker set to **2025–2026** with a note that the wall refreshes for 2026-27 as businesses renew. Also re-tiered the admin-portal Springly directory (`springly-directory.json` sponsor entries Platinum→Champion/Bronze→Varsity + the `.slotab-portal-chip.tier-platinum/-bronze` CSS → `.tier-champion/.tier-varsity`) so the internal `/admin-portal` view matches the public wall (member tiers Tiger Pride/Champion/Coach left as-is — recognition categories, not the stale sponsor labels). (2) **Home "Three ways"→"Two ways to support"**: `THREE_WAYS`→`WAYS_TO_SUPPORT` = **Become a Member (primary, gold-accented) + Volunteer**; dropped the misleading "Sponsor a Team" card; added a small "back a specific sport → donate directly to any team" note under the grid. (3) **Seasonal Donate CTA** (Erik picked "Adopt + seasonal"): new `src/app/data/campaign.json` + `campaign.ts` (`isDonateDriveActive()`, modes off/on/auto-with-dates) resolved at build time in `page.tsx` and passed as a `donateActive` prop to `ClassicHero` (avoids hydration mismatch). Off (default) → hero + bottom CTA lead with **Become a Member**, Donate demoted to a ghost link; On/in-window → **Donate Now** returns to primary. New Decap `campaign` collection added. Only the homepage hero + bottom CTA carried "front and center" Donate prominence (nav pill was retired in #57; footer keeps a plain text link) — both now gated. (4) **Tiger Friend kept**, but removed the "Any gift, any amount — you're a member" bullet the lead flagged (tier + "Any amount" price stay). (5) Fixed stale bottom-CTA copy "$25 / $10-mo" → "Memberships from $50/yr ($5/mo)". **Logged, no code:** Impact-page stats (E19, Adam & Phil); real Donate-drive dates + when to flip the drive on (E20, Erik). Verified via `npm run dev`: home two-ways + Champion/Gold/Silver carousel/wall + seasonal Donate both states; `/membership` wall shows Champion 3 / Gold 14 / Silver 16 / Varsity 17 with all logos + alpha PNGs resolving (200); Tiger Friend line gone; no console errors; `tsc` + `next build` clean. | Claude ✅ |
| 2026-08-06 | 92 | **Cheer/Dance merged into one "Cheer" team + real Fall head coaches added to the live pages (from Erik's definitive coach list).** Erik supplied a board-definitive Fall coach roster (names/emails) and clarified that **Dance and Competitive Cheer are the same squad, called "Cheer"** — reversing the AD's #83/#84 split (the AD had described them as two teams). Applied on the site: **(1) Merge** — `dance` team renamed **Dance → "Cheer"** (`teams.json` + `teams/dance.json` name/sport, tagline → "Tiger spirit and cheer on every sideline."), **kept the `/teams/dance` URL** so the coach-outreach doc link stays valid; **retired Competitive Cheer** — `git rm` of `teams/competitive-cheer.json` + `teams/competitive-cheer/page.tsx`, removed its `teams.json` entry and its `team-photos.ts` map row, and dropped "competitive cheer" from the posed-shot exception note in the `team-photos.ts` header (dance's index shot `ccheering-sideline-2025.jpg` is an action shot, so no exception needed). The two comp-cheer photos (`ccompcheer-2025.jpg`, `ccompcheer-buchanan-2025.jpg`) are now unused in-library — available to fold into the Cheer page gallery later. `/teams/competitive-cheer` now 404s (fine pre-launch/`noindex`; nav + `/teams` index auto-drop it). **(2) Coaches** — added confirmed `headCoach` {name, email} (no bio, so the "Meet the Coach" section stays hidden until real bios arrive — cleaner than Football/GVB's visible stub bios) to the 7 previously coach-less Fall pages: **Cheer** — Lisa McDonald; **Boys Water Polo** — Bryan Traverso; **Girls Water Polo** — Trent Rubio; **Girls Golf** — Josh Heptig; **Boys Cross Country** — Nancy Steinmaus; **Girls Cross Country** — Dan Berkeland; **Flag Football** — Dan Monroe. The XC split from Erik's list is the non-obvious direction (Boys→Steinmaus, Girls→Berkeland). **Girls Tennis left coach-less on purpose** — the list gave an email (`tenniswithdennis40love@gmail.com`) but no name, so the page stays as-is and the outreach doc asks the coach for the display name. Football's JV (Graham Gilmore) + Frosh (Rick Cramer) coaches were captured in the Football outreach doc only, not the Varsity page. Coach identities here supersede the athletics-site pull from #90 (which they happened to confirm). Verified in-browser: `/teams/dance` renders H1 "Cheer" + Head Coach Lisa McDonald; `/teams` index shows a single "Cheer" card → `/teams/dance` and no Competitive Cheer; `/teams/boys-water-polo` shows Bryan Traverso with no stray bio section. `tsc` + `next build` clean (45 static pages; comp-cheer route gone). | Claude ✅ |
| 2026-08-05 | 91 | **`/membership` Tiger Friend card — "no minimum" line-wrap fix.** Erik flagged that in the Tiger Friend tier box "no minimum" was wrapping mid-phrase ("no" and "minimum" on separate lines). It rendered as one inline string `Any amount · no minimum` inside the flex-column `.slotab-tier-amount`, so the narrow card broke it arbitrarily. Fix: dropped the "·", moved "no minimum" onto its own line below "Any amount" via a new `.slotab-tier-per-note { display:block; white-space:nowrap }` modifier on the existing `.slotab-tier-per` span (`MembershipTiers.tsx`). Now mirrors the price/"/ year" hierarchy on the other cards. Verified in-browser (rendered node is block + nowrap inside the Tiger Friend card); `tsc` + `next build` clean, `/membership` prerenders static. | Claude ✅ |
| 2026-08-05 | 90 | **Baseball index/carousel photo was girls basketball — fixed; + Fall-coach outreach packet built.** Erik flagged that "boys baseball" showed a girls-basketball photo. Root cause: `bbaseball-baileyhartford.jpg` was a byte-for-byte duplicate (same md5) of `gbball-hartford.jpg` (girls basketball — Bailey Hartford), misfiled under a `bbaseball-*` name. It was wired into two surfaces: `team-photos.ts` `baseball:` (the `/teams` index + home `TeamsCarousel` card) and `HeroCarousel.tsx` slide 6 (generic "student-athlete" alt). Fix: `team-photos.ts` `baseball` → the real posed team portrait `bbaseball-team-1200x906.png` (no baseball *action* shot exists in the library, so the header's posed-shot exception now lists baseball alongside XC/girls-wrestling/comp-cheer); `HeroCarousel` slide 6 repointed to `gbball-hartford.jpg` with a correct "SLO Girls Basketball — Bailey Hartford" alt (keeps the same visual, now truthfully labeled); the misfiled `bbaseball-baileyhartford.jpg` **`git rm`'d** (zero loss — its pixels remain as `gbball-hartford.jpg`). Verified in-browser: `/teams` baseball card now resolves to `bbaseball-team-1200x906.png`, girls-basketball to `gbball-hartford.jpg`, no dangling refs; `tsc` clean. **Also (not a site change):** assessed all 11 Fall team pages for placeholder/confirm/missing content and generated a per-coach Word packet (11 `.docx`, one per team) listing exactly what each coach must send, with working preview links (`slotab.ravens-peak-consulting.com/teams/<slug>`). **Pulled real coach names + emails from the SLOHS athletics site** (`slohs.slcusd.org/athletics/fall-sports`, read via the in-app browser since WebFetch is firewall-blocked): Football Johnston✓, GVB Meaney✓ (both matched existing data), Boys WP **Bryan Traverso**, Girls WP **Trent Rubio**, Girls Golf **Josh Heptig**, XC **Dan Berkeland + Nancy Steinmaus** (co-listed, no gender split), Flag FB **Dan Monroe**, Dance **Lisa McDonald** (school lists this sideline squad as "Cheer" per #82). **Unresolved from the source:** Girls Tennis head coach = "TBA"; Competitive Cheer has no separate coach listed (only the Dance/"Cheer" line) — both docs ask the coach to supply/confirm. | Claude ✅ |
| 2026-08-05 | 89 | **`/membership` tier-sheet polish + a mobile horizontal-overflow fix (follows #88).** Erik follow-ups after #88 shipped: (1) **Layout** — sponsorship tiers now render as an intentional **3-then-2**: the three ad-perk tiers (Champion/Gold/Silver) in a top row, then **Tiger Pride + Varsity centered on a row below** (split `MembershipTiers.tsx` `SPONSORSHIP_TIERS` into `AD_PERK_TIERS` + `SPONSOR_TIERS`, two grids `.slotab-tier-grid-three` / `.slotab-tier-grid-pair`). (2) **Perks** — added the "Tiger news & event updates" bullet to **Family** and **Individual** (previously only Tiger Friend had it), so every general-membership card signals the CRM/comms value. (3) **Mailing address** — the How-to-Join ZIP was dangling to its own line; wrapped the address in `.slotab-mail-address` (nowrap on desktop so it breaks before "SLOTAB" as a unit; `white-space:normal` under 480px with the city/state/ZIP nbsp-joined so the ZIP still can't strand). (4) **Mobile overflow bug (was live in #88):** `/membership` had horizontal scroll on phones. Root causes, all fixed: `.slotab-tier-grid-general` was a *fixed* `repeat(3, …)` (couldn't collapse); `.slotab-membership-sheet` + `.slotab-tier-group` were `display:grid` with implicit `auto` columns that let the nested `auto-fit` tier grids blow out to max-content (→ added `grid-template-columns: minmax(0,1fr)`); the `-pair`/`-general` overrides were losing to the later same-specificity base `.slotab-tier-grid` rule (→ compound selectors `.slotab-tier-grid.slotab-tier-grid-pair/-general`); and `margin:0 auto` centering was making those grids size to content instead of stretching (→ center via `justify-content:center` with bounded `minmax(180px,240px)` columns). **Verified** at 375px (no horizontal overflow, all cards stack; `/` home was already clean, confirming the bug was section-local) and at 1280px (3-up ad row; Tiger Pride+Varsity centered; Family/Individual/Tiger Friend centered 3-up). `tsc` + `next build` clean; `/membership` prerenders static. | Claude ✅ |
| 2026-08-05 | 88 | **Official 2026-27 Memberships & Sponsorships sheet shipped to `/membership` — resolves the long-pending wine-meeting/Q7 tier decision (#15, #37).** The Sponsorship lead distributed the finalized printed sheet; Erik asked to implement it. This is the real offering that supersedes **both** draft ladders that had been sitting on `/membership` since May pending Q7: the *Supporter/Fan/Booster/Champion/Patron* prototype (`ProposedTiers.tsx`, the 2026-05-11 board-demo draft) **and** the *Platinum/Gold/Silver/Bronze* business tiers (`BecomeASponsor.tsx`, sourced from the old WordPress form). Both component files **deleted**; replaced by one new `MembershipTiers.tsx` (server-static data, `"use client"` only for the print button) rendering the sheet verbatim — **Sponsorship Tiers** (Champion $10,000, Gold $5,000, Silver $2,500 — all three flagged "Includes Ad Perks"; Tiger Pride $1,000/or $95 mo; Varsity $500/or $45 mo, "available to businesses & individuals") + **General Memberships** (Family $125/or $11 mo; Individual $50/or $5 mo) + a **How to Join** footer (online → `/donate`, mail check to *SLOTAB, PO Box 16025, San Luis Obispo, CA 93406*, plus the preserved board-approved print-sheet button (#34/N4) + Email-Membership-VP CTA). Page section 1 kicker/heading changed *Draft for 2026-05-11 Board Demo / Proposed Membership Tiers* → *2026–2027 / Memberships & Sponsorships* with the sheet's 501(c)(3) blurb. Reused the existing `.slotab-tier-*` card CSS; removed the now-dead `.slotab-become-sponsor-*` block (~90 lines) + its `@media print` force-show rule; added `.slotab-membership-sheet` / `.slotab-tier-group(-head)` / `.slotab-tier-per` / `.slotab-tier-join-*` / `.slotab-tier-print`. Also refreshed two bits of now-stale copy on the same page: the "Joining is donating" businesses paragraph (was "Platinum/Gold/Silver/Bronze" → the new tier names) and renamed the member-recognition section heading *General Memberships* → *Thank You to Our Members* so there aren't two "General Memberships" on one page. **Deliberately NOT touched:** the sponsor **logo wall** (`sponsors.json` / `SponsorWall` / `PlatinumCarousel` / CMS collection) still groups the ~50 real sponsors as Platinum/Gold/Silver/Bronze — re-tiering those to the new names needs the board to reclassify each sponsor (new external input, E-row added). Flagged to Erik: several prices were faint in the source photo (read Gold $5,000 / Silver $2,500 annual; recurring $95/$45/$11/$5 mo) — pending his spot-check against the file. Verified via `npm run dev` on `/membership`: tier cards render, ad-perk badges, general grid centered, print/email/donate affordances present, no console errors, `tsc` clean. **Same-session refinements from Erik** (confirmed the faint prices are correct, agreed on the two "NOT touched" calls): (a) dropped "Video" from Champion's two scoreboard-ad bullets → just "Digital ads" (unsure video will run on the current screens); (b) added a print-deadline caveat to Champion's t-shirt bullet ("submit logo + payment before a trimester starts to make that print run") since a late fall sponsor would miss the fall shirts; (c) **added a new lowest entry tier "Tiger Friend"** ("Any amount · no minimum") as a 3rd General-Memberships card — its job is frictionless CRM capture so any donor lands in the system for later upsell ("Tiger Cub" was rejected as too child-like; Erik picked "Tiger Friend" over Supporter/Tiger Fan/Booster). General grid widened 2→3 cols (760px). `MembershipTiers.tsx` `Tier` type gained an optional `anyAmount` flag; `annual` made optional. | Claude ✅ |
| 2026-08-04 | 87 | **CMS editor-onboarding doc refreshed for the 2026-27 board re-send.** Erik wants to re-onboard the whole board to the Decap `/admin` CMS assuming nobody remembers their login and many never finished credential setup. Rather than write a new doc, refreshed the existing `docs/editor-onboarding.md` (kept Erik's voice + "— Erik" sign-off) and fixed two stale bits that would actively confuse a fresh reader: (1) the GitHub-invite email example still named the **old repo** `slo-tab-website` — updated to `SanLuisObispoTAB/website` (renamed 2026-05 per config.yml header) with a reassurance note that a renamed project is still the right invite; (2) it listed only the `slo-tab-website.vercel.app/admin/` URL, but `api/decap/auth/route.ts` + `origin-allowlist.ts` show board members are expected to reach `/admin` via the SLOHS-firewall-friendly CNAME alias `slotab.ravens-peak-consulting.com` — added that as the school-wifi fallback (both origins are allowlisted; OAuth still round-trips through vercel.app and postMessages the token back to whichever allowlisted origin loaded /admin), plus a forward note that the URL becomes `slotab.org/admin/` after the #67 cutover. Also added a top-of-doc split between **returning editors** (new "Just signing back in" two-click path) and **brand-new/unfinished** editors, and a "never got an invite → send me your email + GitHub username" prompt (feeds E15, the 2–3 board editor GitHub usernames still pending). No code/auth change — doc only; the auth flow itself is unchanged. | Claude ✅ |
| 2026-08-04 | 86 | **New-school-year (2026-27) rollover kickoff — mechanical season-label rolls + handoff doc for the incoming President.** With Fall 2026-27 now in-season (site still pre-launch/`noindex`), rolled the pure "current-season" display labels: `TeamPage.tsx` "Meet the Team" heading `{name} — 2025-26` → `2026-27` (shows on every team page), `membership/page.tsx` both section kickers `2025–2026` → `2026–2027` (Business Sponsors + General Memberships), and `sponsors.json` `"season"` → `"2026-2027"`. **Deliberately did NOT roll the board year** (`board.json` `year` + the three hardcoded `2025-26` strings in `board/page.tsx` and `board/[role]/page.tsx`): those sit above the named officer roster and leadership is changing hands in this handoff — bumping the year while last year's officers are still listed would assert a false roster. That bump is now the incoming President's task #1 (single CMS edit alongside the new roster). Footer `© 2026` left as-is (correct calendar-year copyright until Jan 2027). Verified all three rolls render on `/membership` and `/teams/football` ("Varsity Football — 2026-27"). Also produced `docs/new-school-year-2026-27-handoff.md` — a full checklist of every placeholder + data-refresh item (rosters, liaisons, coach bios, wishlists, HOF tickets/donate/committee, Impact ledger, sponsor URLs, events, board roster) for the President to work with her team. Pre-existing duplicate-key console warning on team pages (two "Liaison TBD" placeholders share a key) noted; resolves when real liaison names land. | Claude ✅ |
| 2026-04-07 | — | Build a refactor preview at `ravens-peak-consulting.com/slotab-preview` using Next.js + Vercel. Black/gold visual identity preserved from current WordPress site. | Erik |
| 2026-04-23 | 1 | **Approve migration** from GoDaddy WordPress to Vercel | Board ✅ |
| 2026-04-23 | 2 | **Designate 2–3 board editors** for Decap CMS access | Board ✅ |
| 2026-04-23 | 3 | **GitHub repo owner**: org-like (TBD org name) | Board (org name pending) |
| 2026-04-23 | 4 | **Real Impact ledger data** | Board ✅ — delivered 2026-08-11, shipped in #103 |
| 2026-04-23 | 5 | **HOF info available** — committee + ceremony URLs | Board — to deliver |
| 2026-04-23 | 6 | **NFHS Network**: not included | Board ❌ |
| 2026-04-23 | 7 | **Hudl integration**: awaiting API key | Board 🔴 (see #48) |
| 2026-04-23 | 8 | **Springly integration**: awaiting API key | Board 🔴 (see #48) |
| 2026-04-23 | 9 | **Team page scheduling**: removed for now (lives only on `/upcoming`) | Board ❌ |
| 2026-04-23 | 10 | **Standard Comms Kit**: evolve to not include scheduling info; kit links to central schedule | Board — pending |
| 2026-04-23 | 11 | **Liaisons sourced from the kit** but **shown on the public team page** | Board ✅ |
| 2026-04-23 | 12 | **Photos**: forthcoming | Board — to deliver |
| 2026-04-23 | 13 | **Standard Kit defaults**: pending #10 | Board — pending |
| 2026-04-23 | 14 | **Drop `noindex`** at slotab.org cutover | Board ✅ |
| 2026-04-23 | 15 | **Membership tier model**: considering "donation amount → tier" auto-assignment | Board — in discussion |
| 2026-04-23 | 16 | **No comms on SLOTAB website**: Standard Kit is internal-only | Board ✅ |
| 2026-04-23 | 17 | **Tiger News Network (TNN)**: removed from website; Hudl is sole video source | Board ❌ |
| 2026-04-23 | 18 | **Home page**: Donate-first prominent CTA + smaller Volunteer/Sponsor row | Board ✅ |
| 2026-04-23 | 19 | **Recurring donations**: monthly only | Board ✅ |
| 2026-04-23 | 20 | **75/25 split** (team/general): show only at point of donation | Board ✅ |
| 2026-04-23 | 21 | **Goal model**: per-team goals + per-trimester campaign goal (overall) | Board ✅ |
| 2026-04-23 | 22 | **ParentSquare URL**: `https://www.parentsquare.com/schools/1903/` | Board ✅ |
| 2026-04-23 | 23 | **GitHub repo**: <https://github.com/eramberg/slo-tab-website> (personal account; can transfer to org later) | Erik ✅ |
| 2026-04-23 | 24 | **Vercel project** created and connected to the new repo. Stable URL: <https://slo-tab-website.vercel.app> | Erik ✅ |
| 2026-04-23 | 25 | **GitHub OAuth App + Decap CMS** wired up; admin at /admin works end-to-end | Erik ✅ |
| 2026-05-06 | 26 | **Watch tab**: add "Sponsored by SLOTAB" label on Huddle embeds | Board ✅ |
| 2026-05-06 | 27 | **Homepage CTAs**: Donate is primary; Member + Volunteer collapse into a single secondary button. Top-right nav "Join" pill swaps to "Donate" | Board ✅ |
| 2026-05-06 | 28 | **Donate CTA on team pages**: each team page surfaces both general SLOTAB and sport-specific donate options | Board ✅ |
| 2026-05-06 | 29 | **Donation ladder**: one-time floor **$25** · ladder $25 / $50 / $100 / $200 / $500 / $1000 / $5000 · "Other" custom amount, no cap | Board ✅ |
| 2026-05-06 | 30 | **Recurring donations**: monthly checkbox alongside one-time. **Recurring floor lowered to $10** (one-time stays $25) — recurring at low monthly amounts compounds to meaningful annual value. Backend re-emits each recurring as a fresh monthly one-time entry so Trina's QB workflow doesn't change | Board ✅ |
| 2026-05-06 | 31 | **Square transaction-ID strategy**: each transaction carries a code that encodes sport/team designation, so QB reconciliation report shows customer name/email · sport · amount | Board ✅ (Erik to research mechanics) |
| 2026-05-06 | 32 | **Impact page — simplified first cut**: ONE general SLOTAB thermometer (anchor example: ~$75K for Huddle + T-shirts + senior banners). Per-team thermometers come later once transaction codes land. Optional drill by fundraising drive (fall/winter/spring) | Board ✅ |
| 2026-05-06 | 33 | **Impact updates monthly**, posted before each SLOTAB meeting. Disclaimer: 75% of designated donations go to team (matches what coaches see in their accounts) | Board ✅ |
| 2026-05-06 | 34 | **Sponsor wall layout**: "Become a Sponsor" is a compact expandable ABOVE the wall — collapsed by default so logos stay above the fold; expands to show levels + brochure link | Board ✅ |
| 2026-05-06 | 35 | **Sponsor logos clickable** through to each sponsor's URL | Board ✅ |
| 2026-05-06 | 36 | **Platinum sponsor carousel** on homepage — auto-rotate, click-through; tier-based, zero manual work | Board ✅ |
| 2026-05-06 | 37 | **Membership ↔ sponsorship merge** (philosophy): any donation = membership. Naming TBD ("Supporter / Fan / Booster" floated). 4-year auto-renew option floated. Final levels gated on Deneen + Ann wine meeting | Board — pending wine meeting |
| 2026-05-06 | 38 | **Booster Bash ticket sales** go live 2026-07-01 (kept inside next fiscal year) | Board ✅ |
| 2026-05-06 | 39 | **Events workflow**: shared Google Sheet (board + Adam + Phil + liaisons) auto-syncs to site daily. Add a 2026-2027 page; include SLOTAB events (physicals night, fall/spring parent meetings) | Board ✅ |
| 2026-05-06 | 40 | **Team pages are NOT a game-communication channel** — Home Campus is the source of truth for schedules/changes next year. Practice schedules excluded (too volatile). Rosters + wishlists + liaisons + coach contacts are kept | Board ✅ |
| 2026-05-06 | 41 | **Design direction**: Option #1 *classic collegiate* approved (Stanford/Menlo style); keep the SLO\|TAB masthead. Option #2 *magazine* repurposes as the newsletter template. Option #3 *Huddle-forward* dropped | Board ✅ |
| 2026-05-06 | 42 | **Go-live target 2026-08-01** — just before the new school year. Preview at the next casual board cocktail at the Hub | Board ✅ |
| 2026-05-06 | 43 | **The 25% is the SLOTAB general fund, not "overhead"** — funds shared-benefit items (Huddle, senior banners, T-shirts, etc.) that all teams use. About-page + Impact-page copy must reflect this, since coaches who say "we get nothing from SLOTAB" are overlooking what the general fund pays for on their behalf | Erik ✅ |
| 2026-05-06 | 44 | **Coach onboarding 1-pager** — written deliverable explaining where SLOTAB dollars come from, how the 75/25 split works, what the general fund covers, and how to read team-page totals | Erik to draft ✅ |
| 2026-05-06 | 45 | **Year-over-year totals visible on Impact + team pages** — "FY26 raised $X, FY25 raised $Y" so the page is useful even mid-year | Erik ✅ |
| 2026-05-06 | 46 | **Trina-facing operations doc** for the Square→QB workflow — covers data flow, tag schema, refund/correction path, what to do if the sidetool breaks | Erik to draft ✅ |
| 2026-05-06 | 47 | **Open considerations from Phase 0 plan** — sponsorship-bundle splits (e1), in-kind donations (e2), restricted-vs-unrestricted gifts (e4) — Erik to surface at next board meeting before they get coded | Board — pending discussion |
| 2026-05-06 | 48 | **Springly + Hudl integrations TABLED** until access is sorted. **Springly** (Serenity tier): no Integrations tab visible at admin level — Owner role almost certainly required, OR API access may be a paid add-on Serenity doesn't include; pending support reply. **Hudl** (Pro tier): no self-service developer portal — `developer.hudl.com` routes back to the regular admin console. API access is gated through Hudl's Partner Program (broadcast/stat partners), not exposed to individual schools. Erik to email Hudl support 2026-05-07 asking whether Pro includes any API or if a Partner agreement is required for a single-org public-data integration. Plan B options (Zapier for Springly; manual `data/hudl.json` updates via Decap) remain in place. | Erik 🟡 (Hudl portion unblocked 2026-05-13 via BlueFrame embed — see #59; Springly portion still 🔴) |
| 2026-05-06 | 49 | **Membership Join form folded into the Donate flow** (philosophy from #37 made concrete). The standalone `/membership` "Join Online" form is gone; `/donate` now captures donor identity (Name, Email, Phone, Display-on-Wall checkbox). Submit alert shows membership tier enrollment alongside the donation. Once Springly creds land, one combined POST creates the contact record + donation in a single round-trip. | Erik ✅ |
| 2026-05-06 | 50 | **Nav top-right CTA: Donate → Join** (links to `/membership`). Reverses an earlier swap (#27) — having two Donate CTAs in the same fold (nav + hero) was redundant. Hero keeps the primary "Donate Now" button | Erik ✅ |
| 2026-05-07 | 51 | **Custom domain `slotab.ravens-peak-consulting.com`** added (CNAME → Vercel) so the SLOHS district firewall (which blocks `*.vercel.app`) can reach the preview. Aliased to the `slo-tab-website` Vercel project's production deployment, no code changes needed. Domain pulls Let's Encrypt cert automatically. The old `ravens-peak-consulting.com/slotab-preview` mirror path is now obsolete — this aliases the live build instead of mirroring it into the Raven's Peak repo | Erik ✅ |
| 2026-05-07 | 52 | **Mobile responsive pass**. The 2026-05-06 rebuild was tuned for desktop and read poorly on phones. Added two breakpoint tiers (480px phone + 720px tablet) covering masthead wordmark, hero (edge-to-edge photo, drop the desktop side mask), stats grid (stays 2×2), sponsor wall (force 2-up across all tiers), watch feature (smaller, stronger blackout, title text-shadow), and impact card (full-width, shorter photo). Required re-asserting `.tiger-scope` (0,2,0) overrides for headlines and watch-feature title to beat unscoped late rules in tiger.css | Erik ✅ |
| 2026-05-07 | 53 | **Team photo overhaul** — Erik provided sport-specific photos for nearly every team and adopted a new filename convention: first letter `b`/`g`/`c` (boys/girls/co-ed), then a short sport name (`fball`, `bball`, `vball`, `bvball`, `sball`, `golf`, `soccer`, `wrestling`, `XC`, `track`, `cheering`, `stunt`, `swim`, `lacrosse`, `fieldhockey`). `bball` = basketball, so `bbaseball` is the boys-baseball prefix to avoid collision. PHOTO_BY_SLUG in TeamsCarousel + the four active team-page heroPhotos (football, girls-volleyball, baseball, track-field) + ClassicHero + HeroCarousel + hudl.json + impact.json all migrated to convention names | Erik ✅ |
| 2026-05-07 | 54 | **Originals subdirectory** — `public/photos/originals/` now holds 25 old non-conformant files (date-coded `081xxx`, generic `tennis.jpg`/`volleyball.jpg`/`water-polo-*.jpg`, etc.). Convention-named copies live alongside the new photos in `/photos/`. All in-code references migrated to the new names; the originals are kept for archival/fallback only | Erik ✅ |
| 2026-05-07 | 55 | **`ctrack-2.jpg` (track runner #14 leading) added as a 6th slide on the homepage hero carousel** alongside football×2, water polo, student section, and basketball — gives spring-season balance | Erik ✅ |
| 2026-05-13 | 56 | **Sports list aligned with the official SLOHS athletics site.** Boys Lacrosse + Field Hockey removed (club sports, not on `slohs.slcusd.org/athletics`). Girls Water Polo moved Winter → Fall (Central Section runs both in fall). Girls Wrestling added (Winter); slug `wrestling` renamed to `boys-wrestling` for parity. Cheer split into Fall sideline `cheer` and Winter `competitive-cheer`. Stat-bar count 27 → 26 → 27 (Cheer split offsets removals + Girls Wrestling). Two orphan stock-photo placeholders (`blacrosse-…`, `gfieldhockey-…`) purged. | Erik ✅ |
| 2026-05-13 | 57 | **Shop promoted to top-level nav** between Watch and Hall of Fame (was buried in the Get Involved dropdown). **Top-right Join CTA pill retired** — Donate/Sponsor/Volunteer hero + Get Involved dropdown cover that intent without crowding the nav (reverses #50). **"Wear Your Stripes" featured-gear strip** added to the home page between Teams carousel and Calendar+Watch — 3 spring shirts on cream cards w/ hover lift and "Shop all designs →" linkout. Best-practice rationale: top-nav item for discoverability + home strip for browse-bait; no second pill that would dilute Donate. | Erik ✅ |
| 2026-05-13 | 58 | **Photo audit + 2025-26 team-photo refresh.** Cross-referenced all team slugs vs. the new official zip. Carousel slots updated with 2025-26 SLOHS portraits where the old photo was wrong/shared/placeholder: boys-tennis (CIF 2026 D2 Champions, replaces student-section placeholder), girls-tennis (replaces shot w/ Clovis player in frame), boys + girls cross-country (replaces shared co-ed group shot), boys + girls wrestling (replaces shared dark dramatic shot), competitive-cheer (own portrait, no longer shares fall cheer). Three orphans purged: `cXC-image-1200x900`, `bwrestling-checukk`, `gtennis.jpg`. **Action shots stay on the home carousel; formal team portraits live on individual team pages** via a new "Meet the Team" section. 13 additional 2025-26 team portraits staged in `public/photos/` for the 23 team pages not yet built — just set `"teamPhoto"` in the team JSON to light up. Decap `teamPhoto` field added. | Erik ✅ |
| 2026-05-13 | 59 | **Hudl integration partially unblocks #48 via Hudl Support-built BlueFrame web-component embed.** Path narrative: probed `fan.hudl.com` (no iframe — X-Frame-Options SAMEORIGIN) → confirmed vCloud per-broadcast iframes (`vcloud.hudl.com/broadcast/embed/<id>`) work and are CSP-clean → built a custom catalog + Decap form + per-game pages around real SLOHS broadcast 4031110 (boys volleyball, archived) → Hudl Support then handed us an official `<blueframe-app>` web-component pre-configured to vCloud site 6609 with theme colors already in SLOTAB gold. The portal auto-renders live + upcoming + archived rows with search and section filters. Custom catalog (broadcasts.json, BroadcastGrid/Card/Embed, `/watch/[broadcastId]` per-game pages, Decap "Watch — Broadcast Catalog" collection) decommissioned. `/watch` is now just an intro strip + the BlueFrame embed wrapped in a dark "broadcast surface" section. Editor workflow per game: zero on the SLOTAB side. **Open**: portal currently shows 4 archived broadcasts in an error state (Hudl Support working it) + omits recent broadcasts that are "Available" but not yet "Archived" status — pending the publish-to-viewer workflow answer from support. | Erik ✅ |
| 2026-05-13 | 60 | **Public vs No-Scout default policy for SLOHS broadcasts** drafted. **Public by default (12 teams)** — boys/girls cross country, boys/girls swim & dive, boys/girls golf, boys/girls tennis, track-field, sideline cheer, competitive cheer, stunt: outcomes don't change based on opponent video study (measurable individual performance or judged routines). **No-Scout by default (13 teams)** — football, boys/girls/beach volleyball, boys/girls basketball, boys/girls soccer, boys/girls water polo, baseball, softball, flag football: set plays + tendencies are legit scout targets. **Borderline (2 teams)** — boys/girls wrestling default private but easy to flip per match. Per-broadcast override always available; playoffs may lock normally-public sports; Title IX symmetry preserved. Awaiting AD/coach validation. | Erik (drafted) · Adam/Phil to confirm |
| 2026-05-13 | 61 | **Footer ligature fix.** Manrope ships typographic substitutions `(c) → ©`, `(r) → ®`, `(tm) → ™` in its `liga` (standard ligatures) feature — unusual placement, but verified by diff-test (only `liga 0` stops it; calt/clig/dlig alone don't). Six places on the site were rendering "501(c)(3)" as "501©(3)" (footer, stats bar, donate form, about page, DonateForm disclaimer, tiger-footer). Fixed at `.tiger-scope` body level via `font-feature-settings: "liga" 0, "clig" 0, "calt" 0, "dlig" 0`. Negligible visual cost on Manrope's subtle letter ligatures; IRS designation rendering correctly is non-negotiable. | Erik ✅ |
| 2026-05-14 | 62 | **Doc maintenance handoff.** Claude (the agent doing the implementation work) is now responsible for keeping `docs/project-status.md` current — append a decisions-log row after each shipped chunk, move active-backlog items to Built, refresh External Inputs Pending, then commit alongside the work. Anchored in a new `CLAUDE.md` at repo root so future Claude sessions inherit the instruction. | Erik ✅ |
| 2026-05-14 | 63 | **Springly import research + canonical CSV shape.** Public-marketing-side investigation done — Springly's Serenity tier supports Excel/spreadsheet import with field auto-detection, but the actual import wizard fields are admin-only and require Owner access (still gated by #48). New `docs/springly-import.md` captures what's known, a canonical 2-CSV column shape (`members`, `sponsors`) that maps cleanly to Springly's contacts model AND to the existing `/api/springly/*` route stubs AND to a fallback Zapier path. Two sample CSVs at `docs/springly/test-members.csv` (12 fake households/individuals across all 5 tiers + Coach + lapsed) and `docs/springly/test-sponsors.csv` (8 fake businesses across all 4 sponsor tiers + in-kind + lapsed) — dry-run targets once Owner access lands. Real source spreadsheets from Trina + sponsorship lead still pending. | Erik (research) ✅ |
| 2026-05-14 | 64 | **Board Hub at `/board`** — password-gated organizational-memory section for handoff between board generations. Architecture: Next.js Edge middleware (`src/middleware.ts`, since renamed to `src/proxy.ts` per Next.js 16 — see #65) gates `/board/*` against a `BOARD_PASSWORD` env var, sets a signed cookie (HMAC-SHA256 over expiry, keyed by the password itself — so rotating the password at annual handover automatically kills old sessions). Content lives in `src/app/data/board-handoff.json` as structured per-role exit notes; outgoing officers fill 9 prompts (what worked / what broke / who to call / vendor relationships / where logins LIVE / calendar landmarks / open TODOs / anything else) via a new "Board Handoff Notes" Decap collection. Hub at `/board` lists the 13 roles from the current roster; per-role page at `/board/[role]` shows all years of notes for that role, newest first. Login at `/board/login`; sign-out via `/api/board/logout`. **Hard rule baked into the Decap form hints + the hub banner: never paste passwords or secrets into these fields — the repo is public, only the rendered page is gated. Describe where secrets live (vault pointers) instead.** v1 keeps scope lean on exit-notes only; "key contacts" + "annual calendar" + "process recipes" sections were explicitly deferred. | Erik ✅ |
| 2026-05-14 | 65 | **Decap OAuth host-aware postMessage** (debugging chain for the Board Hub rollout). First Vercel build of #64 failed two ways: (a) `useSearchParams()` in `/board/login` needed a `<Suspense>` boundary even with `dynamic="force-dynamic"` and (b) Next.js 16 deprecated the `middleware.ts` file convention in favor of `proxy.ts` (renamed file + renamed exported function `middleware` → `proxy`; `config.matcher` unchanged). Build then passed but board members hit a second bug: clicking "Login with GitHub" in /admin hung forever. Initial diagnosis (hash deep-link `/admin/#/collections/.../entries/...` racing with Decap's OAuth init) was wrong — the real root cause was that `config.yml` hardcodes `base_url: https://slo-tab-website.vercel.app`, so the Decap OAuth callback always tries to postMessage the token back to the parent admin window using vercel.app as `targetOrigin`. But board members hit `/admin` via the SLOHS-firewall-friendly CNAME alias `slotab.ravens-peak-consulting.com` (#51), so the browser silently drops the postMessage and login hangs. Fix: `/api/decap/auth` now reads the parent origin from the Referer header at the start of the OAuth flow, validates it against a server-side allowlist in `src/app/api/decap/origin-allowlist.ts` (vercel.app, ravens-peak alias, slotab.org future, localhost), persists it through the GitHub round-trip via an httpOnly cookie, and `/api/decap/callback` uses it as the postMessage `targetOrigin`. GitHub OAuth App registration (single redirect_uri at vercel.app) is unchanged — only the in-callback postMessage is host-aware now. Already includes `slotab.org` so it'll keep working through the domain cutover. Also kept the new-tab CTA UX from the misdiagnosis (preserves /board reading context while writing a handoff). | Erik ✅ |
| 2026-05-25 | 66 | **Photo library — 24 additional curated game photos** layered on top of the existing convention-renamed library (#53/#58). Filtered from a 180-photo raw dump that arrived as two folders (`SLOHS Sports Photographs-part-{1,2}/`, 1.7 GB combined). Originals moved into `public/photos/originals/SLOHS Sports Photographs-part-*/` to match the originals-dir pattern from #54, then those specific subdirs were added to `.gitignore` so the 1.7 GB doesn't track or deploy (the existing 25 archival files in `originals/` stay committed). Curation prioritized photographer-flagged variants (`copy`/`5x7`/`8x10`/letter suffixes are print versions the photographer prepared, so reliable favorites). Rejected several with baked-in text overlays ("How About Them Tigers?", "Not in Our House", "Tiger Nation"). Sport breakdown: boys basketball ×6, football ×5 (incl. `tigers-mascot.jpg` studio portrait), boys volleyball ×2, tennis ×3 (b+g), girls soccer ×3, boys soccer ×3, plus `ccheer-team.jpg` and `gwpolo-coach-huddle.jpg` (kept distinct from origin's already-shipped `gwpolo-huddle.jpg`). Carousel (`ClassicHero`) gains slides 7 + 8 — boys basketball (`bbball-shot-394.jpg`) and girls soccer (`gsoccer-huddle.jpg`); both sports were previously absent from the hero rotation despite the broader photo refresh. Also added optional `heroPhotoPosition` to the Team type so per-team hero crops can anchor at "center top" when the source photo's faces sit above its natural center; applied to `girls-volleyball` and `baseball` where the default center crop was chopping faces and landing on chests. | Erik ✅ |
| 2026-05-26 | 67 | **GitHub repo renamed; Vercel deploy in limbo pending pricing question.** GitHub repo transferred from `eramberg/slo-tab-website` (Erik's personal account) to `SanLuisObispoTAB/website` (the SLOTAB org account). Old URL still works via GitHub redirect — `git push` to `eramberg/slo-tab-website.git` returns a `This repository moved` notice and accepts the push, so commits made today (`63a4a77`, `9bac97a`, `f74d101`, `d6a75c6`) landed cleanly on `SanLuisObispoTAB/website`. Updated Decap config `repo:` to the new path explicitly. **The blocker is on Vercel**: a new Vercel project under the SLOTAB org account was created in parallel, but Vercel flagged it for the team/business pricing tier rather than honoring non-profit eligibility, and their sales team hasn't followed up. Until that's resolved, the working deploy target is a new Vercel project under the **Raven's Peak Vercel team** (which Erik already pays for and trusts) connected to `SanLuisObispoTAB/website`. The `slotab.ravens-peak-consulting.com` subdomain (set up in #51 as a CNAME alias to the personal `slo-tab-website` Vercel project) moves to the new Raven's Peak-team project. Note this means a temporary architectural inversion from #51's "production-track Vercel + CNAME for firewall" framing — Raven's Peak-team Vercel is now the primary deploy target, not an alias. **Vercel-side checklist for Erik**: (1) create new project under Raven's Peak team, connect to `SanLuisObispoTAB/website` repo, branch `main`; (2) copy env vars from old project (`BOARD_PASSWORD`, `DECAP_GITHUB_CLIENT_ID`, `DECAP_GITHUB_CLIENT_SECRET`, plus any `SPRINGLY_*` if set); (3) move the `slotab.ravens-peak-consulting.com` custom domain to the new project; (4) update GitHub OAuth App callback URL to the new `*.vercel.app` URL (and `slotab.ravens-peak-consulting.com` already in the postMessage allowlist per #65 keeps working); (5) tell Claude the new `*.vercel.app` URL so `src/app/api/decap/origin-allowlist.ts` + `public/admin/config.yml` `base_url` can be updated; (6) keep chasing Vercel sales — eventual SLOTAB-org project is still the right end state. **What's still hardcoded that we know about**: `slo-tab-website.vercel.app` appears in `src/app/api/decap/origin-allowlist.ts`, `public/admin/config.yml` (`base_url`), and a handful of `docs/*.md` files; do a coordinated find-and-replace once the new URL exists. The Decap auth/callback routes themselves read URLs from the request, not from hardcoded strings, so they need no code changes. | Erik 🟡 (Vercel sales follow-up pending; see also #68 for the practical unblock) |
| 2026-05-26 | 68 | **GitHub repo flipped to public** (`SanLuisObispoTAB/website`). Trying to wire the renamed repo to a fresh Vercel project hit a second wall after the SLOTAB-org pricing question from #67 — Vercel Hobby tier refuses to deploy private GitHub *organization* repos at all (forces Pro: $20/seat/month). But the existing security model already assumes the repo is public — decision #64 explicitly: "the repo is public, only the rendered page is gated; describe where secrets live, not the secrets themselves." So the working assumption was already there; flipping visibility just made it real. Sanity-checked before flipping: `.env*` properly gitignored, no `.env` tracked, no token/secret/password literals in tracked code, git history scan since 2026-03 found no past leaks, board-handoff.json is template content only. Coach/board emails in `data/teams/*.json` and `data/board.json` are already on the public site, so no net new exposure. Net effect: unlocks free Vercel hosting on any account (no Pro needed, no sales follow-up needed), aligns the implementation with the documented assumption from #64, and lets the deploy proceed under the Raven's Peak Vercel team without billing changes. SLOTAB-org Vercel account remains the eventual end state — flipping back to a private repo there if/when the non-profit pricing question resolves is a trivial Vercel-side change with no code impact. | Erik ✅ |
| 2026-05-26 | 69 | **Vercel git-link rescue.** Even after #68 made the repo public, pushes to `SanLuisObispoTAB/website` still weren't triggering auto-deploys. Diagnosed via `vercel git disconnect`: the `slo-tab-website` Vercel project was linked to `eramberg/ravens-peak-consulting` (literally the wrong GitHub repo — likely a misclick during an earlier "reconnect in UI" attempt). Pushes to slo-tab-website's actual code were therefore being ignored by Vercel forever; pushes to `ravens-peak-consulting` would have triggered slo-tab-website rebuilds. Fixed via `vercel git connect https://github.com/SanLuisObispoTAB/website`. While diagnosing, found that `vercel --prod` from the repo root uploaded 3.5 GB and hit Vercel's 100 MB per-file limit because no `.vercelignore` existed — Vercel CLI doesn't fully honor nested `.gitignore` patterns. Added `.vercelignore` (defensive overlap with `.gitignore` plus root-level exclusions for `Team Photos/`, the `public/photos/originals/SLOHS Sports Photographs-part-*` raw dumps, and `.claude/`). Used `vercel --prod --yes` as a one-shot to get the photo + crop work live while the git-link was being unwound; that worked but is not the steady-state path — auto-deploy on push is now restored. | Erik ✅ |
| 2026-05-28 | 70 | **Photo library — 13 additional 2025-26 photos staged + beach-VB carousel upgrade.** Erik delivered a new drop covering sports the existing library was thin on. Convention-renamed and resized via the standard `magick … -resize 1200x\> -quality 82 -strip …` pass: action shots `bbball-amaya.jpg` (boys basketball portrait + teammates), `gfball-cushing.jpg` (B&W flag-football sideline), `gsball-london.jpg` (softball pitcher mid-windup), `gtennis-killenberger.jpg` (tennis racket huddle), `gswim-stroke.jpg` (lap-pool stroke), `gbvball-serve-swanson.jpg` (sunset serve at Cal Poly's Swanson Beach Volleyball Complex — dramatic and on-location), `bwrestling-action.jpg` (ride/control position), `ccheering-sideline-2025.jpg` (Tigers sideline-cheer lineup against dusk sky, Sept 2025 football game); team portraits `ccompcheer-buchanan-2025.jpg` (4th-place team photo from the 12-7 Buchanan tournament, fall colors backdrop), `cstunt-team-2026.jpg` (formal Feb 2026 Stunt team-portrait day); volleyball staging trio `gvball-frosh-team-2025.jpg` (labeled) + `gvball-frosh-team-2025-unlabeled.jpg` (clean for hero crops) + `gvball-jv-team-2025-unlabeled.jpg` (clean version of the existing labeled JV — JV stays the JV-not-Varsity per the `_teamPhoto_note` on `girls-volleyball.json`; we still don't have a Varsity portrait). Two exact dupes dropped at the door: a higher-res copy of `bXC-team-2025.jpg` (identical pixels, just unscaled), and `SLOHS 2025 VARSITY VOLLEY TEAM.jpg` (filename misleads — overlay label and pixel content match the existing JV photo). **One carousel swap shipped**: `beach-volleyball` slot in `TeamsCarousel.tsx` (and the home-page "Live & on-demand" Watch feature card in `page.tsx`) moved from `gbvball-ruby-1200x675.png` to the new Swanson serve photo. The old PNG was a 300×168 placeholder thumbnail (not 1200px wide), so the swap was a clear upgrade rather than a curation re-judgement of #58/#66. **Carousel left otherwise untouched** — the other staged photos sit in `public/photos/` for future team pages (the #58 pattern: just set `teamPhoto` in the team JSON to light them up). The orphaned `gbvball-ruby-1200x675.png` was left in `public/photos/` (kept around per the #54 originals-not-deleted norm; can be moved to `public/photos/originals/` in a future cleanup pass). Verified live via `npm run dev` — both carousel and Watch feature render the new image correctly. | Claude ✅ |
| 2026-05-28 | 71 | **`/teams` index — photo-bloom on season focus.** The full sport-index page at `/teams` used to render text-only cards (sport name + gender + CTA) under per-season section headers. Reworked it so each card now has a photo strip that's collapsed to height: 0 by default and expands to 170px when its season section becomes the most-visible one in the viewport (IntersectionObserver-driven `.is-focused` class, with a 0.15 visibility floor so a sliver of the next section doesn't yank focus prematurely). The bloom staggers across the row (30ms per card via `--card-index` custom property) so it feels like a wave of photos rolling in. Per-card hover/focus also expands a single card's photo, so the page is still exploreable without scrolling. `prefers-reduced-motion` gets a static 120px photo with no transition. "Coming soon" cards get a grayscale+darken filter on their photo so live programs (the ones with team pages) read first. Architecture: split the formerly server-only `src/app/teams/page.tsx` into a thin server shell that keeps `export const metadata` + a new client component `src/app/components/TeamsIndexList.tsx` that owns the observer + the focused-season state. The slug→photo map (formerly inline in `TeamsCarousel.tsx`) moved to `src/app/data/team-photos.ts` so both the home carousel and the new index pull from the same source. Reuses the existing curated action-shot library (#53/#58/#66/#70) — no new photo curation, just a new surface. | Claude ✅ |
| 2026-05-28 | 72 | **Photo-slot audit — bias toward action shots reaffirmed.** Erik flagged that #71's `/teams` bloom shipped with several slots still pointing at formal team portraits when better action shots existed in the curated library. Walked `TEAM_PHOTO_BY_SLUG` in `src/app/data/team-photos.ts` (the shared source for the home carousel + the new index) and swapped 8 portraits → action: `baseball` (team portrait → `bbaseball-baileyhartford.jpg`), `softball` (team group → `gsball-london.jpg` pitcher mid-windup), `boys-basketball` (post-game crowd celebration → `bbball-shot-394.jpg` mid-shot), `boys-soccer` (huddle → `bsoccer-keeper-save.jpg` scrum in the box), `girls-soccer` (team-with-trophy portrait → `gsoccer-celebration.jpg`), `girls-tennis` (formal team portrait → `gtennis-killenberger.jpg` rackets-up huddle), `girls-swim-dive` (poolside team portrait → `gswim-stroke.jpg` overhead lap-pool action), `boys-wrestling` (team portrait → `bwrestling-action.jpg` ride/control). Left unchanged where the action option doesn't exist in the library yet: `boys/girls-cross-country`, `boys/girls-golf`, `girls-wrestling`. The portraits themselves stay reserved for each team's own landing page via `teamPhoto` on `src/app/data/teams/<slug>.json` (the #58 division of labor). Convention recorded in memory so it sticks across sessions. | Claude ✅ |
| 2026-06-16 | 85 | **Action-shot rule re-enforced on the `/teams` index + home carousel (extends #72).** Erik flagged that some index slots still showed formal/posed team shots even though action shots exist. Audited all 27 `TEAM_PHOTO_BY_SLUG` entries (contact sheet) and swapped 5 posed → action: `boys-volleyball` (team+parents group → `bvball-spike`), `boys-tennis` (2026 champions banner → `btennis-serve`), `girls-basketball` (team+mascot group → `gbball-hartford`), `girls-volleyball` (posed-with-plaque → `gvball-slogv-2022`), `dance` (sideline lineup → `ccheering-sideline-2025`, poms-up). Also fixed the `girls-basketball` **page** hero (I'd set it to the posed `gbball-goodwin` in #81 → now `gbball-hartford`; dropped goodwin from its gallery). Remaining posed entries are the documented exception — no action shot exists yet: `boys/girls-cross-country`, `girls-wrestling`, `competitive-cheer`. Codified the rule as a strong note in `team-photos.ts` header. Verified all five swaps render on `/teams`. Build clean. | Claude ✅ |
| 2026-06-16 | 84 | **Competitive Cheer set to Girls.** AD confirmed it's a girls-only team (the one gender left unstated in #83). `gender` `Co-ed` → `Girls` in `teams.json` + `competitive-cheer.json`. All three SLOHS spirit squads (Dance, Competitive Cheer, STUNT) are now girls-only. | Claude ✅ |
| 2026-06-16 | 83 | **Spirit-squad structure corrected per the AD (resolves E16, finalizes #82).** AD confirmed: **Fall** has **Girls Dance** (sideline, the squad #82 renamed from "Cheer") **and Competitive Cheer** (new this past year); **Winter** has no squad of this type; **Spring** is **STUNT, girls-only** (all-caps is the official styling). Applied: `competitive-cheer` season `Winter` → `Fall` (`teams.json` + `competitive-cheer.json`); `stunt` → name/sport **"STUNT"**, gender `Co-ed` → `Girls`, title "STUNT — SLOTAB" (Spring unchanged). Dance was already Fall/Girls from #82 — no change. **Residual:** the AD named Competitive Cheer without a gender, so it stays `Co-ed` for now — confirm if it should be girls. Verified: Fall nav + `/teams` index now list Dance + Competitive Cheer, Winter has no spirit team, Spring shows STUNT. Build clean. | Claude ✅ |
| 2026-06-16 | 82 | **"Cheer" team relabeled "Dance" (Girls); non-team fan photo removed; intake script taught `dance`.** Erik clarified the SLOHS spirit squad's official name is **Dance** and it's a **girls** team (not co-ed), and that the team I'd called "Cheer" is the same squad — so renamed it: `teams.json` slug `cheer` → `dance`, name "Dance", gender Girls (Fall unchanged); data file + route `cheer.json`/`teams/cheer/` → `dance.json`/`teams/dance/` (URL now `/teams/dance`); `TEAM_PHOTO_BY_SLUG` key `cheer` → `dance`. Competitive Cheer + Stunt unchanged. The new official portrait (Erik dropped `SLOHigh_DanceTeam_2025-2026_…jpg` into `photo-inbox/`, processed via `npm run photo-intake` to `gdance-team-2025.jpg`, 1200×800) is the Dance `teamPhoto`; sideline action shots (`ccheering-taryn` hero, `ccheering-sideline-2025` gallery) stay. **Removed `ccheering-2022.jpg` (body-painted student fans, not team members) from the Dance gallery** per Erik — it's kept in the library and still used on the home `HeroCarousel` ("elsewhere on the site" is fine). Added a `dance` keyword (+ girls default) to `scripts/photo-intake.mjs` so it auto-suggests next time (extends #78). The old `ccheer-team-2025.jpg` "Varsity Cheer" portrait stays in the library, now unreferenced. Build clean; `/teams/dance` prerenders. | Claude ✅ |
| 2026-06-16 | 81 | **16 new team pages built — every team with a posed team photo now has one.** Acting on the rule "a team photo means a team page; show its action shots too": added `src/app/data/teams/<slug>.json` + `src/app/teams/<slug>/page.tsx` and flipped `hasPage: true` for girls-tennis, boys/girls water polo, boys/girls basketball, boys/girls soccer, boys/girls cross-country, girls swim & dive, girls golf, boys/girls wrestling, cheer, competitive-cheer, stunt (21 team pages total now). Each uses the action-vs-portrait split (#58/#72): best landscape action shot = `heroPhoto`, posed portrait = `teamPhoto` ("Meet the Team"), remaining action shots = `gallery` (#80). XC + girls-wrestling have no action shot, so the portrait is the hero (anchored `center 25%`) and they omit the gallery. Generated via a one-shot script (since removed); roster/coach/wishlist omitted (self-hide) and one `"Liaison TBD"` placeholder each, pending real content. The 6 action-only teams with no posed portrait (boys volleyball, beach volleyball, softball, boys tennis, boys swim, boys golf) stay page-less until a team photo arrives. `npm run build` clean — all 21 routes prerender static. | Claude ✅ |
| 2026-06-16 | 80 | **Photo gallery added to team pages** so multiple action shots per team are showcased ("use as many photos as possible to celebrate the teams"). New optional `gallery?: string[]` on the `Team` type renders an "In Action" responsive grid (`.slotab-team-gallery` in `slotab.css`, reusing the white-frame look) below the "Meet the Team" portrait, lazy-loaded, natural aspect ratios preserved so landscape + portrait shots mix without cropping. Decap: added a `gallery` list field to the one existing team collection (`teams_football`). | Claude ✅ |
| 2026-06-16 | 79 | **Teams nav dropdown made dynamic + season-filtered; `/teams` index reordered to lead with the active season.** The nav Teams dropdown was a hardcoded 4-team list (in the live `TigerNav.tsx`; `Header.tsx` is unused legacy) — which is why the flag-football page (#74) never appeared in the menu. It now builds from `teams.json`, filtered to `hasPage && (season === navSeason() || Year-round)`, sorted by name/gender, "All Teams" first — so any future page appears automatically with **no manual menu edit**. Extracted a shared `src/app/data/seasons.ts` with `currentSeason()` (moved verbatim from `TeamsCarousel`, which now imports it — no behavior change), `navSeason()` (= current season, except summer Jun–Jul previews the upcoming **Fall**), and `orderedSeasons()` (the three seasons rotated to lead with `navSeason()`, Year-round last). `/teams` index `groupBySeason()` now uses `orderedSeasons()` so the current/upcoming season leads (June→Fall,Winter,Spring unchanged; winter break→Winter,Spring,Fall; spring→Spring,Fall,Winter). Index order is computed server-side at build (refreshes on the next deploy). | Claude ✅ |
| 2026-06-16 | 78 | **Interactive `--rename` added to the photo-intake helper (#77).** For each misnamed, non-duplicate drop, `npm run photo-intake -- --rename` infers the sport token + boys/girls/co-ed prefix from the original filename (keyword rules covering every CIF sport + the co-ed/fixed-prefix cases like track/cheer/stunt → `c`, beach VB/softball/flag/field-hockey → their established prefixes), proposes a `<b|g|c><sport>-<descriptor>` name, and lets the editor accept (Enter), override (type one), or skip (`s`); the typed name is re-validated against the convention and checked for in-inbox collisions before the file is renamed in place. Input is read via the readline async-iterator (not `rl.question`, which only resolves once against piped stdin) so it works both interactively and under `printf … | npm run …`; the `magick` child calls now pin `stdio` stdin to "ignore" so they can't swallow the editor's keystrokes. Tested: sport+gender inference (boys water polo → `bwpolo-`, girls wrestling → `gwrestling-`), Enter-accept, override, an undetectable name prompting for a full entry, and a duplicate correctly excluded from the rename pass. `eslint` clean. README + CLAUDE.md updated. | Claude ✅ |
| 2026-06-16 | 77 | **Photo intake helper added so the staging backlog (#76) can't reaccumulate.** New gitignored `photo-inbox/` drop folder at the repo root (only its `README.md` is tracked, via a `/photo-inbox/*` + `!README.md` `.gitignore` rule) replaces the old habit of dropping raws straight into the served+tracked `public/photos/`. New `scripts/photo-intake.mjs` (+ `npm run photo-intake`) audits each drop — content-hash dup-check against the whole tracked library, `<b|g|c><sport>-<descriptor>` name-convention check, dims/size — and with `--process` resizes the conformant, non-duplicate files into `public/photos/` via the house `magick -resize 1200x\> -quality 82 -strip` pass (auto-normalizes `.jpeg`→`.jpg`). Flags: `--keep-originals` (also archive the full-res source to `originals/<stem>-original.<ext>`; off by default to respect #66's lean-repo stance), `--force` (overwrite a same-name/different-content file). Tested end-to-end against a duplicate, a space/bad-prefix name, an oversized conformant photo, and a `.jpeg` — all classified + processed correctly; test artifacts removed. Workflow documented in `photo-inbox/README.md` and a new bullet in `CLAUDE.md`. No site-facing change. | Claude ✅ |
| 2026-06-16 | 76 | **Staging-dir photo backlog cleared.** The main checkout's `public/photos/` had accumulated 15 untracked drops (a manual staging area, separate from the deploy). Reviewed all 15 against the tracked library by content hash + visual comparison. **Deleted 5 exact byte-for-byte duplicates** of already-published photos (zero loss): `Stunt-CHafourd-.jpg` (= `cstunt-CHafourd.jpg`), `bbball-1200x906.png` (= `bbaseball-team-1200x906.png`), `bbball-riley-e1771872160503.jpg` (= `gbball-riley.jpg`), `ccheering.jpg` (= `cstudent-section.jpg`), `gdance-taryn-1200x800.jpg` (= `ccheering-taryn.jpg`). **Archived the 10 unique-content drops to `public/photos/originals/`** (per the #54 keep-originals norm; board chose archive over delete to preserve the photographer's hi-res deliverables in-repo): five are the hi-res sources of team portraits already published at web size, renamed with an `-original` suffix tying each to its counterpart (`bXC-team-2025-original.jpg`, `gXC-team-2025-original.jpg`, `bwrestling-team-2026-original.jpg`, `gwrestling-team-2026-original.jpg`, `gtennis-team-2025-original.jpg`); one distinct cheer portrait (`ccheer-team-2025-field.jpg`); two **removed-sport** action shots kept for the archive only (`blacrosse-action-2025.jpg`, `gfieldhockey-action-2023.jpg` — lacrosse + field hockey were dropped as non-CIF club sports in #56); and two **previously-purged orphans** re-archived rather than re-surfaced (`bwrestling-checukk.jpg`, `cXC-group-1200x900.jpg` — both purged in #58). **No site-facing change** — none were action shots that fill the XC/girls-wrestling index gaps #72 left open, and the removed/purged categories stay off the site; this was pure backlog hygiene. | Claude ✅ |
| 2026-06-16 | 75 | **Flag-football team portrait swapped for the official one.** The `gflag-team-2025.jpg` slot from #74 turned out to be an earlier stand-in (a daytime home-jersey shot that was already in the repo from the 2025-26 portrait batch `fb95e32`); Erik delivered the real one — "Girls Flag Football Team Photo.jpg", a night-game team photo in white aways at Rio Mesa (RIO MEMORIAL FIELD). Processed the 4032×3024 source through the standard `magick … -resize 1200x\> -quality 82 -strip …` pass into the **same published name** `public/photos/gflag-team-2025.jpg` (1200×900), so `flag-football.json`'s `teamPhoto` needed no change. Archived per the #54 originals norm: the prior stand-in → `public/photos/originals/gflag-team-2025-old.jpg`, and the new full-res source (metadata stripped) → `public/photos/originals/gflag-team-2025-original.jpg`. Removed the loose space-named raw file from the working tree. Verified the new image serves at 1200×900 via a no-store fetch of the `_next/image` endpoint (the in-browser render briefly showed the old shot purely from Next's optimized-image cache, which a fresh deploy invalidates). | Claude ✅ |
| 2026-06-16 | 74 | **Girls Flag Football team page built** to host the new official 2025-26 squad portrait (`public/photos/gflag-team-2025.jpg`). Holds to the action-vs-portrait division of labor (#58/#72, recorded in memory): the strong flag-football action shot (`gfball-saff7-1200x900.jpg`) stays in the home carousel + `/teams` index via `TEAM_PHOTO_BY_SLUG`, and the formal squad portrait lives on the new page's "Meet the Team" section. Added `src/app/data/teams/flag-football.json` (heroPhoto = the action shot anchored `center 30%` so the raised-flag celebration and faces survive the short hero crop; teamPhoto = the new portrait; a single "Liaison TBD" placeholder so the quick-facts band isn't empty) + route `src/app/teams/flag-football/page.tsx`; flipped `hasPage` → true in `teams.json` so the index card links through instead of reading "Coming soon". **No Decap collection added** — matches the existing volleyball/baseball/track-field pattern where only football carries a CMS collection as the exemplar (the config comment notes the others were intentionally omitted to keep it short). Roster/coach/wishlist omitted (those sections self-hide when absent) pending real content from the coach. `npm run build` clean; `/teams/flag-football` prerenders as static. | Claude ✅ |
| 2026-05-29 | 73 | **Security audit + hardening pass over the API attack surface.** Triggered by a Google TLS-rotation memo (no-op for this repo — see session narrative) but expanded into a full audit. The static pages carry no attack surface; the live surface is the four API routes + the `/board` proxy gate. Audit found the codebase already solid (OAuth CSRF nonce + `postMessage` origin allowlist, constant-time password compare, httpOnly/secure/sameSite cookies, fail-closed gating, SRI on the Decap script, no committed secrets, `.env*` gitignored). Three concrete fixes shipped: **(a)** Decap OAuth scope cut from `repo` → `public_repo` in `src/app/api/decap/auth/route.ts` — the CMS target repo `SanLuisObispoTAB/website` is **public** (verified via `gh`), so the full `repo` scope was needlessly handing the browser-held token write access to every private repo the OAuth app can see; `public_repo` is sufficient and shrinks blast radius if the token leaks. **(b)** Added `Strict-Transport-Security: max-age=31536000` in `next.config.ts`, deliberately **without** `includeSubDomains` — these headers apply to every host the app serves including the `slotab.ravens-peak-consulting.com` alias, and `includeSubDomains` there would assert HSTS across all `*.ravens-peak-consulting.com` siblings we don't control. **(c)** Stopped `src/app/api/springly/{member,sponsor}/route.ts` from reflecting raw upstream Springly API responses and exception messages back to the public client (low-sev info disclosure) — now logged server-side, generic message to the browser. `tsc` clean; no new lint issues. Deferred (need infra, not security theater): rate-limiting / bot mitigation on the public Springly form endpoints and the single-shared-password board login (both need a shared store like Vercel KV or a CAPTCHA), and a Content-Security-Policy (risky to ship blind given the external Decap/unpkg + Hudl/YouTube embeds — wants per-page testing). | Claude ✅ |

---

## Current Build State

### Live preview URLs

- **Primary URL (in transition)**: <https://slotab.ravens-peak-consulting.com> — see #67. The CNAME currently points at the old personal `slo-tab-website` Vercel project; that project's GitHub webhook may have severed when the repo was renamed to `SanLuisObispoTAB/website` (the symptom: recent commits push successfully via the GitHub redirect, but don't appear live). Migration to a new Vercel project under the **Raven's Peak Vercel team** is in flight per #67. End state once the SLOTAB-org Vercel pricing question (Vercel sales pending) resolves: a project under the SLOTAB-org Vercel account.
- ⚠ **`slo-tab-website.vercel.app`** — the personal-account Vercel project from #24/#51. Likely no longer auto-deploying after the GitHub repo rename. Don't rely on it; #67 is the migration.
- ❌ **`ravens-peak-consulting.com/slotab-preview`** — abandoned 2026-05-07 (#51). The mirror in the Raven's Peak repo is months out of date. We considered restoring it as a fallback during #67 but rejected — the app now has real API routes (`/api/decap/*`, `/api/board/*`) and proxy middleware that don't work as a pure static export, so a subpath mirror would silently break Decap CMS login and the Board Hub. Subdomain via Raven's Peak Vercel team is the right path.

### Pages

| Page | URL (preview) | Status |
|---|---|---|
| Home | `/slotab-preview/` | ✅ — Donate-first hero, Impact strip, Spring Social, events, Hudl teaser, About strip |
| About | `/about` | ✅ — mission, governance, Title IX, fund flow |
| Impact | `/impact` | ✅ — real FY 2025-26 ledger data ($210,414.79 across 24 programs), watermark removed (#103). Thermometer (#32) still to come |
| Sponsors / Membership | `/membership` | ✅ — sponsor wall (50 logos), Become-a-Sponsor expandable, Print sheet button, member tiers, Join form (stub) |
| Season Passes | `/season-passes` | ✅ — $250 all-sport / $125 single |
| Merch | `/merch` | ✅ — 9 sport-specific shirt designs |
| Hall of Fame | `/hall-of-fame` | ✅ — ceremony date/venue and ticket link confirmed; **Class of 2026 fundraising band live** with its own donation designation (#184). 🟡 goal figure + engraver quote pending the AD; 🔴 committee names still pending board |
| Watch | `/watch` | ✅ structurally; 🔴 6 placeholder Hudl cards — pending real Hudl API key |
| Spring Social | `/spring-social` | ✅ — flyer + venue + RSVP CTA |
| Volunteer | `/volunteer` | ✅ — meeting schedule + opportunities + team liaisons |
| Upcoming | `/upcoming` | ✅ — 45 events from weekly sheet + SLOTAB events; filterable |
| Teams index | `/teams` | ✅ — all 27 programs, 21 with active pages; season sections lead with the active/upcoming season (#79) |
| Football | `/teams/football` | ✅ structurally; 🔴 roster/photos/wishlist placeholder |
| Girls Volleyball | `/teams/girls-volleyball` | ✅ structurally; 🔴 same |
| Baseball | `/teams/baseball` | ✅ structurally; 🔴 same |
| Track & Field | `/teams/track-field` | ✅ — full coaching staff from team site, real upcoming meets |
| Flag Football | `/teams/flag-football` | ✅ structurally; real 2025-26 squad portrait shipped; 🔴 roster/coach/liaison/wishlist pending |
| +16 team pages (#81) | `/teams/<slug>` | ✅ structurally — girls-tennis, boys/girls water polo, boys/girls basketball, boys/girls soccer, boys/girls cross-country, girls swim & dive, girls golf, boys/girls wrestling, dance (renamed from cheer, #82), competitive-cheer, stunt. Hero action shot + portrait + action gallery (#80); 🔴 roster/coach/liaison pending per team |
| Dance | `/teams/dance` | ✅ new 2026-08-20 (#171, photos #172) — action hero (Cal Poly Showcase), two squad portraits, 1-shot gallery, director bio. All from Coach Mettler's own mail. The `/teams/dance → /teams/cheer` redirect (#132) was **removed** to free the route; do not re-add it. 🔴 no liaison; portrait is 2025-26; gender designation + AGHS show season unconfirmed |
| Contact | `/contact` | ✅ — board roster |
| Decap admin | `/admin` | ✅ — 10 editable collections; 🔴 GitHub OAuth env vars pending |
| Springly admin | `/admin-portal` | ✅ — stub; 🔴 awaiting Springly API key |

### Infrastructure

| Item | Status |
|---|---|
| Standalone repo at `~/slotab-website/` | ✅ initial commit; not pushed to GitHub yet |
| `ravens-peak-consulting` mirror at `/slotab-preview` | ✅ kept in sync until cutover |
| Weekly events scraper (Sun/Mon/Wed cron) | ✅ — pulls SLOHS athletic dept Google Sheet |
| Decap CMS shell at `/admin` | ✅ — pinned 3.3.3 + SRI |
| GitHub Actions cron commits as Erik | ✅ — Vercel accepts |
| Security headers in `next.config.ts` | ✅ — X-Frame-Options DENY, nosniff, strict referrer, locked Permissions-Policy |
| `robots: noindex` | ✅ — on until slotab.org cutover |

---

## Active Backlog

In rough priority order. Move done items to **Built** below.

### Donate flow (highest priority)

- [x] **Research Square transaction-ID tagging** (#31) ✅ **Answered 2026-08-18 (#144): metadata does NOT survive into QuickBooks.** It is capped at 10 pairs and is explicitly private to the calling application — Square “doesn't process this field”. **The line item name is the only thing that carries**, which is what the per-sport items in #140 were already doing. Still to confirm with Trina: whether she reconciles from Square's own item-sales reports or a QB connector, since the common connectors post a daily summary with no line detail
- [x] **Decide Square API vs hosted site (Q1)** ✅ **Hybrid — built (#145) and LIVE since 2026-08-21** — Checkout API payment links for one-time gifts *and* sponsorships, live on sandbox; Subscriptions API + Web Payments SDK for monthly as a separate later phase. Blocked only on a production token. *Original note:* #144 recommends a **hybrid**: Checkout API payment links (ad-hoc line items, server-set amounts) for one-time gifts *and* sponsorships; Subscriptions API + Web Payments SDK for monthly, as a separate later phase. Board go-ahead given 2026-08-18; blocked only on Square credentials
- [ ] Multi-sport selection — still one designation per gift, but no longer *because of* the storefront: since #145 we mint our own single-line-item order, so supporting several sports in one gift is now a line-items change on our side rather than a storefront limitation
- [x] **Tier buttons** (#29): $25 / $50 / $100 / $200 / $500 / $1000 / $5000 + "Other" custom field, no cap; **one-time floor $25** ✅ UI prototype shipped 2026-05-06
- [ ] **Monthly recurring** (#30) — 🔴 **UI pulled 2026-08-19 (#154).** The prototype toggle shipped 2026-05-06 but was never wired to anything that could take a recurring payment, and since the checkout became real (#145) leaving it up would have billed a monthly pledge once. Gated behind `RECURRING_ENABLED` in `DonateForm`, every branch intact. Needs the Subscriptions API: a Catalog plan with a variation per price point, a Customer record, and a card on file via the Web Payments SDK (#144)
- [ ] Transaction-ID per donation encodes sport/team designation; QB report shows customer name/email · sport · amount
- [x] **Show 75% team allocation + 25% general-fund destination** at point of donation (#20, #43) — live preview shows split + general-fund framing as shared programs (not overhead) ✅ shipped 2026-05-06
- [x] **Surface Donate CTA on team pages** (#28) — general SLOTAB + sport-specific buttons; deep-links to `/donate?team=<slug>` ✅ shipped 2026-05-06

### Square → QB sidetool (Phase 0 of Impact rollout)

- [ ] **Build the sidetool** — pulls Square transactions, applies the per-donation tag, exports a QB-ready CSV monthly with: customer name/email · intent (general / sport / big-ticket / sponsorship-bundle) · 75/25 split · sport designation
- [ ] **Transaction-ID schema** — short hash that encodes intent (e.g. `B-FB-S25` for Big-ticket-Football-Spring25); decoded in the sidetool
- [ ] **Donation intent taxonomy**: `general` · `sport=<slug>` · `big-ticket=<line-item>` · `sponsorship-bundle=<id>`
- [ ] **Refund / correction workflow** — sidetool flags mismatches; Trina has a "correct this" path that re-tags + re-emits
- [ ] **Recurring-donation intent persistence** — a monthly donor's designation rolls forward unless explicitly changed
- [ ] **Trina-facing operations doc** (#46) — `docs/treasurer-square-qb.md` covering data flow, tag schema, refund path, failure mode runbook

### Sponsors

- [x] Sponsor logos clickable when `website` field is set ✅
- [x] ~~"Become a Sponsor" expandable above the wall~~ ❌ removed 2026-08-05 (#88) — `BecomeASponsor.tsx` deleted; its tier content is superseded by the official sheet in `MembershipTiers.tsx`, its print-sheet button + Email-VP CTA moved into that section's How-to-Join footer
- [x] **Print sponsor sheet button** ✅ (preserved in `MembershipTiers.tsx` footer as `.slotab-tier-print`, #88)
- [x] **Platinum sponsor home-page carousel** (#36) — auto-rotate every 4.5s, pause-on-hover, click-through, dot navigation ✅ shipped 2026-05-06
- [ ] Populate real `website` URLs for each sponsor (~50 sponsors) (#35)
- [ ] Wire brochure PDF link (was slated for the retired expandable; re-home in the How-to-Join footer when the PDF lands)
- [ ] **Re-tier the sponsor logo wall to the new 2026-27 names?** (#88) — the offering is now Champion/Gold/Silver/Tiger Pride/Varsity but `sponsors.json` / `SponsorWall` / `PlatinumCarousel` / the Decap collection still group the ~50 real sponsors as Platinum/Gold/Silver/Bronze. Needs the board to decide whether to reclassify each sponsor (new external input, E-row) before any code change

### Impact page

- [ ] **Phase 1 — one general SLOTAB thermometer** (#32) — ⚠️ **re-derive the goal before building.** The "$75K covers Huddle + T-shirts + senior banners" anchor is ~2× the real FY26 figure: those three came to **$38,327.90** actual (Hudl $23,000 + t-shirts $8,607.90 + banners $6,720) per #103. Goal source = treasurer's planned-budget figure
- [ ] Optional break-down by drive (fall / winter / spring) on the same page (#32)
- [ ] **Monthly update workflow** (#33) — Trina sends report ahead of monthly SLOTAB meeting; "updated monthly" disclaimer
- [ ] Disclaimer: "75% of designated donations go to that team" (#33)
- [ ] **Year-over-year totals** (#45) — show prior-FY raised next to current-FY raised so the page is useful mid-year
- [ ] **General-fund explainer** (#43) — short About-page-or-Impact-page block: what the 25% covers (Huddle, senior banners, T-shirts, sectional fees, support for under-fundraising teams). Not "overhead"
- ✅ ~~Replace the 24 placeholder line items with real ledger data~~ — done 2026-08-11 (#103); FY 2025-26, reconciled to $210,414.79
- ✅ ~~Filter to **large purchases only**; broad-impact framing for shared items~~ — done (#103); named purchases listed individually, allocations rolled into one line
- ✅ ~~Remove the DRAFT watermark + red ribbon when real data lands~~ — done (#103), CSS deleted
- [ ] **Per-team spend detail is deliberately withheld** (#103) — the ledger supports it ($117,050.97 across 24 programs, Football $24,917.73 high / Softball $225 low) but the board chose name-only chips over a public ranking. Revisit only if the board explicitly asks
- [ ] **Ask the athletic department what the allocations bought** — the single highest-value follow-up. 55.6% of the money is currently describable only as "season allocations"; item detail from the AD would turn that one line into the page's best content
- [ ] **Phase 2 (later)** — per-team transparency (totals raised + 75% allocated, no goals yet)
- [ ] **Phase 3 (later still)** — per-team thermometers with goals, big-ticket sub-goals, stretch states

### Coach onboarding 1-pager (#44)

- [ ] Draft `docs/coach-onboarding-slotab-funds.md` — explains where dollars come from, the 75/25 split, what the general fund covers, how to read a team page's totals, how to flag corrections
- [ ] Distribute via Phil + Adam at AD handoff
- [ ] Reference from team-page sidebar so coaches can self-serve

### Membership

- [x] ~~**5-tier ladder prototype** (Supporter / Fan / Booster / Champion / Patron)~~ ❌ removed 2026-08-05 (#88) — superseded by the official 2026-27 sheet; `ProposedTiers.tsx` deleted
- [x] **One-time vs monthly toggle** on tier cards showing both qualification paths ✅ shipped 2026-05-06 (retired with `ProposedTiers`; the new sheet shows annual + recurring inline, no toggle)
- [x] **4-year auto-renew option** in donate form (#37) ✅ UI prototype shipped 2026-05-06 (only visible when monthly is selected)
- [x] ✅ **Membership ↔ sponsorship merge** final naming + level decision (#37) — **resolved 2026-08-05 (#88)** via the Sponsorship lead's official 2026-27 sheet (Champion/Gold/Silver/Tiger Pride/Varsity sponsor tiers + Family/Individual memberships). Shipped to `/membership` as `MembershipTiers.tsx`
- [ ] Real "Join Online" form (live Springly when API key arrives)

### Teams

- [ ] Real rosters from each coach/liaison (currently 12-player placeholder per team)
- [ ] Real wishlist line items per team (currently plausible placeholders) — **wishlists confirmed valuable** (#40)
- [x] **Sport-specific photos for every CIF team** ✅ shipped 2026-05-07 (#53). Every slug in PHOTO_BY_SLUG now points at a real sport photo; T&F hero swapped from the stray tennis placeholder to `ctrack-1200x800.jpg`. 🟡 *Remaining:* `boys-lacrosse` and `field-hockey` are using stock/other-school photos (Los Gatos, Wilton) until SLO Tiger versions arrive
- [ ] Replace the two stock-photo placeholders (lacrosse + field-hockey) with actual SLO Tiger team photos
- [ ] Per-team action shots (rosters / coach portraits / game-day moments) for the 27 individual team pages once each coach contributes
- [ ] Real liaisons per team (currently "Liaison TBD")
- [ ] Coach bios (school site has name + email only)
- [x] **Flag Football page built** (#74) — first team page added since the original four; hosts the new 2025-26 squad portrait. Action shot stays in carousel/index per #72
- [x] **16 more team pages built** (#81) — every team with a posed team photo now has a page (21 total), each with an action-shot gallery (#80). Pages auto-appear in the season-filtered nav + `/teams` index (#79)
- [ ] Build out pages for the 6 remaining action-only teams (boys volleyball, beach volleyball, softball, boys tennis, boys swim, boys golf) once a posed team photo arrives for each
- [ ] **Adam Basch (outgoing AD, abasch@slcusd.org) to shepherd team-page template + outreach to coaches** during the AD handoff overlap with Phil (incoming AD)
- [ ] **Do NOT add game-communication tooling** (#40) — Home Campus is SoT for schedules/changes next year; practice schedules excluded (too volatile)
- [ ] Update the Standard Comms Kit doc with the link-to-schedule pattern instead of in-kit duplication

### Home page

- [ ] **Combine Member + Volunteer into a single secondary CTA** (#27); Donate stays primary
- [ ] **Swap top-right nav "Join" pill → "Donate"** (#27)
- [ ] Apply Design Option #1 (*classic collegiate*) — keep the SLO|TAB masthead (#41)
- [ ] Platinum sponsor carousel slot (links from Sponsors backlog #36)

### Hall of Fame

- [x] ✅ Real induction ceremony date + venue — **October 10, 2026, The Octagon Barn** (corrected 2026-08-11, #106)
- [x] ✅ **Booster Bash ticket sales go live 2026-07-01** (#38) — live at `auctria.events/SLOHSathleticsboosterbash`
- [x] ✅ Real **Booster Bash Tickets** + **HOF Fund Donate** URLs — tickets link is live; the HOF donate ask is now **on-site** rather than an external URL (`/donate?tab=general&team=hall-of-fame`, #184), so `ceremony.donateUrl` stays empty by design
- [ ] 🟡 **AD review of the fundraising band** — sent 2026-08-21 as an unlisted page on production, `slotab.ravens-peak-consulting.com/preview/hof-fund` (#185). PR #6 holds the wiring into `/hall-of-fame` until it comes back approved; **delete the preview route when it does**
- [ ] 🔴 **A real fundraising goal for the Class of 2026 fund** — AD. `fund.goalDollars` in `hof.json` is `0`, which hides the thermometer; one number turns it on (#184)
- [ ] 🟡 **Confirm the dollar-to-item mapping against an engraver quote** — AD/committee. The six levels are the committee's working plan, not quoted pricing (#184)
- [ ] 🟡 **Refresh `fund.raisedDollars` + `raisedAsOf`** from the Hall of Fame row on `/board/square-report` after each SLOTAB meeting — Treasurer (#184)
- [ ] 🟡 **Photos from the school's HOF page** — `slohs.slcusd.org` is blocked by this session's egress proxy, so its inductee and ceremony images could not be pulled. Manual drop into `photo-inbox/` (#184)
- [ ] Real committee roster
- [ ] Alumni Membership tier — pricing + benefits + join URL (the official 2026-27 sheet / #88 did **not** cover Alumni, so this is still open but no longer gated on the now-resolved wine meeting; tracked as E8)

### Watch (Hudl)

**🔴 BLOCKED 2026-05-06** — All Hudl auto-population work tabled (#48) until Erik secures Owner-level access to the SLOHS Hudl Pro account. The Pro tier exposes the API via `developer.hudl.com` OAuth, but app registration requires Owner credentials.

- [ ] 🔴 Real Hudl game data (auto-scrape via API once Owner-level OAuth credentials arrive)
- [ ] 🔴 Per-game iframe embeds (replace link-out cards on paid Hudl)
- [ ] 🔴 Live-badge pipeline that flips when a stream is live
- [x] **"Sponsored by SLOTAB" label on each Huddle embed** (#26) ✅ shipped 2026-05-06; Watch page intro also updated to credit SLOTAB and tie to donations

**Plan B if super-admin path stays blocked**: keep the current link-out model. Coaches/liaisons paste embed URLs into `data/hudl.json` via Decap CMS as games happen. The "Sponsored by SLOTAB" labels (#26) already deliver the donation-glue value the transcript prioritized.

**What's needed when unblocked**: Hudl Client ID · Client Secret · API base URL · org ID 5267 · scopes (`video:read`, `team:read`, `organization:read`, ideally `live:read`) — set as Vercel env vars `HUDL_CLIENT_ID`, `HUDL_CLIENT_SECRET`, `HUDL_API_BASE`, `HUDL_ORG_ID`.

### Springly

**🔴 BLOCKED 2026-05-06** — Tabled (#48) until Erik secures Owner-level credentials. Confirmed plan tier is **Serenity**; admin login received 2026-05-06 had no Integrations tab visible — Owner role almost certainly required to access API settings (or to confirm whether Serenity includes API access at all).

- [ ] 🔴 Confirm Serenity tier includes API access (or identify the add-on / upgrade path)
- [ ] 🔴 Generate API key from Springly Owner admin and capture endpoint base URL
- [ ] 🔴 Add `SPRINGLY_API_BASE` + `SPRINGLY_API_KEY` env vars on Vercel
- [ ] 🔴 Confirm the Springly REST endpoint path (currently guessed `/contacts` in `src/app/api/springly/*/route.ts`)
- [ ] 🔴 Swap admin-portal page from static JSON to live fetch
- [x] **Import research + canonical CSV shape** drafted in `docs/springly-import.md` (#63) ✅ 2026-05-14 — covers what's known/unknown about the import wizard, the canonical 2-CSV shape (members/sponsors), 8 gotchas to audit before importing real data, and ordered next-steps
- [x] **Sample test CSVs** at `docs/springly/test-members.csv` + `test-sponsors.csv` ✅ 2026-05-14 — fake-data dry-run targets ready for the moment Owner access lands
- [ ] 🟡 **Capture Springly import-wizard screens** to `docs/springly/import-wizard-screens/` once Owner access is sorted (blocks the precise column mapping for the real import)
- [ ] 🟡 **Receive real source spreadsheets** — Trina (members/donor ledger), sponsorship lead (sponsor list)
- [ ] 🟡 **Normalize source → canonical CSVs** via a one-shot `scripts/normalize-springly-import.ts`
- [ ] 🔴 Bulk-import existing membership data into Springly (final step after the above)

**Plan B if super-admin path stays blocked**: route the Join form through a Zapier webhook that creates the Springly contact (~30 min code change, ~$20/mo Zapier cost). Embedded Springly form widget is also possible if Serenity exposes one.

**Support email already drafted** in our 2026-05-06 conversation — Erik will send it once he's the Owner or has confirmation that Serenity includes API access.

### Events workflow (#39)

- [ ] Set up shared Google Sheet (board + Adam + Phil + liaisons) as the source of truth
- [ ] Daily auto-sync from sheet → site (extends existing Sun/Mon/Wed cron)
- [ ] Add 2026-2027 tab to the sheet
- [ ] Include SLOTAB events on the sheet: physicals night, fall parent meeting, spring parent meeting, board meetings, drives

### Deploy / GitHub

- [x] Push `slotab-website` to a new GitHub repo ✅ — <https://github.com/eramberg/slo-tab-website>
- [x] Connect to Vercel as its own project ✅
- [x] Update Decap config `repo` field to the new repo path ✅
- [x] Update GitHub OAuth App callback URL to the new deploy URL ✅
- [x] Add `DECAP_GITHUB_CLIENT_ID` + `DECAP_GITHUB_CLIENT_SECRET` env vars ✅
- [ ] Designate the 2–3 board editors and grant Write access on the new repo
- [ ] **Preview the site at the next casual board cocktail at the Hub** (#42)
- [x] **Go-live** ✅ — DNS cutover + host-aware `noindex` landed 2026-08-15 (#100, #115)
- [ ] 🔴 **Cancel GoDaddy hosting** — the “48h after go-live” step from #42 was never done, and #141 confirmed the old WordPress site is **still serving `200`**. Blocked on finding the GoDaddy account

### Security / hardening (#73)

Security audit on 2026-05-29 found the API surface solid and shipped three fixes (Decap OAuth scope → `public_repo`, HSTS header, Springly routes stop leaking upstream errors). Two items were deferred because they need infrastructure, not just code:

- [ ] ⏳ **Rate-limiting / abuse protection** on the public Springly form endpoints (`/api/springly/{member,sponsor}`) and the board login (`/api/board/login`). The realistic threats are scripted spam of the Springly CRM (cost + junk contacts) and online brute-force of the single shared board password — neither is stopped by an Origin/CSRF check, since an attacker need not use a browser. Needs a shared counter (Vercel KV / Upstash) or a CAPTCHA. **Do before Springly credentials go live** (currently still 🔴 per #8/#48, so not yet exploitable).
- [ ] ⏳ **Content-Security-Policy header.** High value but risky to ship blind — the site loads an external Decap script from unpkg and embeds Hudl/BlueFrame, YouTube, and Google Fonts. Ship `Content-Security-Policy-Report-Only` first, watch the violation reports across `/admin`, `/watch`, and team pages, then promote to enforcing. Pairs naturally with the 2026-08-01 go-live hardening.

---

## Pending Snapshot — 2026-05-06

> **Read as history, not as a to-do list.** This section is a snapshot of 2026-05-06. Its Square items — Q1, T2, and the transaction-ID research — were all resolved between #144 and #164 and the integration went live on 2026-08-21. Left unedited so the record of what was actually pending that day stays intact.

Cross-cut of everything still pending, grouped by blocker. Detailed
versions of each row appear in *Active Backlog*, *Open Questions*,
and *External Inputs Pending* below — this section is the at-a-glance
view to skim before each session.

### Resolved by the 2026-05-06 transcript

All six items previously in *Pending the transcript* now have direction. Q1 (Square API vs. hosted) is the one architectural decision still outstanding.

| # | Item | Outcome |
|---|---|---|
| ~~T1~~ | Donate page details | ✅ Resolved — see #29 (ladder), #30 (recurring), #31 (transaction-ID for QB) |
| T2 | Square API vs. hosted-site | 🟡 Erik researching mechanics (see #31); shared with Trina before commit |
| ~~T3~~ | Impact page rebuild | ✅ Simplified — see #32, #33. One general thermometer first; per-team later |
| ~~T4~~ | Membership tier auto-assignment | ✅ **Fully resolved 2026-08-05 (#88)** — *any donation = membership* + final levels/naming landed via the Sponsorship lead's official 2026-27 sheet (was pending the wine meeting / Q7) |
| ~~T5~~ | Platinum sponsor home rotator | ✅ Approved — auto-rotate, click-through (#36) |
| ~~T6~~ | Open-ended | ✅ See "New from 2026-05-06" below |

### New from 2026-05-06 (not previously in the doc)

| # | Item |
|---|---|
| N1 | "Sponsored by SLOTAB" label on Huddle embeds (#26) |
| N2 | Combine Member+Volunteer into one CTA; swap top-right "Join" → "Donate" (#27) |
| N3 | Donate CTA surfaced on team pages (general + sport-specific) (#28) |
| N4 | "Become a Sponsor" expandable ABOVE wall (collapsed default) (#34) |
| N5 | Booster Bash sales launch 2026-07-01 (#38) |
| N6 | Shared Events Google Sheet (Phil + liaisons + board) → daily auto-sync (#39) |
| N7 | Team pages are NOT for game communication — Home Campus is SoT (#40) |
| N8 | Design Option #1 *classic collegiate* selected; keep SLO\|TAB masthead (#41) |
| N9 | Go-live target 2026-08-01; preview at the Hub cocktail meeting (#42) |

### Pending board decisions (4)

| # | Decision needed | Unblocks |
|---|---|---|
| Q1 | **Square API vs. hosted Square site** | Donate rebuild architecture; Erik researching tag-with-transaction-ID approach (#31), will share findings with Trina |
| ~~Q2~~ | ~~GitHub org name~~ ✅ — repo at `eramberg/slo-tab-website` | — |
| ~~Q3~~ | ~~Donation-to-tier mapping~~ → superseded: any donation = membership (#37); naming + 4-year auto-renew pending Deneen + Ann wine meeting | — |
| ~~Q4~~ | ~~Per-team goal source~~ → deferred until after first general-thermometer cut lands (#32) | — |
| ~~Q5~~ | ~~Per-trimester goal~~ → reframed as the general SLOTAB thermometer (#32) with optional drive breakdown | — |
| Q6 | **Standard Comms Kit defaults** post-#10 evolution | Internal kit playbook update |
| ~~Q7~~ | ~~**Wine meeting outcome** (Deneen + Ann) — final membership/sponsorship levels, naming, 4-year auto-renew~~ ✅ **Resolved 2026-08-05 (#88)** — the Sponsorship lead's official 2026-27 sheet is the answer: sponsor tiers Champion/Gold/Silver/Tiger Pride/Varsity + memberships Family/Individual. Shipped to `/membership`. (The floated "Supporter/Fan/Booster" naming was not adopted.) | — |

### Pending external inputs (20)

| # | Input | Source | Unblocks |
|---|---|---|---|
| E1 | Hudl API key | Hudl admin / paid plan dashboard | Auto-population of `data/hudl.json`, real Watch tab |
| E2 | Sample Hudl per-game embed URL | Hudl admin | Confirms iframe format for inline embeds |
| E3 | Springly API base URL + key | Springly admin | Live Join form + admin portal |
| E4 | HOF ceremony date + venue | HOF Committee | Removes "TBD" from HOF page |
| E5 | Booster Bash Tickets URL | Board | Replaces `#` on HOF page |
| E6 | HOF Fund Donate URL | Board | Replaces `#` on HOF page |
| E7 | HOF Committee roster | HOF Committee | Replaces placeholder list |
| E8 | Alumni Membership pricing + benefits + join URL | Board | Removes placeholder copy |
| E9 | Real per-team rosters | Each coach/liaison | Replaces 12-player placeholder per team |
| E10 | Real per-team wishlist line items | Each coach/liaison | Replaces fake $ amounts |
| E11 | Real liaison names + emails per team | Board (sourced from comms kit) | Replaces "Liaison TBD" |
| E12 | Coach bios | Each coach | School site has only name + email |
| ~~E13~~ | ~~Sport / game photos~~ | ~~Board / parents~~ | ✅ Resolved 2026-05-07 (#53) — every slug has a sport-specific photo. Open: replace stock-photo placeholders for `boys-lacrosse` (Los Gatos) and `field-hockey` (Wilton) with SLO Tiger versions |
| E14 | Sponsor websites | Sponsorship Development | Logos become clickable (~50 sponsors) |
| E15 | 2–3 board editor GitHub usernames | Board | Decap CMS write access on the new repo |
| ~~E16~~ | ~~Spirit-squad structure — distinct teams? official names + genders?~~ | ~~AD~~ | ✅ Resolved 2026-06-16 (#83, #84) — AD confirmed: Fall = Girls Dance + Competitive Cheer; Winter = none; Spring = STUNT (all-caps). All three squads girls-only |
| ~~E17~~ | ~~Sponsor logo-wall re-tiering decision (#88)~~ ✅ Resolved 2026-08-06 (#93) — lead confirmed re-tier; wall renamed Platinum→Champion/Bronze→Varsity, 2025-26 logos kept for now | Board / Sponsorship Development | — |
| ~~E18~~ | ~~Confirm the faint prices on the printed sheet (#88)~~ ✅ Confirmed 2026-08-05 by Erik — Gold $5,000 / Silver $2,500 annual; recurring $95 / $45 / $11 / $5 mo all correct as read | Erik / Sponsorship lead | — |
| E19 | Impact-page statistics + real numbers (#93) | Adam Basch & Phil Angel | Replaces placeholder figures on `/impact` with meaningful data |
| E20 | Real Donate-drive dates + flip the drive on when it runs (#93) — edit `campaign.json` via the Decap "Donate Drive" collection (mode on/auto + start/end) | Erik / Board | Surfaces the prominent "Donate Now" CTA during the seasonal campaign |

### Pending the GitHub org move (7)

G1 + G2 done ✅. Remaining items below.

| # | Item | Notes |
|---|---|---|
| ~~G1~~ | ~~Push to new GitHub repo~~ | ✅ <https://github.com/eramberg/slo-tab-website> |
| ~~G2~~ | ~~Update `public/admin/config.yml` `backend.repo`~~ | ✅ `eramberg/slo-tab-website` |
| ~~G3~~ | ~~Connect Vercel project to the new repo~~ | ✅ |
| ~~G4~~ | ~~GitHub OAuth App + env vars~~ | ✅ Decap CMS at /admin verified end-to-end |
| G5 | DNS cutover — `slotab.org` → Vercel | 🟡 **IN PROGRESS 2026-08-11 — Erik-side.** Full step-by-step in **`docs/dns-cutover-runbook.md`**, written against the verified account state (team `eramberg-9955s-projects`, project `slo-tab-website`). Key rules: change **records, never nameservers** (protects MX/TXT), and check `dig MX slotab.org` first |
| ~~G6~~ | ~~Drop `robots: noindex`~~ | ✅ **Done 2026-08-11 (#100)** — replaced with something better than a flag: indexing is host-aware, so slotab.org is indexable while every other host stays `noindex` permanently. No longer coupled to G5's timing |
| G7 | Cancel GoDaddy Managed WordPress | **48 hours after G5.** Cancel the **hosting product only** — never the domain registration, or the DNS records go with it. Back up the old WordPress site first. Destructive and irreversible — Erik-side |
| G8 | Submit `slotab.org/sitemap.xml` to Google Search Console | After G5. Sitemap + robots.txt ship as of #100 |
| G9 | Swap Decap `base_url` to slotab.org **and** the GitHub OAuth App callback URL, in one sitting | After G5. Doing either alone breaks `/admin` login for the whole board (#100) |

### Highest-leverage unblockers

Transcript ✅ resolved most architectural questions. Remaining big levers:
- **Square API vs. hosted (Q1)** + transaction-ID research → unblocks Donate page rebuild (#29, #30, #31)
- **Wine meeting (Q7)** → unblocks final Membership levels + naming
- **🔴 Owner-level Hudl Pro credentials** (#48) → real Watch tab. *Tabled 2026-05-06.*
- **🔴 Owner-level Springly Serenity credentials** (#48) → live Join form + admin portal. *Tabled 2026-05-06.*
- ~~**Real Impact ledger data (E_impact)**~~ ✅ **Resolved 2026-08-11 (#103)** — watermark dropped. Successor ask: *what did the $117K in team allocations actually buy?* (athletic department, not treasurer)

**Total open**: 2 board-blocked · 13 external-blocked (2 of those tabled #48) · 3 deploy-blocked

---

## Open Questions (waiting on a decision)

| # | Question | Notes |
|---|---|---|
| ~~Q1~~ | ~~Square API vs hosted Square site?~~ | ✅ **Resolved and shipped 2026-08-21 — a hybrid, and it is live.** #144 answered the gating question (#31): Square's order metadata is capped at 10 pairs, private to the calling application, and **never reaches a QuickBooks report** — so the transaction-ID scheme as conceived could not have worked. The **line item name** is what carries. #145 built the Checkout API path (payment links with server-set amounts and ad-hoc line items) for one-time gifts *and* sponsorships; recurring stays out until the Subscriptions API (#154). #164 re-pointed it at the account that actually takes the club's money (#162). Trina was consulted (#161) and approved; a real donation completed 2026-08-21. |
| ~~Q2~~ | ~~New GitHub org name?~~ ✅ Resolved 2026-04-23 — `eramberg/slo-tab-website` | — |
| ~~Q3~~ | ~~Donation → tier mapping~~ ✅ Superseded 2026-05-06 — any donation = membership (#37); levels TBD via wine meeting | — |
| ~~Q4~~ | ~~Per-team fundraising goals~~ ✅ Deferred 2026-05-06 — start with one general SLOTAB thermometer (#32); per-team comes later | — |
| ~~Q5~~ | ~~Per-trimester campaign goal source~~ ✅ Reframed 2026-05-06 — general SLOTAB thermometer with optional drive breakdown (#32) | — |
| Q6 | Standard Comms Kit defaults post-#10 | Internal playbook evolution; kit links to central schedule. |
| ~~Q7~~ | ~~Wine meeting (Deneen + Ann) outcome~~ | ✅ **Overtaken by the board's final 2026-27 sheet, 2026-08-20 (#170).** All three parts are settled by it. **Naming:** the ladder is Champion / Gold / Silver / Tiger Pride / Varsity plus Family / Individual / Tiger Friend — *"Supporter / Fan / Booster"* was never the club's ladder, it came from a May research draft that had leaked into `/donate` and was removed in #158, where it was telling a $1,500 donor they were "Champion" against a page selling Champion at $10,000. **Level → benefits table:** transcribed from the sheet and now the single source both `/membership` and the donate box render from. **4-year auto-renew:** gone with monthly giving (#154), which was pulled because the checkout can only charge once. |

---

## External Inputs Pending

| Item | Owner | Notes |
|---|---|---|
| 🟡 **Does the Square storefront already have a Hall of Fame item? If so the fallback stops losing the designation** | Erik | #182's product sweep of `slotab-3.square.site` recorded the store carrying "memberships, sponsorships, sport passes, team donations **and Hall of Fame**" — so a Hall of Fame tile may already exist. That matters for exactly one path: since the production token landed (#181) production mints real checkouts, and the storefront handoff now fires **only when something is broken** (missing variable, deactivated location, outage). On that path a `hall-of-fame` gift falls through to GENERAL ATHLETICS and the donor is asked to email us the designation and any tribute (#184). If the item exists, adding one line to `PATH_BY_SLUG` in `src/app/data/square-donate.ts` makes the degraded path lossless instead. **Could not be verified from here** — `slotab-3.square.site` is blocked by this session's egress proxy, and guessing a product URL would be worse than the honest warning that is there now. Needed: the product path from the live storefront, the same way the other 27 were captured |
| 🟡 **A goal figure for the Hall of Fame fund — promised by the AD, thermometer hidden until it lands** | Erik → AD | **Downgraded from 🔴 on 2026-08-24 (#186): the AD has said he is putting a number together**, so this is now awaited rather than unasked. Note the number changed meaning with his review — it is a target for the **standing fund**, not for one class, so whatever he gives should be a figure the fund is measured against going forward (an annual target or a campaign total), and `fund.goalLabel` is the phrase it is read against (*"$6,400 raised of $25,000 **for the Hall of Fame Fund**"*). The render path is proven: it was exercised with temporary figures and produced the right bar width, percentage and ARIA values, so the only missing input is the number itself. The band shipped with `fund.goalDollars: 0` in `src/app/data/hof.json`, which hides the progress bar entirely — deliberate, because a goal invented by the website on a 501(c)(3) donation page is worse than no goal. Set it and the bar appears with no code change — Decap: *Hall of Fame → Hall of Fame Fund → Fundraising goal*. Two figures make it work properly: the **goal**, and a **`raisedDollars` + `raisedAsOf`** pair refreshed from the Hall of Fame row on `/board/square-report` after each meeting, so nobody reads a stale total as live |
| 🟡 **An engraver quote, before the giving levels are repeated in print** | Erik → AD / HOF committee | The ladder on `/hall-of-fame` maps dollars to objects — **$50 nameplate · $100 medallion · $250 award · $500 one inductee fully honored · $1,000 ceremony underwriter · $2,500 legacy patron** (#184). Those amounts are the committee's *working plan*, not quoted pricing, and the page says so ("levels are how the committee plans to spend the fund"). That is honest on a web page; it would not be on a printed program or a mailer that says a specific gift buys a specific object. Confirm the real per-item costs and adjust `fund.levels` in `hof.json` — the numbers are data, the component reads whatever is there. **Since #186 these are recurring costs, not one class's shopping list**, so the useful question for the AD is what a typical induction costs per inductee, not what the Class of 2026 specifically needs |
| 🟡 **Hall of Fame photos from the school site could not be pulled from here** | Erik | Erik pointed at `slohs.slcusd.org/athletics/hall-of-fame` on 2026-08-21 and cleared any image on it for use. **The host is blocked by this session's network egress policy** — both `curl` and the fetch tool are refused at the proxy — so no image on that page could be read, let alone imported, and the band was built from four photographs already in `public/photos` (three of them previously unreferenced). Same manual hand-off as the Dance and XC drops: save the images, drop them in `photo-inbox/`, then `npm run photo-intake` → `--process` → **`npm run photo-usage`** (non-optional — the school page may well be serving re-exports of shots already in the library, which the byte-level dup check will not catch, #111/#112). Most valuable would be **inductee portraits or a shot of the wall display in the Big Gym**: the band currently argues for the fund with team photography, because there is no photograph of the thing being funded |
| 🔴 **A $2,500 Gold sponsor from 17 Aug is not on the sponsor wall — and $2,500 is not Gold any more** | Erik → Dannene / Board | Surfaced 2026-08-23 by the "before" screenshot in Dannene's own Square Notifications mail, which happens to be a **real sponsorship receipt**: order **#965796768**, 17 Aug 4:08 PM, **$2,500.00**, item **"Gold Business Sponsorship"**, sports credited **Dance** and **Cross Country**, buyer **Tera Dube — (805) 797-4638 — tera@bhgrehaven.com** (Better Homes & Gardens Real Estate Haven Properties), MasterCard 0768 keyed. **Two problems.** (1) **She is not on the sponsor wall.** `sponsors.json` carries nine businesses and none of them is hers, so a paying sponsor has had no website recognition since 17 August. (2) **She paid $2,500 for "Gold".** That is the **#143 mispricing** — the storefront sold Gold at $2,500 against the site's $5,000 — and 17 Aug is *before* the 20 Aug re-tier that closed it (#163). On the board's final sheet **$2,500 is Silver**. So somebody has to decide what the club actually sold her: honour Gold at the price she was shown, or place her at Silver and explain. **That is a board call, not a code change** — but until it is made her wall entry cannot be written, because the tier is the one thing a wall entry has to state. Needed to close: the tier decision, a high-resolution logo and a website URL |
| 🟡 **Was the 22 Aug $2,500 payment link a Silver sponsorship? If so it reached nobody** | Erik → Square dashboard | The "after" screenshot in the same mail is a real payment: **$2,500.00, paid 22 Aug 2026 6:33 PM, Heather Richert, heatre24@gmail.com, VISA 2957**, through a **payment link**. $2,500 is exactly **Silver Sponsor** on the current sheet, and 22 Aug is the day after the checkout went live (#181) — so this may well be the **first business sponsorship taken through the new flow**. If it is, the fulfilment handoff built in #177 **did not fire**, because the mailer has never been configured, and a Silver sponsor is owed two banner locations, passes and three credited sports that nobody has been told about. **Check in the Square dashboard:** open the payment, look at the order metadata. `source: slotab-website` with `kind: sponsorship` and `tier: silver` confirms it, and `sports` names the teams she chose. If the metadata is absent it was minted outside the website and is a different question. This is the concrete cost of the env-var row below still being open |
| 🟡 **A decision row was overwritten on `main`: the Dance 2026-27 portrait work is live but no longer documented** | Erik / whoever owns `main` | **Found 2026-08-23 while merging `main` into the #187 branch — flagged, deliberately not "fixed" here.** Commit **`de0d033`** added decision **#186 — "Dance has its official 2026-27 portrait, and the coach's Future Tigers photo is on the page"**, covering the new `{photo, caption}` gallery shape and the two audit scripts taught to read it. The next commit on `main`, **`23fc8a7`** (Hall of Fame standing fund), **reused #186 for itself** and removed the Dance row *and* its `Last updated` entry. **The code shipped and is live** — `gdance-team-2026.jpg`, `gdance-future-tigers-2025.jpg` and the `dance.json` changes are all on `main` — **so the site now carries work with no record of why.** This is precisely what *"Never edit prior rows"* exists to prevent, and it is worth noting that the convention did not fail quietly: the collision surfaced the moment two branches tried to merge. **Not corrected from this branch on purpose:** renumbering somebody else's decision is the same act that caused the problem, and picking a new number for the Dance row (191? a second 186?) is a call for whoever owns that history. **The original text is not lost** — recover it with `git show de0d033:docs/project-status.md` and search for *"Dance has its official 2026-27 portrait"*. **How to check now:** `git log --oneline -- docs/project-status.md` and diff `de0d033` against `23fc8a7`. |
| 🔴 **Something is POST-ing to `https://slotab.org/api/webhook` every few minutes and getting a 404 — the path does not exist. If that is Square, no webhook has ever been  ✅ **Closed 2026-08-23 (#191). It was Square, exactly as suspected.** Erik opened the dashboard: subscription **`wbhk_f7a4328dae9f45fe862c689a09679839`**, production, **Enabled**, event `payment.updated`, API version `2026-08-19`, **URL `https://slotab.org/api/webhook`** — one path segment short of the route. The delivery log is nothing but `404 payment.updated https://slotab.org/api/webhook` with retries queued. **So the #177 sponsorship handoff has never fired, not once, despite all four environment variables being correctly set** — which is precisely why it looked healthy from Vercel and why #189's "check, don't cite" rule found it. **Fixed in the code rather than only in the dashboard, because Square had pending retries**: a deploy lands in a minute, a dashboard edit waits on a volunteer, and Square eventually gives up. `/api/webhook` is now a working alias for the same handler, and `SQUARE_WEBHOOK_URL` accepts a comma-separated list with the canonical/alias sibling added automatically — so signatures verify whichever spelling Square uses, and **no env change is required either way**. **How to check now:** `/board` lists the accepted URLs; Square's own delivery log should stop showing 404s within minutes of the deploy. |
| ~~🔴 **The Square notification emails are blocked on env vars**~~ ✅ **RESOLVED — all four were already set; the row was wrong, twice over** | ~~Erik~~ | ✅ **Closed 2026-08-23 (#188). Erik: "SQUARE_WEBHOOK_SIGNATURE_KEY and SQUARE_WEBHOOK_URL also already exist."** All four variables were in Vercel; this row asserted otherwise for two days and I repeated it three times without checking. **As of 2026-08-23, per Erik: `RESEND_API_KEY`, `EMAIL_FROM`, `SQUARE_WEBHOOK_SIGNATURE_KEY` and `SQUARE_WEBHOOK_URL` are all set.** **How to check now, rather than trusting this line:** open **`/board`** — the Operations block reads the four variables live and shows 🔴 off / 🟡 half on / ✅ on (`lib/notification-config.ts`, #187). Second check, from anywhere: `curl -X POST https://slotab.org/api/square/webhook -d '{}'` — a **401** means the signature key and URL are set and it rejected an unsigned caller; a **503** means they are not. *(That check could not be run from the Claude session — egress to `slotab.org` is refused by the sandbox's network policy, and the Vercel MCP does not expose environment variables. Which is the point of the `/board` readout: the answer has to live where someone who can look will see it.)* **What this means:** the sponsorship handoff from #177 has been live since it shipped, and the donation checklist goes live the moment #186 merges — nothing is waiting on anybody. ~~🟡 Still genuinely unconfirmed: whether Resend has ever successfully *delivered*.~~ ✅ **RESOLVED 2026-08-24 00:16 UTC (#192) — it delivered.** A real Square retry landed on the new alias route and the log read `donation email for HVLTzFNhAoluSFeVGCo985Od28EZY: **sent**`. The mailer was never broken; nothing had ever been able to reach it. No 422, so `EMAIL_FROM` is on a verified domain. *Original row, written with #177 on 2026-08-21 and stale within the day:* built and **deliberately inert until configured** — needed `SQUARE_WEBHOOK_SIGNATURE_KEY`, `SQUARE_WEBHOOK_URL`, `RESEND_API_KEY` and `EMAIL_FROM`; "there is **no email account or provider yet**, so Resend signup is a prerequisite" |
| ~~🟡 **Two sponsor-tier discrepancies against the VP's 17 Aug email**~~ | ~~Erik → Dannene~~ | ✅ **Resolved 2026-08-21 (#178) — Dannene's email was right on both counts, and the newer list was wrong.** Crocker is **Silver**, not Tiger Pride; SLO Safe Ride is a **Gold** sponsor and had simply been omitted. Both corrected, wall now at **9** sponsors. Worth remembering the lesson: the printed list Erik was handed was the *newer* document and still lost a paying Gold sponsor, so newer did not mean more reliable — cross-checking it against the VP's email is what caught it. *Original row:* The board list Erik supplied 2026-08-21 disagrees with Dannene's 17 Aug "Business Sponsors" email in two places, and the newer list was taken as authoritative for #176. Worth confirming rather than assuming: **(1) Crocker Construction** is **Silver** in her email and **Tiger Pride** on the newer list — a move *down* the ladder ($2,500 → $1,000), which is unusual enough to be a transcription slip. **(2) SLO Safe Ride** is a **Gold** sponsor in her email and **absent entirely** from the newer list. We still hold their banner artwork in `photo-inbox/`, and their logo is still in `public/sponsors/gold/`, so if they did renew it is a one-line restore |
| 🔴 **A push to `main` can silently fail to deploy — verify the live page, don't trust the push** | Erik | **Happened 2026-08-20.** Commit `726c731` (Teams menu + the Cheer and Flag Football rosters) reached `origin/main`, but Vercel **never queued a build for that SHA** — not a failed build, *no deployment record at all*. Newest production build stayed at `cf79d63`, so the rosters were committed, pushed and invisible, with **no error surfaced anywhere**. Erik caught it by looking ("I don't see any of the rosters"). Project settings and the GitHub integration were both healthy, so it was a dropped webhook; an **empty commit re-fired it** and the build went green in ~30s. Cheap check after any push: `curl -sL https://slotab.org/<page> | grep <something new>`, or compare `git ls-remote origin refs/heads/main` against the project's latest deployment SHA. **Related and unresolved:** commits have been landing on `origin/main` via `update by push` **without anyone running `git push`** in the session — worth finding out what is doing that, since it means work reaches the production branch without a deliberate ship step |
| 🟡 **Boys XC roster is a `.xlsx` Gmail attachment — needs a manual hand-off** | Erik → Coach Steinmaus | Nancy attached **`xc roster 2026 boys.xlsx`** on 2026-08-16 ("a solid list of our boys xc team. There may yet be a few changes"). Same wall as the photos in #171: the Gmail connector has **no attachment-download tool**, so it cannot be read from here. Easiest fixes, in order: **re-send it as a Google Sheet link** (that is exactly how the Cheer and Flag Football rosters were reachable in #173), or drop the file somewhere in the repo working tree. Needed per athlete: **first name, last name, grade** — jersey number and event are optional now. Note there is **no girls XC roster** either, and the page runs the two squads as one combined program |
| 🟡 **Cheer: is there a Frosh team, and have the other captains been announced?** | Erik → Coach McDonald | Two loose ends surfaced by the roster in #173. **(1) Frosh.** Her info doc says "We have three **TEAMS** for Fall: VARSITY, JV BLACK, JV GOLD" and supplied three rosters — but `/teams/cheer` carries a **Frosh squad portrait** from the #137 photo drop, and #137 recorded her naming *four* Fall teams. So the page now shows a Frosh photo with no Frosh roster. Either Frosh folded into JV Gold or its roster is simply missing. **(2) Captains.** The doc lists four more beyond the two published — Varsity Spirit Captain **Scarlet McBride**, JV Black **Kassandra Ramirez** + **Grace Fields**, JV Gold **Emily Masingale** + **Scarlet Salomon Gaspar** — marked "will not be announced until later this week" as of 17 Aug. That week has passed, but **an embargo is not assumed lifted**, so they are deliberately not on the page. One yes and they go up |
| ~~🟡 **Dance photos are sitting in Gmail and need a manual drop into `photo-inbox/`**~~ | ~~Erik~~ | ✅ **Resolved 2026-08-20 (#172) — Erik dropped them, all 7 reviewed, 3 used.** Hero is a real Cal Poly Showcase pom routine, plus a second squad portrait and one gallery shot; the other four were rejected on inspection (a multi-team Cal Poly group, a 5-dancer posed shot, a soft video still, and the program flyer). **The 15 MB `SLOHigh_DanceTeam_…jpg` was the same image as `gdance-team-2025.jpg`** at RMSE 0.0017 and was correctly not re-imported — exactly the trap this row warned about. **Still open:** no 2026-27 squad portrait (the one on the page is last season's), and the AGHS Dance Show is still undated. *Original row:* `/teams/dance` shipped 2026-08-20 (#171) with **one** photo — the portrait already in the library. Everything else Coach Mettler supplied is **unreachable from here**: the Gmail connector has no attachment-download tool, and her Drive originals are up to **15 MB**, which cannot be round-tripped through a model context. So this is a genuine manual step, not an oversight. **Two sources:** Phil Angel's 2026-08-18 forward carries **7 attachments** (`Dance National Showcase-266.jpg`, `Dance National Showcase-137.jpg`, `IMG_9030.JPG`, `IMG_6548 3.jpg`, `IMG_1548.jpg`, `1.PNG`, `IMG_6436 2.jpg`), and Mandi's June mail links **9 Drive files**. The two *National Showcase* frames are the most promising **action** candidates, which is exactly what the page lacks. Drop into `photo-inbox/`, then `npm run photo-intake` → `--process`, then **`npm run photo-usage`** — non-optional here, because `SLOHigh_DanceTeam_2025-2026_7RV06387.jpg` is already in the library as `gdance-team-2025.jpg` and byte-level dup checking will not catch a re-export of it (#111/#112) |
| 🟡 **Does Dance compete in Spring, and does it carry a gender designation?** | Erik → Coach Mettler | Two questions #171 declined to guess. **Spring:** half-answered 2026-08-20 (#172) — the Showcase photos carry EXIF **2026-02-17**, so the **Cal Poly Showcase is Winter**, not Spring. `seasons: ["Fall", "Winter"]` stands; only the **AGHS Dance Show** is still undated. If it is a spring show, add `"Spring"` and the team appears in that block automatically. **Gender:** currently omitted, using the optional-gender path from #122. E16 has the AD confirming the spirit squads girls-only (June 2026), but the 2025-26 portrait appears to include at least one student who does not present as female, so "Girls" was not asserted against the photo. Mandi can settle both in one reply |
| 🔴 **NEEDED: first names + email addresses for Dance's liaisons, Connoly and Olsen** | Erik / Board | **This is the specific ask** (logged 2026-08-20 at Erik's request): we have **surnames only**. To publish them on `/teams/dance` we need, for each of the two, a **first name** and an **email address** — the `Liaison` type requires `email`, so a name on its own cannot go on the page. Note the trail: #562 recorded the board's 2026-27 sheet listing Dance's liaisons as **Connoly + Olsen**, the same pair that sat wrongly on the *Cheer* file until #122 replaced them (they were on Cheer only because the two teams shared the `dance` slug then). So **the sheet very likely already has both** — worth pulling it before re-asking the board. Possible lead on the first: Coach McDonald's own info doc names a **Felisha Connolly** \<felishae@gmail.com\> as holding cheer photo galleries, and #122 removed a "Felisha Connoly" from the Cheer file — **same person, two spellings, and not yet confirmed to be the Dance liaison**, so it is a lead to check, not an answer to paste in. Until then `/teams/dance` is the only Fall team page with no liaison and `team-audit` flags it |
| 🟡 **Cross country's wish list is still with Derek Folden** | Erik → Coach Steinmaus / Derek Folden | The **photos are done** — both 2026 camp shots landed 2026-08-20 (#172) and now lead the gallery (18 shots). What remains is the **wish list** Nancy mentioned 2026-08-16, still with team rep **Derek Folden**, who was reorganising it. `cross-country.json` has no `wishlist` block, so `/teams/cross-country` shows **no "Team Wishlist" section at all** — one of the few pages with nothing for a donor to fund by name. Nancy also said "a few more pics coming" beyond the two received, so more may arrive |
| 🟡 **Dance's 2026-27 roster — the flyer has last year's** | Erik → Coach Mettler | `1.PNG` in the #172 drop is the program flyer and carries the **complete 2025-26 roster** (19 names, by row). Not published, for two reasons: it is **last season's** list on a 2026-27 page, and the `roster` type needs **number, year and position** per athlete while the flyer gives only names. If Mandi sends the 2026-27 squad with years, the page gains a Roster section; if she sends only names, the `roster` type would need a change first. The flyer also has a slightly fuller bio than the school page (adds the World Series of Beach Volleyball) — the published bio uses the school-page version, which is current |
| ~~🔴 **Which Square account should take donations?**~~ ✅ **Resolved 2026-08-20 (#164)** — pointed at `6YKJFPPASVNW3`, the account already receiving donations and wired to QuickBooks. Launch unblocked. *Original row:* | Erik → Board + Trina | **Square's AI support refused the lookup on privacy grounds (2026-08-19)** — escalate to a *human* on the phone, not chat. Recovery steps in `docs/square-account-recovery.md`, now including the **nine club mailboxes** to probe via password reset (Square's own suggestion) and the $25-receipt route that turns you into a customer with standing. — the fastest routes are internal (Trina's QuickBooks connector names the account it is authorised against; the bank deposit descriptor identifies it too), with a Square Support recovery pack as the fallback.  Two accounts exist (#162): **`6YKJFPPASVNW3`** owns `slotab-3.square.site` and takes **every donation today**; **`1RZ6X80KDPPNR`** holds `SLOTAB`/`SLOTAB Merch` and issued the production token now in Vercel. **Do not set `SQUARE_LIVE_DONATE=true` until this is settled** — it would move donation income to a different account, likely outside Trina's QuickBooks connector. Needs: who holds `6YKJFPPASVNW3`, which account the connector is attached to, and which bank account each deposits to. Simplest fix is a production token from `6YKJFPPASVNW3` |
| ~~🟡 **Weekly donation report emailed to Trina**~~ ✅ **Built and verified in production 2026-08-21** — Mondays, trailing 7 days, to `slotabtreas@gmail.com` via Vercel Cron (#180). A manual trigger returned `{"ok":true,"email":"sent"}`, which also confirmed the production Square token reads correctly and that Resend accepts mail from the newly verified `slotab.org`. *Original row:* | Erik → Claude | Requested 2026-08-21 to `slotabtreas@gmail.com`. **Where it runs:** a **Vercel Cron** hitting a protected endpoint is preferable to a GitHub Action, because the Square token already lives in Vercel — an Action would mean copying an **unrestricted production payments token** into GitHub secrets, a second home for the most dangerous credential the club holds. **How it sends:** needs an email provider (Resend/Postmark free tier, or Gmail SMTP with an app password on the treasurer mailbox). Also to settle: which day, trailing 7 days or Mon-Sun, and whether it goes to Trina alone or the wider board |
| 🟡 **Sponsorship workflow under live test by the Sponsorship lead** | Sponsorship lead → Erik | Started 2026-08-21. The one path not yet exercised end to end with real money, and the one with the most moving parts: tier selection, sport designation, business name, the Square checkout, **and the webhook that emails the Membership VP** (#177). Each piece is verified in isolation — a correctly signed webhook renders the fulfilment email against real Square data — but nothing has run the whole chain. Watch for: the contact **name** populating (a synthetic test showed *not provided* because it had no cardholder name), the handoff email actually arriving via Resend, and the payment appearing in `/board/square-report` with the sports credited. Any test purchase is a **real charge** and needs refunding in Square |
| 🟡 **Rebuild `/merch` as a real shopfront — waiting on photos + inventory** | Erik → Claude | Agreed 2026-08-21 to defer until **new merch photography arrives and the inventory is listed in the store**. Today `/merch` is a *design gallery plus a button*: the nine sport-specific shirts shown are ordered through coaches and ParentSquare and are **not** store items (#183), so the tiles deliberately do not link. When both land, replace the gallery with the store's actual products — hoodies, hats, spirit tees — each linking to its own listing. The per-item `url` field on `Shirt` already exists (#182), so the work is swapping the data, not rebuilding the page. Store is `slotab-apparel.square.site/slotab-merch` — a third storefront, on the club's second Square account |
| 🟢 **Launching is now a one-value change** | Erik | With a production token in place, `/donate` and `/membership` stay on the old storefront until **`SQUARE_LIVE_DONATE=true`** is set in Vercel (then redeploy — `/membership` resolves at build time). Set it back to anything else to revert instantly. This exists so the board can review the genuine production checkout *before* donors meet it (#149) |
| 🟡 **Set `SQUARE_PREVIEW_SLUG`, then delete the preview route when done** | Erik | `/donate/preview/<slug>` (#146) exercises the new checkout on the live site while `/donate` stays on the old storefront. Set **`SQUARE_PREVIEW_SLUG`** in Vercel to a random string of 8+ characters and redeploy; the route 404s until you do. It runs on **sandbox**, so use Square's test card `4111 1111 1111 1111`. **Remove the route once the production token is in** — it has no purpose after that, and a forgotten test checkout on a fundraising site is a liability |
| 🔴 **Square production token — the last step before real money moves** | Erik | Phase one is built and verified but runs on **sandbox credentials only** (#145). Going live needs the **Production** access token and Live Location ID from the same Credentials page, set in Vercel as `SQUARE_ACCESS_TOKEN` / `SQUARE_LOCATION_ID` with `SQUARE_ENVIRONMENT=production`. **Then redeploy** — `/membership` decides at build time whether to show its checkout buttons (#146), so saving the variables alone will not switch them on. Until that happens the live site deliberately refuses to use the sandbox credentials it already holds, and `/donate` keeps using the old storefront. **Treat the production token like the Square account password** — it is a *personal access token*, which Square documents as unrestricted and able to "impersonate the account owner", including refunds and payouts. There is no narrower option short of OAuth. Record who generated it in the Board Hub so it can be rotated at handover — the same gap that left nobody knowing who holds the GoDaddy account |
| ~~🟡 **Ask Trina how she actually reconciles Square today**~~ ✅ **Resolved 2026-08-19 — a QuickBooks connector.** Which means line-item naming does not reach her at all, and the per-sport allocation has to come from `npm run square-report` instead (#161). *Original row:* | Erik → Trina | #144 established that the **line item name** is the only thing that carries, and #145 now writes a precise one for every gift. What is still unconfirmed is what Trina reads: **Square's own item-sales reporting** (which shows those names) or a **QuickBooks connector** (the common ones post a *daily summary* with no line detail, and Intuit's own guidance says customer names and emails are not imported). If it is the latter, the naming work buys less than it looks and the reconciliation sidetool matters more. One conversation decides how much of the Phase-0 sidetool is still needed |
| 🟡 **Rate limiting is a speed bump, not a gate** | Claude | `/api/square/payment-link` mints real Square orders and ships with an **in-memory, per-instance** limiter (10/min/IP). On Vercel each instance has its own memory, so a spray across instances gets more through, and it resets on cold start. The realistic threat is nuisance rather than theft — no money moves without a card, so abuse means junk DRAFT orders polluting the Treasurer's Square reporting. The real fix is the shared counter (Vercel KV / Upstash) that **#73 already called for** and deferred; the limiter's call signature was written to be swappable for it |
| ~~🔴 **Square storefront is selling the pre-#88 sponsorship ladder — re-tier it**~~ ✅ **Done 2026-08-19 (#163)** — all seven tiers verified matching to the dollar. *Original row:*  | Erik → Board (with the Sponsorship lead) | Found while wiring the business enrolment path (#143), and it involves real money on a live campaign. The storefront's **Business Sponsorships** category still carries **Platinum / Gold / Silver / Bronze** at the old prices and perks: **Gold is $2,500** with a one-year banner and four passes (this site sells Gold at **$5,000**, two-year banner, eight passes), and **Platinum is $5,000** carrying the perk list this site sells as **Champion at $10,000**. **Bronze no longer exists** on our sheet at all. Any business that reaches those items today buys roughly half the package the website promised them, or the club under-charges by $5,000. Enrolment is routed through the board for now rather than deep-linked, so nothing is mis-sold — but **the storefront and the website are two published SLOTAB surfaces contradicting each other on price**, which is worth closing quickly. Once the items match the 2026-27 sheet, the tier cards can deep-link per tier exactly as the sport donations do (#140) |
| 🟡 **Align the storefront's donation amounts with ours — a storefront edit, no code** | Erik → Board | From #143: the Square items offer **$10 / $20 / $50 / $100**; our donate ladder is **$25 / $50 / $100 / $200 / $500 / $1,000 / $5,000**. Only **$50 and $100** line up, so a donor who picks $25 or $200 lands on a page with no matching button and has to find the $0.00 “enter your own amount” option — which is exactly the confusion #142 was raised about, one step further along. Square **cannot** be made to pre-fill the amount from a URL (tested and ruled out in #143), so matching the two ladders by hand is the only fix available before the Q1 API decision |
| 🟡 **The Business Sponsorships lead has no published email** | Erik → Rich Ungeheuer | `board.json` lists **Rich Ungeheuer — Go Fan Passes & Business Sponsorships** and **Anne Ramberg — Sponsorships Assistant**, neither with an email. The new business enrolment panel (#143) therefore routes enquiries to **slotabmembership@gmail.com**, which works but sends business sponsorship to the membership inbox. A dedicated address — or Rich's own — would put enquiries in front of the person who owns them. Part of the wider “11 board rows with no email” gap from #135 |
| 🔴 **The old WordPress site is still live — cancel GoDaddy hosting** | Erik | Confirmed 2026-08-18 (#141): the pre-cutover site still answers **`200` over HTTPS** at the old GoDaddy IP `160.153.0.90`, serving WordPress under a valid slotab.org certificate. The runbook’s *“cancel GoDaddy hosting 48h after go-live”* step was never done, so the club is still paying for it and an **unpatched WordPress install is still exposed**. DNS no longer points there so nothing routes to it in normal use — but it is the origin of the 31-day cache headers still ticking through parents’ browsers, and it should not outlive the migration. **Gated on the same blocker as the domain:** nobody yet knows which GoDaddy account holds it (see the account row below) |
| 🟡 **Comms: one reload fixes it, and `www.slotab.org` sidesteps it entirely** | Erik → Board | From #141: anyone who loaded the old site shortly before the 2026-08-15 cutover can still be served it **from their own browser cache**, for up to 31 days from that visit — so it clears itself by roughly **2026-09-15** with nobody doing anything. If a message goes out meanwhile, keep the ask small: **one ordinary reload** replaces the stale copy permanently — not “clear your cache”, not a hard-refresh. Two things now work in the background: every old URL redirects instead of 404ing, and on Chrome / Edge / Firefox / Android clicking any old link silently evicts the stale copy. **Safari ignores that mechanism**, so iPhone users are the ones most likely to still need telling |
| 🔴 **Spot-check the live Donate handoff in a real browser** | Erik | The interim Square wiring (#140) could not be exercised in the preview pane — scrolling frozen, React unresponsive to synthetic events. **Please click through once on slotab.org**: fill the form, hit Donate, confirm the correct sport item opens on Square and the follow-up panel appears. Same verification gap as #101's sponsorship marker |
| ~~🔴 **Q1 — Square API vs hosted storefront**~~ ✅ **Resolved and live 2026-08-21.** Hybrid: Checkout API payment links for one-time gifts and sponsorships (#145), pointed at the account that actually takes the club's money (#164), approved by Trina and switched on — a real donation completed. Recurring remains out until the Subscriptions API (#154). *Original row:* 🔴 **Q1 — Square API vs hosted storefront** | Erik → Board (with Trina) | **The real fix behind #140's stopgap, and it was missing from this register entirely** until 2026-08-17 — it lives in the Active Backlog and the Q-table, not here. Gated on **#31**: does Square support per-transaction custom metadata that survives into QuickBooks reports? That answer decides API vs hosted, which in turn unblocks the donate rebuild, the reconciliation sidetool, the transaction-ID schema, the refund path and recurring-donation intent. Until then the storefront takes the money but monthly giving is not possible at all |
| 🟡 **JV Black and JV Gold squad photos** | Coach McDonald → Erik | The JV split is new for 2026-27, so last season's shoot has a single combined JV portrait (#139). The Fall cheer page now states the split and what each squad supports; **two separate squad photos** would replace that combined one with a proper Black and Gold pair. No rush — the page reads correctly meanwhile |
| ❓ **Seven Tiger Cheer stunt videos — where do they go?** | Erik / Board | **Ruled out as a TCC photo source 2026-08-17 (#138):** Erik asked whether stills could serve as TCC action shots. Sampled the start, middle and end of all seven clips — **14 frames, every one shot on the running track beside the football field**, daylight, stadium and hills behind, crowd in the stands. That is *fall sideline* cheer at a game; TCC competes indoors on a mat, so stills would misrepresent the team the same way the dance photos did. Independently, the clips are **768x432** (some 432x768) against a 1200px standard, so even for the sideline pages a still would be a downgrade on the four high-res action photos the Fall page already carries. The original question stands:  The coach's 2026-08-17 drop included **7 MP4 clips** (Kick Full Basket Toss, Double Rewind Pyramid with Bajas, B E A T with Stunts…), ~20 MB, and they were **not used** (#137). Team pages have no video support, and #59 deliberately made Hudl the video platform rather than building a custom catalogue. Options: put them on Hudl, host them on the team's own site and link out, or build video support — the last being a real feature, not a data drop. They are the single best evidence for the *elite and athletic* framing the coach asked for, so worth answering rather than dropping |
| 🟡 **TCC competition action shot — only the coach can supply this** | Coach McDonald → Erik | **Confirmed 2026-08-17 (#138)** that nothing already in hand can fill it: the stunt videos are sideline footage too, so there is no TCC competition imagery anywhere in the drop. A single photo from a competition would close it. Everything in the 2026-08-17 drop is **sideline** cheer — fall on the football field, winter in the gym — while TCC competes on a mat (#137). Its hero stays photoless and its card stays a posed portrait. A single competition photo would fix both |
| 🔴 **Five fall coaches have never replied** | Erik | Surfaced by an inbox audit (#132). No response to the 6 Aug request from: **Dennis** (tennis, tenniswithdennis40love@gmail.com), **Kennedy Meaney**, **Pat Johnston / Graham Gilmore / Rick Cramer**, **Dan Berkeland** (XC girls — note his co-head Nancy Steinmaus replied in full), and **Trent Rubio**. Also **Josh Heptig**, whose first send bounced on a mistyped address before being resent — worth confirming the resend landed. Their pages are running on whatever was there pre-launch |
| ~~🟡 **Cheer: Frosh squad + "elite athlete" framing**~~ | ~~Erik → Coach McDonald~~ | ✅ **Resolved 2026-08-17 (#137, #139)** — the Fall page shows three captioned squad portraits and leads with a stunt action shot. The four-teams-three-photos puzzle is explained: **JV Black / JV Gold is a new 2026-27 split**, so no separate photos exist yet — the page states the split and what each squad supports (#139). *Original note:* Two items from her 13 Aug brief that #132 could not close. Her Fall structure is **four** sideline teams — Varsity, JV Black, JV Gold and **Frosh** — but the intake doc listed only three and the site has no field for squad names short of rosters or per-squad photos. Separately she asked the page to read as *elite and competitive* rather than "spirit squad", which is really a photo-selection question and waits on her action shots. Worth asking whether per-squad team photos would cover both |
| 🔴 **September board meeting — the 14th or the 16th? Zoom or Teams?** | Erik → Andria (President) | The President's SLOHS announcement says **"September 16th at 6 pm at Cannon Corp"** joinable **via Teams**; the site says **Monday September 14**, "(Zoom available)", noted as *moved a week for Labor Day* (#131). **Sept 16 is a Wednesday**, breaking the first-Monday cadence every other 2026-27 meeting follows — so this looks like it may be a slip in the email, but that is a guess and the site was left unchanged. Both the date and the Zoom-vs-Teams question need one answer from Andria. The announcement has already gone out to the whole athletics list, so whichever is right, the other audience needs correcting |
| 🟡 **Athletic department hasn't posted this week's schedule tab** | Erik → SLOHS Athletics | **Diagnosed 2026-08-16 (#133): the scraper is fine, the sheet is empty.** The only live tab is still **"Aug 10 - Aug 15"** — a week that has ended — and its CSV contains a header row and nothing else. The cron is running on schedule and parsing correctly, so `/upcoming` is falling back to the static schedule import and carrying no department-sourced games. The SLOHS announcement points parents at that weekly sheet as the authority, so it is worth a nudge to whoever maintains it. Nothing to fix on our side; the next run after they post will pick it up |
| 🟡 **Do we want match results on the site?** | Board (product call) | The SLOHS announcement carried results — girls volleyball swept Morro Bay and beat Stockdale 3-0, tennis and football scrimmages (#131). **There is nowhere to put them:** `events.json` holds fixtures with no score field, and decision #40 deliberately keeps comms-channel features off team pages with Home Campus as the source of truth. So results were dropped rather than shoehorned. If the board wants a results or news strip, it is a feature to scope — including who would keep it current every week, which is the part that usually decides it |
| ~~🟡 **"Varsity Girls Flag Football" — is it one team or two?**~~ | ~~Erik → Coach Monroe~~ | ✅ **Answered 2026-08-20 (#173) — two squads, and the roster proved it.** Coach Monroe's roster sheet has **two tables: Varsity (17) and JV (22)**, which matches the JV coaching staff that raised the question. Page renamed to **"Girls Flag Football"** and both squads are published, so the team file finally agrees with `teams.json`, the nav and the `/teams` card. **Still open, minor:** he signs himself **Dan** while the site says **Daniel**; the sheet spells one athlete **"Joise Winter"**, which looks like "Josie" but was transcribed as written rather than corrected on a guess; and captains are still "working on this". *Original row:* The page is titled **Varsity** Girls Flag Football, but the staff list he supplied includes two **Co-JV Head Coaches** and a **JV Assistant** (#130), so JV coaches now sit under a Varsity heading. Either the program fields both squads and the page should drop "Varsity" — the nav and `/teams` card already read just "Girls Flag Football" from `teams.json`, so only the team file disagrees — or JV wants its own page. Same shape as the Cheer-vs-Dance question below. Also worth asking: he signs himself **Dan**, the site says **Daniel** |
| 🟡 **Flag football: captains, roster, links, team photo still to come** | Coach Monroe → Erik | All four were marked in-progress on his intake form (#130), so nothing was invented. **Picture day was 8/19** — the team photo should exist by now and is worth chasing, since the page currently uses last season's. Roster is "mostly complete", pending uniforms and final positions — and is gated behind the roster policy question below regardless |
| 🟡 **Confirm three sponsor names against their own artwork** | Erik → Cheer / sponsors | The names supplied differ from what the logos themselves say (#129), and a sponsor's listing should match how they present themselves: "SLO Bank" → **SLO Credit Union** (already corrected), "Crocker Construction" → **Crocker Coastal Construction**, "San Luis Heart" → **San Luis Obispo Heart**, "Primary Eye Care" → **Primary Eyecare Center**. The artwork spellings are live; worth a confirmation, especially for a $2,500 and two $1,000 relationships. Also: Crocker's banner has a **yellow ground** rather than a transparent or white one, so its tile reads as a colour block beside its neighbours — fine if that's their banner, but they may have a plain-background version |
| 🟡 **Add an asset check to the build, so a missing alpha logo fails loudly** | Claude | Twice now a sponsor logo has shipped without its `/sponsors/alpha/` counterpart and 404'd on the live homepage — once mine (#129), twice pre-existing. `next build` cannot catch it: the path is a plain string and a missing `public/` file fails silently at runtime. A tiny script walking `sponsors.json` and asserting both files exist, wired into `npm run build` or CI, would end this class of bug permanently. Same idea would cover team-page photo paths |
| ~~🔴 **SLO Credit Union logo — needed as a FILE on disk**~~ | ~~Erik~~ | ✅ **Resolved 2026-08-16 (#128)** — Erik dropped it into `photo-inbox/`; trimmed, placed at `public/sponsors/gold/SLO-Credit-Union.png` and live on the Gold wall linking to slocu.com. *(The inbox copy is left in place per the intake workflow — safe to delete.)* *Original note:* Name and tier are settled: **SLO Credit Union** (not "SLO Bank") at **Gold** ($5,000), confirmed by Erik 2026-08-16. Still blocked on the artwork. The logo was pasted into chat as an inline image and is **not in either attachment** — the intake `.docx` has no `word/media/` at all, and the XC `.zip` contains no logo file. An image visible in a conversation cannot be written to disk, so it has to arrive as an actual file (save it to `~/Downloads`, or drop it in `photo-inbox/`). Target `public/sponsors/gold/`, matching the existing wall (Logo-Engle-grey.png is 500x171, Cool-cats.jpeg 482x361). A transparent PNG or SVG is best; the supplied square lockup with "CREDIT UNION" beneath the mark is the right asset — preferable to pulling one off their website, which risks a different variant and brand-usage questions |
| 🟡 **Spot-check older photos for silent EXIF rotation** | Claude | `photo-intake` stripped EXIF orientation without `-auto-orient` until #127, so **any previously processed photo whose camera recorded rotation as a flag rather than in pixels may be live and sideways**. It would look obviously wrong, so nothing subtle is hiding — but nobody has swept `public/photos/` for it. Worth a pass over portrait-orientation shots in particular. Small job, no rush, but it will not fix itself |
| ~~🟡 **17 XC state-meet photos awaiting curation**~~ | ~~Erik + Claude~~ | ✅ **Resolved 2026-08-16 (#127)** — classified from a contact sheet rather than filenames: 9 boys, 7 girls, 1 coaching-staff shot. All 17 processed into `public/photos/` and live; 16 action frames in the XC gallery, the posed coaches photo in `teamPhotos`. Erik to inspect and correct any misreads |
| 🟡 **Publish athlete rosters, or not? — Tiger Cheer would be first** | Board (policy call) | **Now two teams waiting:** Cheer supplied 48 names, and XC boys supplied 44 (#126) — whose spreadsheet also carries **Student IDs**, which must be stripped at intake and never reach the repo regardless of the outcome. Cheer supplied a **48-name roster** across Varsity / JV Black / JV Gold with positions and grade levels (#125). **No team on the site currently publishes athlete names** — the `roster` field exists in `TeamPage` but is unused by every team file — so saying yes sets precedent for minors' names + grade on a public page, and other teams will reasonably expect the same. Two sub-questions if approved: whether grade level is included, and that `RosterEntry` needs reshaping since it requires a jersey `number` that cheer athletes don't have (positions are Base / Flyer / Backspot / Tumbler). Also parked: the team's **core values and sayings**, which have no field on the team page yet |
| ~~🟡 **Real Tiger Cheer photos**~~ | ~~Cheer coach (Lisa McDonald) → Erik~~ | ✅ **Resolved 2026-08-17 (#137)** — 12 labelled photos supplied and live: fall and winter action heroes, three Fall squad portraits (Varsity / JV / Frosh), a winter Varsity portrait and TCC's official squad photo. **One gap remains, tracked below:** no TCC *competition* shot. *Original note:* **Partly closed 2026-08-17 (#136):** the Fall page now has a verified team portrait (`ccheer-team-2025.jpg`, which carries "2025 VARSITY CHEER" printed in the image). **Still needed: action shots** — all three cheer pages run photoless heroes, and TCC in particular is represented only by posed line-ups, which undersells exactly the athleticism the coach asked us to convey. Also worth confirming which squad portrait is the *winter* team. *Original note:* Erik identified the banner and team portrait on `/teams/dance` as **dance team** photos; both were removed 2026-08-16 (#123), so the page now runs with a **black photoless hero and no Meet the Team section** until replacements arrive. Coach has been asked. Needed: **one action/game shot** for the hero and **one posed squad portrait** for Meet the Team — per the standing rule (#72/#85), an action shot for the hero, never a portrait. The one remaining photo (`ccheering-sideline-2025.jpg`) is unverified by the team, so it is worth confirming that one is cheer too while asking. Note the overlap with **Cheer vs Dance** below: if Dance becomes its own team, the two pulled files are its photos, not deletions |
| 🟡 **Do the four section events actually record on the Hobby tier?** | Erik (Vercel dashboard) | Pageview analytics is confirmed collecting (#124), but the **custom events are not**. `section_first_view` (membership / sponsorship / donate / volunteer) and `returning_visitor` are the numbers the board actually asked for, and they surface in an **Events** panel that wasn't visible in the dashboard screenshot. The project is on **Hobby**; custom events may be paid-tier. **Check:** scroll the Analytics page for an Events section listing those two names. If absent, it is a plan decision — the tracking code is correct and needs no change. Also worth confirming Hobby's **retention window and monthly event cap**, since a shorter window means no full-season or year-over-year comparison |
| ~~🔴 **Enable Vercel Web Analytics — the board still has no readable numbers**~~ | ~~Erik (Vercel dashboard)~~ | ✅ **Resolved 2026-08-16 (#124)** — Erik enabled it; dashboard confirms 10 visitors / 52 page views on `slo-tab-website` with the curve starting Aug 15-16. The earlier `404` was the Vercel connector's token lacking analytics read scope, not the site. Custom events split out into the row above. *Original note:* Erik enabled it, but **verification failed** — the Analytics API still returns `404 Web Analytics not found` for `slo-tab-website`, queried by both project ID and slug, before *and* after generating fresh production traffic with consent granted. The client half is confirmed good: `script.js` `200`, `/view` `200`, and `/event` `200` (a `returning_visitor` custom event fired on the second visit), so the site is emitting correctly. Most likely causes, in order: **(a)** *Speed Insights* toggled instead of *Web Analytics* — adjacent in the dashboard and easy to mix up; **(b)** enabled under a different Vercel account, since the connector sees only `eramberg-9955's projects`, which holds both `slo-tab-website` and `ravens-peak-consulting` and both 404 — and #102 already found the "Raven's Peak team" framing in #67/#69 didn't match reality; **(c)** the connector token lacking analytics read scope, less likely as that usually 403s rather than 404s. **Settle it in the dashboard:** project → **Analytics** tab — a chart or "Collecting data…" means on, an "Enable" button means off; confirm the tab is *Web Analytics* and not *Speed Insights*. *Original note:* the tracking from #101 has been live since 2026-08-11 but **Web Analytics was never switched on**, so every `section_first_view` and `returning_visitor` event has been accepted with a `200` and discarded (#119). Vercel → `slo-tab-website` → **Analytics → Enable**. **Nothing is retroactive** — the count starts from the moment it is flipped, so the sooner the better if the board wants a full first season. Until then the only visible data is a single visitor's own record on `/privacy`. Worth a sanity check a day later that `section_first_view` rows are appearing for membership / sponsorship / donate / volunteer |
| ~~🟡 **Uncommitted `/board/cms-access` work — ship or discard?**~~ | ~~Erik + Claude~~ | ✅ **Resolved 2026-08-16 (#118)** — reviewed against current `main` the same sitting it was surfaced and shipped in part: the page + hub link went in, the `config.yml` and `editor-onboarding.md` hunks were dropped as superseded, and `README.md` + `decap-setup.md` applied cleanly (the latter fixing a wrong-repo error that was still live on `main`). Nothing outstanding remains off `origin/main`. *Original note:* sitting **uncommitted** in the `.claude/worktrees/hopeful-ramanujan-745dc7` worktree since **2026-05-18**, branched **100 commits** behind current `main`: a new `src/app/board/cms-access/page.tsx` plus edits to `README.md`, `docs/decap-setup.md`, `docs/editor-onboarding.md`, `docs/project-status.md`, `public/admin/config.yml`, and `src/app/board/page.tsx`. Nothing equivalent exists on `main` — the page was never shipped. Surfaced during the post-launch push inventory (#117); it is the **only** outstanding work not now on `origin/main`, and being uncommitted it has no backup. Much of the doc side is likely superseded by the CMS-onboarding refresh (#87) and the Word-guide work. **Next step:** rebase that worktree onto current `main` and review what still applies before anything ships |
| 🔴 **Renew slotab.org / confirm auto-renew** | Erik + GoDaddy billing holder | **Expires 2026-09-19** (39 days from 2026-08-11). If it lapses the site goes down and the DNS records go with it. Confirm auto-renew is ON and the card on file is current — the board just changed hands (#113) |
| 🔴 **Which GoDaddy account holds slotab.org? — BLOCKER** | Board (Trina / Liz / Kiwi / Jen Melton) | The **SLOTABSEC@gmail.com** account Erik was given is **completely empty** — no products, domains, renewals, or delegate access — so the domain lives in an unknown account. Hosting reverse-resolves to `secureserver.net`, so registration + hosting are almost certainly together. Best leads: the annual GoDaddy **charge** (Treasurer's books) and the **renewal notices** GoDaddy is sending right now. **Gates the whole cutover, and the 2026-09-19 renewal.** Escalation path + the permanent fix (Account Change into a SLOTAB-owned account, Delegate Access, record it in `/board`) in `docs/dns-cutover-runbook.md` (#113) |
| ~~🔴 **Real Impact ledger data**~~ | ~~Treasurer (Trina)~~ | ✅ **Resolved 2026-08-11 (#103)** — Erik supplied the FY 2025-26 QuickBooks `Transaction Report` + `Statement of Activity`; reconciled to $210,414.79 and shipped the same day the draft items were cleared (#94), so `/impact` never showed the "being compiled" note in production. **Caveat:** the exports give *disbursements*, not itemized purchases — 55.6% is per-team allocations with no item detail. Successor ask is on the athletic department, not the Treasurer — see the Impact backlog |
| 🟡 **Rest of the 2026-27 SLOTAB events** | Board | ✅ Board meetings done (#97/#98) — all ten, Aug 2026–May 2027; August moved to Mon Aug 10 and is recorded as held, venue confirmed. ✅ ~~fall parent meeting~~ — **resolved 2026-08-16 (#131)**, Mon 17 Aug 6:00-8:00 PM from the SLOHS announcement. *Still needed:* **physicals night**, **spring fundraiser** |
| ❓ **"Donate a ticket to a coach" — do we want it?** | Erik / Trina | Trina raised it 2026-08-11 (#98) as a possible Booster Bash option. Product decision, not implemented |
| ~~❓ **Dance needs its own page — Cheer has vacated the slug**~~ | ~~Erik / Board~~ | ✅ **Resolved 2026-08-20 (#171) — `/teams/dance` is live and Mandi Mettler is on the site.** Built from her own 2026-08-18 mail plus the school Dance page she pointed at. Removing the `permanent: true` redirect #132 left in `next.config.ts` was the actual work; the data file was the easy half. **Two smaller questions spun out of it rather than being guessed** — whether Dance also competes in **Spring** (only Fall + Winter are claimed) and whether the team carries a **gender designation** (currently omitted). Both are one reply from Mandi, tracked below. *Original note:* **Reframed 2026-08-16 (#132).** The original question is settled: Coach McDonald confirmed Cheer and Dance are separate programs, and Cheer has moved off `/teams/dance` to `/teams/cheer`, so the slug is free for the real Dance team. What remains is that **Dance has no page and Coach Mandi Mettler is listed nowhere**, while the board's own 2026-27 sheet lists her. Phil Angel identified Mandi as the dance director in his 16 Aug mail. *Original note below.* |
| ❓ ~~**Cheer vs Dance — one team or two?**~~ | Erik / Board | `main` merged them into one **Cheer** team on the `/teams/dance` slug (its #92) and retired the competitive-cheer page. But the **board's own 2026-27 sheet lists them separately**: Cheer (Lisa McDonald) *and* Dance (Mandi Mettler, liaisons Connoly + Olsen). Main's structure was kept on merge (#98) and the sheet's confirmed Cheer liaison applied — so **Dance currently has no page and Coach Mettler isn't listed anywhere**. Needs a call |
| **Booster Bash flyer + official copy** | Fall Fundraiser / Erik | `/booster-bash` is live with real date, venue, ticket prices, cornhole entry, auction-donation and volunteer links, and the QR tag. Artwork and final wording drop in at /admin → Featured Event Pages (#98) |
| **Booster Bash volunteer roles** | Fall Fundraiser | Trina flagged the gaps 2026-08-11 (#98): **silent-auction coordinators, a live auctioneer, and an MC**. The page now asks for all three via `slotabboosterbash@gmail.com` |
| 🟡 **Per-sport streaming list** | Head coaches (via Erik) | **The AD deferred to each head coach** (#97) rather than setting a blanket policy, so #60's drafted defaults are per-coach choices now. Awaiting the complete list; `/watch` already tells visitors coverage varies because each coach decides |
| ⏳ **Hudl multi-camera install date** | Erik / AD | **TBD as of 2026-08-11** (#97) — `/watch` says "Install date to be announced" |
| **Girls Tennis schedule** | Coach / AD | MaxPreps has **no** girls tennis schedule, so nothing was invented (#96). It'll appear once SLOHS posts it to the weekly sheet |
| 🟡 **Board emails — 11 rows have none, not just one** | Board | **Reframed 2026-08-17 (#135).** This row named only the BBQ Coordinator, but **11 board entries carry a name and no email**: Secretary, Go Fan Passes & Business Sponsorships, Apparel, Concessions, Member at Large, Spring Fundraiser *(listed as “Open — volunteer needed”)*, Sponsorships Assistant, Sports-Specific Apparel, BBQ Coordinator (Amber Whighton), Newsletter, and Website. Worth collecting in one pass rather than one at a time |
| 🟡 **Merch online-store URL + fall shirt designs** | Apparel | **Confirmed still open 2026-08-17 (#135)** — `STORE_URL` is still `""`, so the "Shop SLOTAB Merch" button stays hidden; the gallery still holds only spring art. Note the nav's **Shop** link points at `/merch`, so visitors land on a page with nothing to buy (#94) |
| **Remove the password from the board spreadsheet** | Erik / Board | The *2026-27 Board & Liaisons Coaches* sheet has a live Gmail password in the Fall Fundraiser row. Not in the repo and never will be — but it should come out of the sheet and be rotated (#95) |
| 🟡 **Move `/admin-portal` behind the board login** | Erik | **Confirmed still open 2026-08-17 (#135)** — the route exists and is publicly reachable. Verified genuinely empty: it renders "Stub mode — Springly not connected yet" and reads `springly-directory.json`, which holds **0 members and 0 sponsors**. So nothing is exposed today, but it must be gated before it ever holds live records (#94) |
| **Verify the `$1.4M raised since 2012` home stat** | Board / Treasurer | Unverified front-page claim carried over from the old site (#94) |
| **Boys Water Polo roster + team photos** | Coach Bryan Traverso | Promised 2026-08-11 — roster "by Saturday", photos after team pictures (#95) |
| ~~GitHub org name~~ | ~~Board~~ | ✅ Resolved — repo is at **`SanLuisObispoTAB/website`**. *(Note corrected 2026-08-17: it still read `eramberg/slo-tab-website`, the pre-transfer path, long after the move in #67.)* |
| **2–3 board editor GitHub usernames** | Board | Get Write access on the new repo for Decap CMS commits |
| ❓ **Thermometer goal $ — but is the thermometer still wanted?** | Erik / Board | **Queried 2026-08-17 (#135): there is no thermometer anywhere on the site.** No component, no markup, nothing — so this row asks for the goal number of a feature that was never built. Either it should be built (and then it needs the number, reference ~$75K: Huddle + T-shirts + senior banners) or the row should be closed. Not a data gap, a scope question (#32) |
| ❓ **Monthly team-totals report — has this cadence started?** | Treasurer (Trina) | **Queried 2026-08-17 (#135).** `/impact` was ultimately built from the year-end QuickBooks exports (#103), not from a monthly drop, so it is unclear whether this recurring report ever began or is still expected. Worth confirming before the September meeting (#33) |
| ❓ **Wine meeting outcome — already answered?** | Erik / Board | **Queried 2026-08-17 (#135).** This is listed as open here but **struck through as resolved (Q7) earlier in the same document**, and #88 shipped the official 2026-27 sheet described as resolving the wine-meeting/Q7 tier decision — with the ladder settled as Champion / Gold / Silver / Tiger Pride / Varsity rather than the mooted "Supporter / Fan / Booster". If the naming and the 4-year auto-renew question are genuinely closed, this row should go (#37, Q7) |
| **Sponsor brochure PDF** | Sponsorship Development | Link inside the "Become a Sponsor" expandable (#34) |
| ~~**Platinum-tier sponsor list**~~ | ~~Sponsorship Development~~ | ✅ **Closed 2026-08-17 on inspection (#135)** — the tier was renamed **Champion** in the 2026-27 re-tier and is populated (Dairy Creek, Five O Driving School, Crew Wealth Management), driving the carousel as intended |
| ~~**HOF ceremony date + venue**~~ | ~~HOF Committee~~ | ✅ **Closed 2026-08-17 on inspection (#135)** — `hof.json` carries `Class of 2026 Induction`, **October 10 2026**, venue **The Octagon Barn**, corrected 2026-08-11 from the earlier Oct 3 / Holland Ranch. Nothing outstanding |
| ~~**HOF Booster Bash Tickets URL**~~ | ~~Board~~ | ✅ **Closed 2026-08-17 on inspection (#135)** — real Auctria link live (`auctria.events/SLOHSathleticsboosterbash`), the same vanity URL the printed QR encodes. **`hof.json` now contains zero `#` placeholders**, so the 'replace the dead link' framing is obsolete |
| 🟡 **HOF Fund Donate URL** | Board | **Confirmed still open 2026-08-17 (#135)** — but the framing was wrong: `donateUrl` is `""`, not `#`. The page **hides the CTA entirely** until a real URL is set, so nothing is broken in public; the donate route simply doesn't exist yet |
| 🟡 **HOF Committee roster** | HOF Committee | **Confirmed still open 2026-08-17 (#135)** — `committee` is an empty array, so the section self-hides. No placeholder text is showing publicly |
| 🟡 **Alumni Membership pricing + join URL** | Board | **Confirmed still open 2026-08-17 (#135)** — the title and blurb are written and live; `joinUrl` is `""` so the CTA is hidden, and no price is named anywhere. Rolls into the wine-meeting question above |
| **Real per-team rosters** | Team coaches/liaisons | Adam Basch (outgoing AD) to drive outreach during handoff to Phil. Replace placeholder in `data/teams/<slug>.json` |
| 🟡 **Team wishlists — 4 of 20 teams** | Team coaches/liaisons | **Re-measured 2026-08-17 (#135):** cheer, competitive cheer, flag football and boys water polo. XC's is drafted and sitting with Derek Folden. This coverage number is what gates the aggregate wishlist section on `/impact` — a 'balanced selection' drawn from four teams would read as favouritism (#40) |
| 🟡 **Team liaisons — 8 of 20 still missing** | Board | **Re-measured 2026-08-17 (#135):** 12 of the 20 team pages now carry a real liaison, so this is well past half done rather than untouched. Outstanding: **boys-basketball, boys-soccer, boys-wrestling, flag-football, girls-tennis, girls-wrestling, stunt, track-field**. Sourced from the comms kit; surfaced on team pages |
| ~~**SLO Tiger lacrosse + field-hockey photos**~~ | ~~Board / parents~~ | ✅ Resolved 2026-05-13 — both sports removed (club, not on official SLOHS list — #56); placeholder photos purged |
| ~~🔴 **Owner-level Hudl Pro credentials**~~ | ~~Erik (#48)~~ | ✅ Resolved 2026-05-13 via Hudl Support-built BlueFrame embed (#59). vCloud admin access secured; per-broadcast `<blueframe-app>` widget covers live + upcoming + archive |
| ~~🔴 **Sample Hudl per-game embed URL**~~ | ~~Erik (#48)~~ | ✅ Resolved 2026-05-13 — confirmed `vcloud.hudl.com/broadcast/embed/<id>` iframe format with SLOHS broadcast 4031110 |
| **Hudl Archive / Publish-to-Viewer workflow** | Erik (open Hudl support thread) | Each new broadcast must be moved into "Archived" viewer status to appear in the BlueFrame portal — the per-broadcast "Available" toggle alone embeds-by-ID but doesn't list. Need the UI control + ideally a per-account default-on (#59). |
| **SLOHS coach decision on per-sport public-vs-No-Scout defaults** | Adam Basch + Phil Angel + sport coaches | Drafted policy in #60 — public for 12 individual/judged sports, No-Scout for 13 team-strategic sports, wrestling borderline. Coaches confirm or override per sport |
| ~~**2025-26 Spring team portraits**~~ | ~~Board / parents~~ | ✅ **Largely closed 2026-08-17 on inspection (#135)** — of the 20 teams that have pages, **18 carry a portrait**. Only **`track-field`** and **`cheer`** lack one, and cheer's is already tracked above. The other sports this row named — softball, beach volleyball, boys volleyball, boys swim & dive, boys tennis — **have no team page at all** (`hasPage: false`), so they were never going to show a portrait. Original note: Fall + Winter portraits delivered 2026-05-13; Spring still pending the next batch |
| 🔴 **Owner-level Springly Serenity credentials** | Erik (#48) | Serenity tier confirmed; admin login lacks Integrations tab → Owner role needed. *Tabled 2026-05-06* until secured |
| 🔴 **Existing Springly membership data** | Board | Currently in Google Docs; bulk-import after creds arrive (depends on #48) |
| 🟡 **Sponsor websites — only 5 of 54 have one** | Sponsorship Development | **Re-measured 2026-08-17 (#135):** just **5 of 54** sponsors have a `website`, so 49 logos are still unclickable. Barely started, not nearly done — worth knowing before promising sponsors the link. Add the `website` field in `data/sponsors.json` (#35) |
| ❓ **Shared Events Google Sheet — superseded?** | Erik / Board | **Queried 2026-08-17 (#135).** The intent was one sheet as the single source of truth with a daily sync. What actually shipped is a **weekly cron against the athletic department's own schedule sheet**, plus hand-maintained SLOTAB events in the CMS. That may already be the answer in practice — worth deciding whether this row describes something still wanted or a superseded plan (#39) |

---

## 2026-05-06 evening session — what shipped

Long working session running through the Claude Design handoff and
~15 rounds of design fidelity. Site at `slo-tab-website.vercel.app`
is now ready for the 2026-05-11 cocktail board demo at the Hub.

### Tiger design system (new)
- Source Serif 4 (variable font, `opsz` axis enabled) + Manrope +
  JetBrains Mono via `next/font`. Headlines weight 650 with
  `font-optical-sizing: auto` for the lighter editorial display cut.
- Token system: gold `#f5b800` · black `#0a0a0a` · cream `#fbf7ec` ·
  paper `#f3ecda` · bone `#e9e0c8` · graphite `#454340`. Square
  everywhere, hairline borders, no shadows except dropdown menu.
- Full design system in `src/app/tiger.css` (~2200 lines).

### New chrome on every page (replaces legacy Header / Footer)
- `SiteBanner` — black bar with serif "SLO | TAB" wordmark + cream
  tagline, gold border-bottom.
- `TopBar` — score-ticker marquee (5 hardcoded items for now;
  CMS-driven post-demo).
- `TigerNav` — sticky glassy nav with hover dropdowns, JS-managed
  open/close so dropdowns close on link click. Top-right pill is
  "Join" (links to `/membership`).
- `TigerFooter` — dark four-column footer.

### Classic homepage (full rebuild)
- Hero photo carousel, 5 slides, 9.5s rotation, per-slide
  object-position anchors, mask-gradient sidebars fading into the
  dark hero bg. Slides constrained to nav inner width
  (logo's left edge → Join button's right edge).
- Stats bar (27 teams · 600+ athletes · $1.4M raised since 2012).
- Three-ways cards.
- Impact split with $116,800 callout.
- **Platinum sponsor carousel** between Impact and Teams.
- **Seasonal Teams carousel** — auto-detects current trimester
  (Spring at the demo date), shows all 12 in-season teams in a
  scroll-snap carousel with prev/next arrows, drag-scroll on touch.
- Calendar + Watch split — calendar is now **live data** from
  `slotab-events.json` + scraped `weekly-events.json` (filtered to
  ≥ today, sorted ascending, top 5).
- Sponsor wall (dark section) with **alpha-channel logos** —
  pre-processed every existing logo via ImageMagick into
  `public/sponsors/alpha/`. Tiles white, hairline border, hover
  lift to gold.
- Hall of Fame strip (6 inductees previewed, links to full hall).
- Closing gold CTA — "One school. One pride."

### `/donate` and `/membership` updates
- `/donate` now captures donor identity (Name + Email required,
  Phone optional, Donor Wall opt-out checkbox). Every donation
  enrolls as membership at the matching tier (#37 + #49). Submit
  is stubbed for the Square integration which is gated on Q1.
- `/membership` "Proposed Tiers" prototype (5-tier draft from
  `docs/membership-tiers-research.md`) + sponsor wall + sponsor
  expandable. Old "Join Online" form removed (#49) — now links
  to `/donate`.

### Inner-page typography pass
- Updated `.slotab-scope h1/h2/h3/h4` to render in Tiger Source Serif
  (mixed case, weight 650). Auto-underline on every `<a>` removed —
  only `.slotab-prose` paragraphs keep underlined links. Net effect:
  `/about`, `/teams`, `/watch`, `/hall-of-fame`, `/impact`, `/contact`,
  `/spring-social`, `/upcoming`, `/season-passes`, `/merch`,
  `/volunteer` all visually align with the new design without a
  per-page reskin.

### Operations
- Weekly events scrape workflow fixed (was failing every cron run on
  a stale `tnn-videos.json` reference). Manual trigger 2026-05-06
  evening confirmed the scraper itself works; cron will now actually
  commit refreshes Sun/Mon/Wed.
- ImageMagick installed locally; `public/sponsors/alpha/` populated
  for all 4 tiers (~50 logos).

### Known follow-ups
- More real data (sponsor URLs in progress via Decap, real Impact
  ledger numbers pending Trina, real team rosters pending Adam).
- Square/QB sidetool architecture (Q1) still gating live donate.
- Springly + Hudl integrations 🔴 blocked on access (#48).
- Editorial design + mode-switch infrastructure deferred to
  post-demo.

---

## 2026-05-07 session — what shipped

Short follow-on after the 2026-05-06 rebuild, prompted by the
SLOHS network blocking `*.vercel.app` and the user previewing
on a phone.

### Custom domain via Raven's Peak (#51)
- Added `slotab.ravens-peak-consulting.com` as a custom domain on
  the `slo-tab-website` Vercel project (Settings → Domains).
- Cloudflare CNAME `slotab` → `cname.vercel-dns.com`, proxy off
  (gray cloud) so Vercel terminates SSL.
- Vercel auto-issued a Let's Encrypt R13 cert. Domain attached as
  the production alias of the latest deployment so it inherits the
  same auth posture as `slo-tab-website.vercel.app` (no SSO wall).
- No code changes — the canonical Vercel URL still works
  identically; this just adds an alternate hostname.

### Mobile responsive pass (#52)
Single-file CSS update to `src/app/tiger.css`. The desktop design
rendered with several issues on a 375-400px viewport:
- SiteBanner ate ~145px of vertical space before any content.
- Hero photo got sandwiched between dark side bands + a mask
  gradient that ate the photo's edges, leaving a narrow visible
  strip in the center.
- Stats grid collapsed to a 4-tall single column at <480px.
- Sponsor wall rendered as 1-up giant tiles for ~50 sponsors.
- Watch feature title vanished against busy game photos.

Fixes (commit `d8f2f82`):
- **480px tier**: SLO|TAB wordmark 40→28px, drop the dot separator
  in the tagline so it sits on one line, scale hero / closing-CTA /
  page-header headlines down. Stats grid stays 2×2.
- **720px tier**: hero slide + overlays use `left: 0; right: 0`
  (drop the desktop-only side bands) and disable the side mask
  gradient on the photo. Strengthen the bottom darkness gradient
  for legibility on the narrower crop. Sponsor grid forces 2-up
  for every tier with shorter tiles. Impact card stretches full
  viewport width with photo height 560→360. Watch feature shrinks
  to 280px with stronger blackout + text-shadow on the title.
- The (0,2,0) `.tiger-scope` overrides at the end of `tiger.css`
  outranked unscoped media-query rules for hero/CTA/page-header
  headlines + watch-feature title — re-asserted them inside the
  mobile blocks at matching specificity.

### Known follow-ups
- Real Hudl thumbnails on the watch feature will improve title
  contrast further once they land.
- A few tall sponsor logos may want tier-specific tile aspect
  ratios eventually (Crew Wealth, Adventist Health, etc. sit fine
  at 2-up but their proportions vary).
- iOS Safari sometimes hangs onto cached CSS — long-press refresh
  or Private Tab if a test viewer reports the old layout.

### Photo overhaul (#53–#55)

Same 2026-05-07 working session, after the mobile pass. Erik dropped
two batches of sport-specific photos into `public/photos/` along with
a filename convention to keep the directory navigable as it grows.

**Convention.** First letter `b`/`g`/`c` (boys/girls/co-ed), then a
short sport name. `bbaseball-` is the boys-baseball prefix because
`bball` was already needed for basketball.

| Token | Sport |
|---|---|
| `bball` | basketball |
| `bbaseball` | baseball |
| `fball` | football (boys); `gfball` is girls flag football |
| `vball` / `bvball` | volleyball / beach volleyball |
| `sball` | softball |
| `wpolo` | water polo |
| `swim` | swim & dive |
| `XC` | cross country |
| `track` | track & field |
| `cheering` | cheer / fan section |
| `stunt` | stunt |
| `golf` · `soccer` · `wrestling` · `tennis` · `lacrosse` · `fieldhockey` · `dance` | as-is |

**Coverage.** Every CIF varsity sport on `data/teams.json` now has
a sport-specific photo on the homepage seasonal-teams carousel and
(for the four pages with `hasPage: true`) the team-page hero.

Two slugs are using **other-school stock photos** as placeholders:

| Slug | Photo | Issue |
|---|---|---|
| `boys-lacrosse` | `blacrosse-20250501-025-9463.jpeg` | Jerseys say **LOS GATOS** |
| `field-hockey` | `gfieldhockey-JRW_FH_100923_206.jpg` | Jersey says **WILTON** |

Replace these when SLO-specific photos are available; both have
inline `// Note:` flags in `PHOTO_BY_SLUG`.

**Mid-flight corrections** (Erik review):

- `bbball-riley-…jpg` was actually a *girls* basketball photo —
  renamed to `gbball-riley.jpg` and dropped from the boys-basketball
  slot. (boys-basketball later filled by `bbball-e1765386832968.jpg`,
  the team celebrating with the student section.)
- `ccheering.jpg` was actually a body-paint *student section* photo,
  not the Cheer team — renamed to `cstudent-section.jpg`. The actual
  Cheer team photo (originally `gdance-taryn-…`) became
  `ccheering-taryn.jpg` and now powers the `cheer` slug.

**Originals directory (#54).** 25 old non-conformant files (date-
coded `081222SLOvPR`, `03122GVWaterPolo`, plus generics like
`tennis.jpg` / `volleyball.jpg` / `water-polo-*.jpg`) moved to
`public/photos/originals/`. Convention-named copies live alongside
the new photos in `/photos/`. All in-code references — `HeroCarousel`,
`ClassicHero`, `page.tsx` impact card + watch thumbs,
`PHOTO_BY_SLUG`, `hudl.json`, `impact.json`, `baseball.json` —
migrated to the new names.

**Hero carousel addition (#55).** `ctrack-2.jpg` (a SLO Tiger track
athlete, runner #14, leading a Templeton runner mid-stride) is now
the 6th slide on the homepage hero. The carousel was football-heavy
(tunnel + helmets + water polo + student section + basketball);
adding track gives spring-season balance.

**Bonus images not yet placed:**
- `gswim-old-1200x727.jpg` — older girls swim group celebration
  (gswim-10 is the primary; this is a backup)
- `bgolf-raf-1200x1082.jpg`, `ggolf-admin-ajax.png`, `bsoccer-1200x800.jpeg`,
  `gfball-saff{1,6,9}.jpg`, `bbball-1200x906.png` (originally mis-
  prefixed; was actually baseball, now `bbaseball-team-1200x906.png`)
  — alternates not in active use but available for rotation

---

## 2026-05-13 / 14 session — what shipped

Long working session after the 2026-05-11 board cocktail at the Hub. The board reviewed the live preview, surfaced concrete cleanup items, and Erik came back with the official 2025-26 team-photo zip + new Hudl admin access.

### Sports list cleanup (#56)
- Removed Boys Lacrosse + Field Hockey from `teams.json`, `TeamsCarousel` `PHOTO_BY_SLUG`, and `events.ts` (boys-lacrosse was in the `EventCategory` union + `CATEGORY_ORDER`).
- Moved Girls Water Polo Winter → Fall (Central Section runs both polos in fall, not Winter like Southern Section).
- Added Girls Wrestling (Winter). Renamed `wrestling` slug → `boys-wrestling` for parallelism.
- Split Cheer: `cheer` is Fall sideline; new `competitive-cheer` is Winter competition. Stunt unchanged (Spring).
- Stat-bar count `27 → 26 → 27` after the changes net out.

### Nav + home page (#57)
- "Shop" promoted to top-level nav item between Watch and Hall of Fame; "Merch" removed from Get Involved dropdown.
- Top-right "Join" CTA pill retired — Donate/Sponsor/Volunteer hero + Get Involved dropdown cover it (reverses #50).
- New "Wear Your Stripes" featured-gear strip on the home page between the Teams carousel and Calendar+Watch — three Spring shirts (Baseball / Track & Field / Beach Volleyball) on cream cards w/ hover lift, "Shop all designs →" link. Single `FEATURED_MERCH` array at top of `page.tsx` for seasonal swap.

### Team photo refresh (#58)
- Visual audit of every PHOTO_BY_SLUG entry vs. the actual rendered images. Flagged: girls-tennis (Clovis player in shot), boys/girls cross country (shared co-ed photo), boys/girls wrestling (shared dark portrait), competitive-cheer (shared fall photo), boys-tennis (student-section placeholder).
- Erik delivered a 393 MB zip with official 2025-26 team portraits for every Fall + Winter sport (plus stunt + girls swim). All photos resized to 1200px wide / 82% quality / metadata stripped → ~150–400 KB each.
- 7 carousel slots updated with action-or-team-portrait fit; 13 additional team portraits staged in `public/photos/` for the 23 future team pages.
- New **"Meet the Team"** section on individual team pages, sitting between Quick Facts and Donate CTAs. Renders only when a team has its `teamPhoto` field set. Decap CMS now exposes `teamPhoto` field (separate from `heroPhoto`). Wired for football, girls-volleyball, baseball (3 of 4 active team pages — track-field skipped, no Spring photo in this zip yet).
- 3 orphan photos purged: `cXC-image-1200x900.jpeg`, `bwrestling-checukk.jpg`, `gtennis.jpg`.

### Hudl integration — biggest unblock since #48 (#59)
The narrative:
1. Started by probing fan.hudl.com → can't iframe (X-Frame-Options SAMEORIGIN).
2. Found that **vCloud's per-broadcast iframes** at `vcloud.hudl.com/broadcast/embed/<id>` *are* CSP-clean. Built a proof on /watch with broadcast 3864918 (public College of Marin baseball game). Worked.
3. Erik got vCloud admin access; broadcast 4031110 (SLOHS boys volleyball, archived) plays. But "Broadcast Unavailable" overlay until Erik found the "Available" toggle.
4. Built a full custom catalog around it: `broadcasts.json` + `broadcasts.ts` helpers + `BroadcastCard` / `BroadcastGrid` / `BroadcastEmbed` components + `/watch/[broadcastId]` per-game pages + Decap "Watch — Broadcast Catalog" collection + per-team strip on team pages. All shipped.
5. Hudl Support then handed us an **official `<blueframe-app>` web-component embed** pre-configured to vCloud site 6609 with theme colors *already in SLOTAB gold*. The widget renders the full SLOHS portal — live + upcoming + archived rows, search, section filters — auto-populating from vCloud. Zero ongoing editor work.
6. **Dropped the entire custom catalog.** `/watch` is now the BlueFrame embed wrapped in a dark "broadcast surface" section under a SLOTAB-branded intro strip. Per-team page strips swap to a "Open the Tigers Watch Portal →" CTA. Net 760+ lines of code deleted.

**Open**: portal currently lists 4 archived broadcasts that are in error state (Hudl Support clearing them) and omits recent "Available but not Archived" broadcasts (4031110, the stunt event) — pending the publish-to-viewer workflow answer.

### Public vs No-Scout policy draft (#60)
Drafted for Adam/Phil's review:
- **Public by default (12)**: cross country (b+g), swim & dive (b+g), golf (b+g), tennis (b+g), track-field, sideline cheer, competitive cheer, stunt — measurable individual performance + judged routines have no scout value.
- **No-Scout by default (13)**: football, volleyball (b/g/beach), basketball (b+g), soccer (b+g), water polo (b+g), baseball, softball, flag football — set plays + tendencies legitimately scouted.
- **Borderline (2)**: wrestling (b+g) — default private, easy to open per match.
- All defaults overridable per-broadcast; playoffs may lock normally-public sports.

### Footer ligature fix (#61)
- Manrope ships `(c) → ©`, `(r) → ®`, `(tm) → ™` in its standard `liga` feature (verified by diff-test — `calt`/`clig`/`dlig` alone don't stop it).
- Six places rendering "501©(3)" instead of "501(c)(3)": footer (TigerFooter + legacy Footer), home stats bar, donate form tax-deductible bullet, about page, DonateForm disclaimer.
- Fixed at `.tiger-scope` body level via `font-feature-settings: "liga" 0, "clig" 0, "calt" 0, "dlig" 0`. Negligible visual impact (Manrope's letter ligatures are subtle).

### Doc maintenance handoff (#62)
- Added `CLAUDE.md` at repo root with a `docs/project-status.md` maintenance instruction so future Claude sessions inherit the discipline.

---

## 2026-05-25 session — what shipped

### Photo library — 24 additional curated game photos (#66)

Erik dropped two folders (`SLOHS Sports Photographs-part-{1,2}/`,
1.7 GB combined) into `public/photos/` for filtering — a 180-photo
raw dump from the team photographer. Worked through them
sport-by-sport, prioritizing the photographer's own flagged
variants first: files with `copy`/`5x7`/`8x10`/letter suffixes
are print versions the photographer prepared themselves, so they
tend to be their own picks.

**Final keepers (24)** — all resized via the standard recipe
(`magick … -resize 1200x\> -quality 82 -strip`) and renamed to
the `<b|g|c><sport>-<descriptor>.jpg` convention:

- **Boys basketball (6):** `bbball-shot-394.jpg` (hero — shot
  attempt over PR #11 defender, peak action), `bbball-drive-past.jpg`,
  `bbball-fastbreak.jpg`, `bbball-drive-portrait.jpg`,
  `bbball-huddle.jpg` + `bbball-huddle-bw.jpg` (locker-room
  storytelling shots)
- **Football (5):** `football-helmets-bw.jpg` (B&W team holding
  helmets aloft, stadium light + mountains in BG),
  `football-four-backs-bw.jpg`, `football-team-meeting.jpg`,
  `football-jv-helmets-bw.jpg`, `tigers-mascot.jpg` (studio
  mascot portrait, jersey #61)
- **Boys volleyball (2):** `bvball-net-action.jpg`, `bvball-spike.jpg`
- **Tennis (3):** `btennis-backhand.jpg`, `btennis-serve.jpg`,
  `gtennis-huddle.jpg` (team huddle with mountain BG)
- **Girls soccer (3):** `gsoccer-huddle.jpg`, `gsoccer-celebration.jpg`,
  `gsoccer-keeper-save.jpg`
- **Boys soccer (3):** `bsoccer-huddle.jpg`, `bsoccer-keeper-save.jpg`,
  `bsoccer-dribble.jpg`
- **Cheer + water polo:** `ccheer-team.jpg`,
  `gwpolo-coach-huddle.jpg` (named distinctly from the existing
  `gwpolo-huddle.jpg` in the hero rotation to avoid collision)

Rejected several photographer-flagged shots that had text overlays
baked in ("How About Them Tigers?", "Not in Our House",
"Tiger Nation", "3..2..1.. We have liftoff!") — unusable for the
site without retouching.

### Carousel — boys basketball + girls soccer

`src/app/components/tiger/ClassicHero.tsx` gains slides 7 and 8
for boys basketball (`bbball-shot-394.jpg`, "Boys Basketball ·
attacking the rim") and girls soccer (`gsoccer-huddle.jpg`,
"Girls Soccer · the huddle before kickoff"). Both sports were
absent from the hero rotation despite the broader photo refresh
in #53 and #58 — this fills those slots. Existing 6 slides
untouched. `objectPosition` tuned to keep the focal action
visible against the text overlay (basketball "center 30%" anchors
on the players' faces; soccer "center 45%" balances huddle +
sky). Verified live in the local dev server.

### Originals archive

Both source folders moved into
`public/photos/originals/SLOHS Sports Photographs-part-{1,2}/`
to match the originals-dir pattern from #54. Added a `.gitignore`
rule for those specific subdirs (`/public/photos/originals/SLOHS Sports Photographs-part-*/`)
so the 1.7 GB doesn't track or deploy — the existing 25 small
archival files in `originals/` remain committed as before.

### Coordination notes

This session started against a stale local clone — the working
checkout was 28 commits behind `origin/main`, and what looked
like in-progress untracked photos locally turned out to be files
already shipped on origin (#53/#54/#58). After initial commits
that conflicted on rebase, did a `git reset --hard origin/main`
(tagged the discarded commits as `backup/photos-2026-05-25` for
recovery), backed the 24 new keepers to `/tmp`, and re-applied
the work cleanly against origin's current state. **Lesson:**
fetch + status against `origin/main` early in any session, not
just before push.

---

## Pre-2026-05-11 demo polish (Erik flagged 2026-05-06 evening)

Items to land before the casual board cocktail at the Hub:

- [ ] **Real data on home page calendar** — current section uses 5 hardcoded events (Apr 9, Apr 13, Apr 18, May 4, May 12). Apr/early-May entries are now in the past. Either wire the home calendar to `data/weekly-events.json` + `data/slotab-events.json` (real data, auto-refreshing) or refresh the hardcoded list with future-only events.
- [ ] **Verify weekly events scraper is firing** — the GitHub Actions workflow at `.github/workflows/update-events.yml` schedules Sun 11pm + Mon 6am Pacific + Wed 14:00 UTC scrapes of the SLOHS athletic Google Sheet into `data/weekly-events.json`. The data file last updated 2026-04-23, so the cron may not have run since. Manually triggered 2026-05-06 evening — confirm it commits a refresh.
- [ ] **Purge stale events from the home calendar** — anything before today's date should drop off automatically once the calendar reads from the JSON sources.
- [ ] **Quickie color + font matching pass on inner pages** — `/about`, `/teams`, `/teams/<slug>`, `/watch`, `/hall-of-fame`, `/impact`, `/contact`, `/spring-social`, `/upcoming`, `/season-passes`, `/merch`, `/volunteer`. Don't fully reskin — just retune the legacy `.slotab-scope` typography to match the new Tiger design system (serif headings instead of uppercase Manrope, drop the auto-underline link styling, align colors). New chrome already wraps these pages.
- [ ] **More real data generally** — sponsor URLs (in progress via Decap), real Impact ledger numbers (pending Trina), real team rosters (pending Adam Basch).

---

## 2026-06-16 session — what shipped

A photo + team-pages sprint that started with one new photo and grew into a
full Teams expansion.

### Flag Football page, then the official portrait (#74, #75, #76)
Built the first new team page since the original four to host a girls
flag-football team photo, keeping the action shot in the carousel/index and the
portrait on the page (#74). The first photo turned out to be an earlier stand-in
already in the repo; Erik's real one (`Girls Flag Football Team Photo.jpg`, a
night game at Rio Mesa) was sitting in the **main checkout**, so it was processed
into the published `gflag-team-2025.jpg` slot and the stand-in archived (#75).
That surfaced a backlog of 15 unprocessed drops in `public/photos/` — 5 exact
dupes deleted, 10 archived to `originals/` (#76).

### Photo-intake helper (#77, #78)
To stop the staging backlog reforming, added a gitignored `photo-inbox/` drop
folder + `scripts/photo-intake.mjs` (`npm run photo-intake`) that audits drops
(dup-check, name-convention check, dims) and `--process`-resizes the conformant
ones. Added an interactive `--rename` step that infers sport + gender from the
original filename and suggests a convention name (#78). Two real bugs caught in
testing: `rl.question` only resolves once against piped stdin (switched to the
readline async-iterator) and the `magick` children were swallowing stdin (pinned
to "ignore").

### Teams expansion (#79, #80, #81)
Erik noticed the flag-football page never appeared in the nav dropdown (it was a
hardcoded list) and that many team photos sat unused. Three changes:
- **#79** — extracted `src/app/data/seasons.ts` (`currentSeason`/`navSeason`/
  `orderedSeasons`); the nav Teams dropdown now builds from `teams.json` filtered
  to the in-season teams (summer previews Fall), so new pages appear
  automatically. `/teams` index sections now lead with the active/upcoming season.
- **#80** — a `gallery` field + "In Action" grid on team pages, natural aspect
  ratios preserved, to showcase as many action shots as possible.
- **#81** — built the 16 pages for every team with a posed team photo (21 total),
  each with hero action shot + portrait + gallery. The 6 action-only teams wait
  for a posed photo. `CLAUDE.md` now documents the (now mostly automatic)
  team-page process.

---

## 2026-05-06 — Action Items (who owes what)

> **Read as history, not as a to-do list.** This section is a snapshot of 2026-05-06. Its Square items — Q1, T2, and the transaction-ID research — were all resolved between #144 and #164 and the integration went live on 2026-08-21. Left unedited so the record of what was actually pending that day stays intact.

| Owner | Item |
|---|---|
| **Erik** | Research Square transaction-ID + custom-metadata mechanics; share findings with Trina before committing to Square API vs hosted (#31, Q1) |
| **Erik** | Wire up the architectural changes from the transcript (#26–#42) on the Vercel-hosted preview |
| **Erik** | Preview demo at the casual cocktail board meeting at the Hub on **2026-05-11** |
| **Erik** | Draft `docs/coach-onboarding-slotab-funds.md` — 1-pager on how SLOTAB dollars work for coaches (#44) |
| **Erik** | Draft `docs/treasurer-square-qb.md` — Trina-facing operations doc for the Square→QB sidetool (#46) |
| **Erik** | Survey best-practice membership tier ladders for HS booster orgs; propose a SLOTAB ladder for the wine meeting (#37) |
| **Erik** | Secure Owner-level credentials for both Springly (Serenity) and Hudl (Pro) — both integrations tabled until then (#48) |
| **Board (Trina)** | Email Impact-page ledger data (dollars funded, items purchased, teams/programs) to Erik + team |
| **Board** | Schedule the Deneen + Ann wine meeting (within ~30 days of 2026-05-06) re: membership/sponsorship merge |
| **Board** | Discuss open Phase 0 considerations: sponsorship-bundle splits (e1), in-kind donations (e2), restricted-vs-unrestricted gifts (e4) (#47) |
| **Board** | Email the intern re: Booster Bash ticketing setup; sales launch 2026-07-01 |
| **Board** | Create + share the Events Google Sheet (Adam + Phil + liaisons + board); add 2026-2027 tab + SLOTAB events |
| **Adam Basch** (abasch@slcusd.org) | Volunteered to shepherd team-page templates with coaches during AD-handoff overlap with Phil |

---

## Built (already shipped)

Track removed/completed items here so they don't clutter the active backlog.

- ✅ Refactored 9-page WordPress → Next.js with the original visual language
- ✅ Modernized SLO|TAB masthead + 7-photo carousel + GO TIGERS!
- ✅ 50-logo sponsor wall sourced from JSON (Decap-editable)
- ✅ Hall of Fame page with 46 inductees scraped from school site, filterable by sport
- ✅ Hall of Fame **fundraising band + `hall-of-fame` donation designation** — 100% to the fund (no 75/25 split), object-based giving ladder with `?amount=` deep links, "in honor of" tribute carried in Square metadata, Trina's `Donation: Hall of Fame` QuickBooks class carried into the note/report/CSV, Decap-editable, thermometer built but hidden pending a real goal (#184)
- ✅ Impact ledger with By Year / By Team toggle (DRAFT data)
- ✅ Watch tab with Hudl placeholder grid + live badge support
- ✅ Upcoming events page with weekly-sheet scraper + sport filter
- ✅ Per-team pages: Football, Girls Volleyball, Baseball, Track & Field
- ✅ Standard Comms Kit playbook (internal-only) — Remind + BAND + ParentSquare with opt-out
- ✅ 6-item dropdown nav with persistent gold "Join" button
- ✅ Donate-first hero CTA replacing "Become a Member" + "Sign Up to Volunteer"
- ✅ Sponsor logos optionally clickable (when `website` field set)
- ✅ "Become a Sponsor" expandable + Print Sponsor Sheet button
- ✅ Decap CMS at `/admin` with 10 collections + Editorial Workflow
- ✅ Springly two-way stub (board admin portal + Join form + API routes)
- ✅ Security audit + fixes (CSRF state, SRI, postMessage origin, scraper sanitization)
- ✅ Standalone repo extracted to `~/slotab-website/`
- ✅ Board migration plan (`docs/migration-plan.md`)
- ✅ Decap setup guide (`docs/decap-setup.md`)
- ✅ Team comms kit playbook (`docs/team-comms-kit.md`)
- ❌ Tiger News Network — removed (Hudl is sole video source)
- ❌ NFHS Network placeholder — removed
- ❌ Auto-pulled team-page schedule — removed (schedule lives only on `/upcoming`)
- ❌ "For Players & Parents" team-page comms strip — removed (comms internal-only)
- ❌ "Three Ways to Support" home page cards — replaced by Donate-first hero

---

## Impact rollout plan — per-team thermometer progression

Approved 2026-05-06 as a phased path. Goal: ship a useful general thermometer at go-live, then earn the right to per-team thermometers by getting tracking, workflows, and coach-clarity right first.

### What the plan needs to solve (from the 2026-05-06 working session)

- **(a)** Solid efficient tracking of donation intent (general / sport / big-ticket) so dollars can be matched to budgets
- **(b)** Workflows to support tracking + reporting
- **(c)** Coach clarity — some currently believe they receive zero from SLOTAB
- **(d)** Transparency at the right level — donors see impact, no perception of unfair allocation
- **(e3)** Year-over-year comparison so the Impact page works mid-year ✅ approved
- **(e6)** Coach onboarding 1-pager ✅ approved (#44)
- **Pending board discussion:** (e1) sponsorship-bundle splits · (e2) in-kind donations · (e4) restricted-vs-unrestricted gifts (#47)

### Phase 0 — Foundations (before any thermometer is live)

| Workstream | Solves |
|---|---|
| Donation intent taxonomy: `general` · `sport=<slug>` · `big-ticket=<line-item>` · `sponsorship-bundle=<id>` | (a) |
| Square ↔ QB sidetool: pulls Square, applies tag, exports monthly QB-ready CSV | (a)(b) |
| Transaction-ID schema (e.g. `B-FB-S25` = Big-ticket / Football / Spring 25) | (a)(b) |
| Refund + correction workflow with audit trail in QB | (b) |
| Recurring-donation intent persistence (designation rolls forward each month) | (a)(b) |
| Trina-facing operations doc (#46) | (b) institutional knowledge |
| Coach onboarding 1-pager (#44) — frames the 25% as general fund, not overhead (#43) | (c) |

### Phase 1 — General thermometer (target: 2026-08-01 go-live)

- One thermometer on `/impact` anchored to treasurer's planned-budget figure
- Optional drive breakdown (fall / winter / spring)
- Monthly update cadence + 75% disclaimer
- **Year-end "Totals by Team" table** (no goals — just dollars in / dollars allocated)
- General-fund explainer block (what the 25% covers)
- YoY comparison (#45)

### Phase 2 — Team-level transparency (no goals yet)

- Each team page: "FY raised toward this team · 75% allocated · big-ticket sub-totals"
- **Coach-facing view** (gated): donor list + designation
- Public language emphasizes *what was raised*, not *what was spent on what* — preserves (d)
- Requires Phase 0 reliable for at least one full reporting cycle

### Phase 3 — Per-team thermometers with goals

- Goal-setting playbook: liaison drafts → board reviews → coach validates
- Stretch-goal states (don't stop at 100%)
- Big-ticket items as separate sub-goals
- Carryover policy at fiscal year end

### Phase 4 — Donor recognition + feedback loop

- Anonymous-default toggle on the donate form
- Year-end donor thank-you email tying their gift to outcomes
- Goal-met thank-you flows

---

## How to update this doc

When something lands or a decision is made:

1. **Decision** → append a row to *Decisions Log* with today's date and a one-line summary.
2. **Built / shipped** → move the item from *Active Backlog* to *Built*, prefix with ✅ or ❌.
3. **Blocked or deferred** → mark 🔴 / ⏳ with a one-line reason, leave in *Active Backlog*.
4. **External input received** → strike or remove the row from *External Inputs Pending* and update the relevant data file.
5. **Save & commit** with a short message like `docs: status update — [thing]`.
